# AGENTS.md

The guidance for this repository lives in [`CLAUDE.md`](CLAUDE.md). It is
tool-agnostic despite the filename: what it documents is the repo's
architecture, build and quality gates, conventions, and testing standards.

**Read [`CLAUDE.md`](CLAUDE.md) before working here**, whichever agent you are.

This file is deliberately a pointer, not a copy. It previously held a
near-verbatim duplicate of `CLAUDE.md` differing only in the title line, which
would have drifted out of sync the first time either file was edited.
