# Rebuild session state

Updated September 10, 2026. Active, unbudgeted goal: implement all 16 approved tasks and R01–R16. Execute inline. User now wants **full offline mocks for Google and Synology**; live acceptance is deferred, not verified or a current blocker. No native apps. No commits, pushes, merges, installs, publication or live-account access. Preserve original source/data and unrelated work.

## Workspace and authority

- Worktree: `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`; branch `feature/nuncio-google-first-rebuild`; baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`.
- Run Cargo from `rebuild/`, or use `--manifest-path rebuild/Cargo.toml` from this worktree. Separate nested Rust 2021/1.97.1 workspace; CLI has no engine dependency. Four production crates plus unshipped test-support.
- Approved spec/plan: `../../docs/superpowers/{specs,plans}/2026-09-10-google-first-rebuild.md`; audit `../../docs/reviews/2026-09-10-nuncio-audit-and-rebuild.md`. Read repository AGENTS→CLAUDE, rebuild/AGENTS, shared personal rules/style and these state docs after reset. Existing approval overrides issue/commit/delegation/checkpoint defaults in skills. Do not restart planning.
- Detailed historical checkpoints preserved in [history/2026-09-10-through-send-enqueue.md](history/2026-09-10-through-send-enqueue.md). [VERIFICATION.md](VERIFICATION.md) has commands/results, [TODO.md](TODO.md) tracks remaining tasks. Nothing is committed.

## Current implementation checkpoint

Task 09 is active. Draft CRUD, upload, reply/reply-all/forward, frozen send enqueue, operation get and queued cancellation now work through Engine, authenticated API and actual CLI. **No provider write dispatcher exists yet.** Operation list/history and audited resolution are implemented; successful resolution through provider-driven system/E2E scenarios, complete state-machine acceptance, and provider writes remain. No R01–R16 is fully marked complete.

Source schema **10**. Test binaries at `target/test-harness/debug/{nunciod,nuncio-cli}` were rebuilt for enqueue E2E (schema 10); the latest E2E build includes list/history/resolve and the shutdown wrappers. Subsequent storage changes add SMTP partial receipts and richer desired send metadata; those are not yet in the last E2E artifacts. Production binaries at `target/production/release/{nunciod,nuncio-cli}` still represent verified Task 08/schema 6. Do not use those production binaries on a schema-10 profile or claim final artifacts are current.

### Drafts and MIME

- `domain/drafts.rs`: strict email_address 0.2.9 addr-spec validation, recipient arrays, header injection rejection, up to 1000 recipients, 1 MiB combined text/HTML. Incomplete local drafts allowed; sending requires recipients. Explicit account supplies From.
- Schemas 7–9 and `store/{drafts,draft_uploads,prepared_drafts}.rs`: optimistic draft versions, account ownership, revision-bound pagination, context and immutable blob references. Uploads stage encrypted 256 KiB chunks; 64 MiB aggregate attachments, 256 parts, four active leases. EOF verifies size/hash/version before one transactional publication/change revision. RAII owns a reserved cleanup queue slot and worker lifetime; cancellation cleans staging, startup removes crash leftovers. Upload RPC stops on daemon shutdown or 30-second deadline. CLI hashes regular files before pull-based streaming and rejects changed/unreadable files.
- `domain/mail.rs` already transfer-decodes attachments without charset normalization; `with_parsed` drains nested parser messages iteratively on drop. Do not repeat the stale warning that text attachments still become UTF-8.
- `domain/prepare.rs`: Reply-To arrays, To/Cc deduplication and selected-address self exclusion, Bcc exclusion, Message-ID/References including single In-Reply-To fallback, preserved forward text/HTML and attachments with MIME parameters/inline CID. `Engine::prepare_draft` uses downloaded originals, one composition memory permit, blocking parsing, and an atomic draft/blob/context transaction. Edits preserve context. Additional aliases are not inferred.
- `domain/submission.rs`: mail-builder 0.5.0 (default features disabled) supplies MIME assembly. Fixed Message-ID/Date; multipart related/alternative/mixed; leaf attachments explicitly base64 to preserve bytes. SMTP wire strips Bcc with parser header offsets; local Sent copy retains it; Gmail submission includes Bcc for envelope delivery. Changed reply subjects clear Gmail thread targeting. Encoded wire capped at 64 MiB. Nested message/rfc822 or multipart attachments use raw 8bit: strict SMTP transport validation/negotiation is still owed. mail-builder was fetched/locked successfully; no installation.
- Proto mail.proto and daemon/CLI drafts modules implement save/show/list/delete/attach, reply/reply-all/forward. Optional attachment --charset/--inline/--content-id. RUNNING.md documents bounds and commands. Attachment remove/download from drafts remains polish owed before final acceptance.

### Operation journal foundation

- `store/operation_schema.sql`, `operations.rs`, `operation_attempts.rs` implement schema 10. Tables: operations, send_payloads, operation_attempts, operation_receipts, operation_resolutions. Composite account ownership and unique `(account_id,request_id)`; all eight operation states constrained in SQL. Audited terminal resolution actions now use operation_resolutions.rs; all mutations remain schema 10.
- EnqueueSend uses INSERT ON CONFLICT inside the transaction, compares canonical request fingerprint, validates snapshot version on first insertion, and atomically stores desired send intent/frozen wire/Sent blobs and one operation revision. Canonical request is version/kind/draft_id/optional expected_version. Identical retry returns the original even after editing/deleting the draft; changed request conflicts. Frozen bytes never depend on the current draft afterward.
- Engine send facade checks identity before and after composition admission. Up to 64 small send waiters, one composition memory permit; permit remains owned through the queued SQL write and cancellation. Concurrent same-identity RPCs coalesce to one operation. SendDraft RPC has a 30-second/shutdown-aware wrapper; retry the same identity after a lost local acknowledgement.
- Attempts distinguish Dispatch from Reconcile. Begin dispatch admits queued/due retry_wait; reconcile admits uncertain+needs_reconciliation; one unfinished attempt/account via unique SQL index. Recovery atomically ends interrupted attempts as uncertain, sets reconciliation work and emits operation revisions. Startup calls recovery before servers/jobs. Ordinary dispatch/cancel cannot bypass uncertainty. Rejected reconciliation reads cannot prove non-delivery. Applied requires a typed receipt kind matching the operation/provider. Repeatable retry currently only permits mail_change, never send/calendar/copy. Reconciliation that remains unknown stops automatic reconciliation looping.
- Remaining correctness work: full rejected/retry/conflict/uncertainty matrix, provider-driven resolution system/E2E, typed mail/calendar payloads and projection intent views. Current cancellation supports queued only. List/history use account/query/page-size/global-revision-bound keyset pages (1–100); attempts newest first, receipts include observed_at_ms. Library all-attempts helper is bounded to 1000 attempts. SMTP acceptance now has a separate durable receipt before Sent copy; Sent-copy completion requires that prior acceptance, and copy failure cannot relabel accepted SMTP as rejected. Strict SMTP/IMAP protocol execution is still Task 12.
- Public current methods include Engine send_draft/operation/list_operations/operation_attempts/cancel_operation/resolve_operation; Mail SendDraft; Operations ListOperations/ListAttempts/GetOperation/CancelOperation/ResolveOperation; CLI `mail send --account --draft --request-id [--version]`, `operation show/cancel --account --operation` (`operations` alias). Enqueue success is not delivery. No --wait yet. Paused accounts can enqueue offline. No provider send/copy/notification effect has been performed by production code.


### Resolution checkpoint

- ResolveOperation requires expected operation version and one explicit decision. Only uncertain/conflict work without an active attempt is eligible. One terminal audit row retains full decision/evidence, version and replacement ID; identical retries return the original with no extra revision, including after lost local acknowledgements. Changed decisions conflict.
- Abandon preserves uncertain/conflict state with abandoned disposition. Google-send ConfirmApplied requires provider message ID, matching frozen RFC Message-ID, bounded observation timestamp and explanatory note; state becomes applied/manual_confirmed without rewriting provider attempts or inventing a provider receipt. Confirmation for Calendar/mail changes/Synology is deliberately still rejected until their concrete evidence models are implemented in 10–12.
- Resend requires a fresh canonical UUID and explicit duplicate-risk acknowledgement. Engine reloads the immutable original after draft deletion, regenerates only outer Message-ID/Date via parser offsets and mail-builder header encoding, and atomically commits a new queued operation plus the original resolution. Body bytes, recipients, thread and SMTP Bcc separation stay intact. Memory permit is held through blocking composition and queued SQL commit. CLI: `operation resolve --account A --operation O --version V --file decision.json`; resend additionally requires `--accept-duplicate-risk` and emits the duplicate-delivery warning. Operation show includes audit rows; newly queued desired metadata includes frozen Message-ID/sender/recipients/version.
- Latest checks: feature workspace Clippy exit 0 before SMTP additions; operation_system 4 passed; google_e2e 13 passed with freshly built actual binaries (includes auth/queued-resolution rejection and CLI duplicate-risk guard). Engine frozen_message + operation_recovery + operation_resolution + smtp_operation: 6 passed, exit 0, before the richer desired metadata edit. Earlier pages/recovery targeted 2 passed and list/history system/E2E passed. Full Task 09 normal/feature gates pending.
- Behavioral reds fixed: an Applied Google receipt without provider ID was accepted; a Sent-copy receipt marked SMTP send applied without SMTP acceptance. Both now reject; automatic attempt finish also rejects manual_confirmation receipts. SMTP final acceptance can omit queue ID because the protocol does not require one, persists across restart, and remains separate from the later positively acknowledged/read Sent placement.
- Added tests operation_resolution.rs (3) and smtp_operation.rs (1); expanded frozen_message.rs/recovery, operation_system and actual draft_e2e. Positive RPC/CLI resolution is not yet claimed: no production dispatch exists to create real uncertain outcomes. No new remote effects, real account access or live compatibility claims.

## Latest verification

All automated accounts/secrets are synthetic; only numeric-loopback services are used. Appropriate test runs used narrowly authorized sandbox escalation for local listeners. No remote CI or real-provider compatibility claim.

- `cargo test --locked -p nuncio-engine --test draft_domain --test draft_storage --test draft_upload --test draft_prepare --test mime --test calendar_schema`: exit 0, **10 passed** at schema 9 checkpoint. Includes non-UTF-8 fidelity, injection, scope/encryption, upload hash/offset/partial/lease/cleanup, and begin→concurrent edit→finish version rejection. Schema assertions subsequently updated exactly to 10.
- `cargo test --locked -p nuncio-engine --test operation_storage --test operation_recovery --test frozen_message`: exit 0, **3 passed** after send facade/guard work. Enqueue identity/immutability/cancellation/encryption, interrupted dispatch→reconcile and no blind redispatch, positive receipt, SMTP Bcc separation and exact named attachment bytes.
- `cargo test --locked -p nuncio-test-support --test operation_system`: exit 0, **4 passed**. Draft CRUD/upload/preparation/auth/restart plus eight simultaneous identical SendDraft requests, paused queue identity after edits/deletion/restart and independent zero sends.
- Latest targeted `... --test operation_system send_enqueue_rpc`: exit 0, **1 passed / 3 filtered**. Strengthened exact global revision delta + WatchChanges account/resource assertions prove the eight concurrent calls emit one recoverable operation change.
- `python3 scripts/verify.py --suite google_e2e`: exit 0, **13 passed / 0 failed / 0 ignored**, with actual freshly built daemon/CLI. Evidence at `test-results/google_e2e/{results.json,google_e2e.log,runs/}`. Drafts survive crash/full resync; offline reply/forward survives crash; paused queue identity survives crash/draft deletion and cancellation. Independent provider counters remain zero for these local-only actions.
- Independent new raw HTTP/mailparse reply-forward fixture contract: exit 0, **1 passed / 22 filtered**. Full mock suite now has 23 cases; latest whole-suite verified checkpoint remains Task 08's 22 until rerun.
- Latest feature workspace Clippy exits **0**. Initial failure from new shared Operations client helpers being dead in google_system's private support module was fixed by making reusable test support public, matching operation_system. No assertions or production lint rules removed. Latest WatchChanges test-only addition was afterward.
- Earlier compiler-only reds: missing new APIs, wrong SystemHarness method, wrong AccountRequest type, moved ID borrow. Frozen MIME behavioral red was an attachment-order assumption; fixed by identifying filename/CID while preserving exact bytes, count and charset assertions.
- Task 09 full normal/feature gates and final production rebuild remain pending. Do not count overlapping configurations as distinct tests.

## Next concrete action

1. Finish full outcome/cancel/receipt guards and provider-driven resolution tests. List/history now work through all layers; resolutions work in storage and API/CLI negative guards, with successful real-provider-effect paths owed to Task 10.
2. Implement Google write worker and send adapter, with tests first against the already independent mock: provider acceptance receipt, positive read reconciliation, uncertain absence, no blind retries after 5xx/lost acknowledgement, explicit resend and independent accepted-send counts. Integrate operation worker into owned shutdown and account/global coordination. Add actual CLI --wait and inspectable failure receipts.
3. Finish Task 09 requirements, then Google dispatch/mutations (10) with independent accepted-send counts under crash/lost acknowledgement; Calendar writes/freebusy (11) with independent notification counts; full Synology mocks and strict local independent IMAP/SMTP servers (12).
4. Maintenance (13), adversarial/resource/security bounds (14), current local artifacts and CI egress denial/workflows (15), acceptance worksheet/final R01–R16 matrix (16). Live acceptance remains deferred until separately authorized.

## Prior verified foundation and outstanding cross-feature work

Tasks 01/02/04/05/07 verified offline. Task 08 reader runtime gate at `test-results/task08/run-gate.py` passed eleven commands, 85 normal/85 feature tests (overlap), separate mock 22/system 16/E2E 10/release isolation 1. Owned scheduler, per-account lane before global two-account semaphore, coalescing, cancellation, persistent numeric/HTTP-date Retry-After, paused auth, wake catch-up and revision stream are implemented. Independent mock observations attribute revoked/OAuth attempts through private credential hashes without granting authorization. Test clocks advance across restart; feature-only clock-offset-ms simulates wake.

Task 03 still owes CI egress denial with 12/15. Task 06 runtime crash reconciliation passed page caps 1/10; pending-operation preservation through projection reset still owed. Task 08 operation-transition stream assertions now cover enqueue but full attempt/resolution transitions remain. Task 14 keeps maximum-payload/parser memory, worker-lifecycle stress (earlier SQLCipher assertion-unwind SIGSEGV mitigation not a proven root cause), stalled payload streams and aggregate draft gRPC response limits (default 4 MiB may be below worst-case metadata+body). Docker 29.7.2 was available; no Synology/IMAP containers have been started. All details/older evidence are in VERIFICATION and the archived session history.
