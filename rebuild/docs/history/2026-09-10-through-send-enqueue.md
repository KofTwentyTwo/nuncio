# Rebuild session state

Updated: 2026-09-10. Active goal: fully implement R01–R16 and all 16 approved tasks. Execution is inline. Branch: `feature/nuncio-google-first-rebuild`; worktree: `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`; baseline: `a268fd5a004977b65fd0347ceeb1c69755529aa3`. No issue required. No commits/pushes/merges/install/publication/live actions authorized. Root source and original profiles are preserved; root `.gitignore` gained the worktree exclusion, without a commit.

## Current checkpoint

Latest checkpoint: Engine::send_draft now freezes an immutable draft snapshot and durably enqueues it. It checks request identity before touching mutable drafts, serializes bounded MIME work, rechecks identity after admission so concurrent identical requests all return the same operation, and retains the composition permit through the queued SQL write. At most 64 send admission waiters; one composition memory slot. SendDraft has a 30-second/shutdown-aware RPC wrapper; a lost local acknowledgement requires retrying the same request identity. New authenticated Operations GetOperation/CancelOperation and CLI `mail send --account --draft --request-id [--version]`, `operation show/cancel --account --operation` exist (`operations` alias accepted). Sending currently only enqueues: no provider dispatcher or --wait exists yet. Paused/disconnected accounts can queue offline. Complete list/history/resolve methods still pending.

Latest observed checks: operation_storage + operation_recovery + frozen_message exit 0 (3 passed). Feature workspace Clippy before the latest API test additions exits 0. `cargo test --locked -p nuncio-test-support --test operation_system` exits 0 (4 passed, zero failed/ignored). It tests 8 simultaneous identical SendDraft RPCs returning exactly the same operation; auth on SendDraft and both Operations methods; changed request payload conflict; draft edits/deletion and restart preserving queue identity; paused account yields independently zero accepted sends. Initial new system compile used nonexistent DisconnectAccountRequest; corrected to actual AccountRequest. Actual `python3 scripts/verify.py --suite google_e2e` exits 0 with a fresh schema-10 test build and 13 subprocess cases, including paused queue identity through daemon force-kill/draft deletion and cancellation, with zero provider sends. E2E logs/results at test-results/google_e2e/. The final SendDraft shutdown wrapper was added during that build run, so its exact binary inclusion is not established; source system tests cover the latest source. Latest feature Clippy initially failed because shared Operations client helpers were considered dead code in google_system’s private support module. Made that reusable test support module public, matching operation_system; no assertion or production lint was removed. Feature Clippy rerun exits 0. Production artifacts still schema 6; no Task 09 full normal/feature gate or production rebuild yet.

Next action: implement bounded operation listing and attempt/receipt history, complete outcome/state tests (rejected retry/definitive fail, conflicts, unresolved outcomes), and audited resolution. Keep abandonment visibly uncertain, only acknowledge positive success evidence, and require explicit duplication-risk acknowledgement plus a fresh request identity for resend; retain frozen content independent of deleted drafts. SMTP accepted/send-copy substeps still need durable partial receipts before Task 12 dispatch. Then Google send/mutation executor (Task 10), Calendar writes (11), full independent Synology services (12), maintenance/bounds/artifacts (13–15), and deferred live worksheet (16). Task 06 pending-operation preservation through projection reset and Task 08 operation transition stream integration are still owed. Do not mark Task 09 or goal complete.

Operation journal foundation is now implemented in store/{operation_schema.sql,operations.rs,operation_attempts.rs}; source schema 10 and exact schema assertions updated. EnqueueSend atomically inserts unique (account_id,request_id), canonical request fingerprint, desired send intent, immutable wire/Sent blobs and metadata, then one operation change revision. It checks the draft snapshot version on first insertion; identical retries return the original operation even after draft edits/deletion, while a changed expected-version/request fingerprint conflicts. Queued cancellation is durable/idempotent. No Operations RPC/CLI or Engine send facade exists yet.

`cargo test --locked -p nuncio-engine --test operation_storage`: exit 0, 1 passed. It tests concurrent duplicate requests, changed payload conflict, stale first enqueue rollback, ownership, frozen bytes surviving draft edits/deletion and reopen, cancellation and exact operation revision. Initial missing-interface compile red was followed by a borrow error in the blob lookup closure; cloning its ID fixed the borrow without changing semantics. Feature workspace Clippy then exited 0.

New operation_recovery test first failed compilation on absent attempt/recovery interfaces. Added Dispatch versus Reconcile admission, atomic attempt/state changes, typed outcomes, bounded receipts, and startup recovery before servers/jobs. Running work becomes uncertain with needs_reconciliation; ordinary dispatch/cancellation cannot bypass it. A rejected reconciliation read cannot establish non-delivery. Positive receipt can finish reconciliation. RetryRepeatable currently permits only mail_change, never send/calendar notifications/copy. Schema has durable attempt/receipt/resolution tables, but resolution actions, list/pagination, partial SMTP receipt substeps and full outcome matrix testing are not implemented. Operations remain storage-only; no remote actions have occurred. `cargo test --locked -p nuncio-engine --test operation_storage --test operation_recovery` exits 0 (2 passed, zero failed/ignored). Feature workspace Clippy exited 0 at that checkpoint.

Next action: finish attempt/outcome tests and audited resolution, expose operation inspection/list/cancel/resolve and send enqueue through Engine/proto/CLI, retain composition admission permits through frozen MIME queue writes, add request-identity lookup before freezing on retries, and validate via separate operation_system plus actual subprocess E2E. Then implement real Google executor in Task 10 (no blind resend) and Calendar mutations in Task 11. Old production artifacts schema 6; test binaries schema 9; source schema 10. Full mocks for both providers remain the scope; Synology strict local servers are still Task 12. Do not mark Task 09 or the goal complete.

Frozen MIME work has begun: added mail-builder 0.5.0 with default features disabled to workspace/engine; `cargo fetch` exits 0 (one new locked package, no installation). `domain/submission.rs::freeze` validates sender/recipients, fixes caller-supplied Message-ID/Date, preserves reply headers, clears Gmail thread targeting on changed reply subjects, creates multipart alternative/related/mixed structure, and bounds encoded wire MIME at 64 MiB. Leaf attachment base64 is built with the existing base64 crate plus bounded MIME line wrapping so inline text bytes are not newline/charset-normalized. Nested message/rfc822 and multipart attachments retain raw 8bit bytes; future strict SMTP must validate/announce their transport requirements. SMTP wire excludes Bcc using mail-parser header offsets while its separate local Sent copy retains Bcc; Gmail submission retains Bcc for envelope delivery. No outgoing provider calls or journal integration exist yet.

`cargo test --locked -p nuncio-engine --test frozen_message`: initially failed missing module after dependency fetch, then failed an attachment-order assumption (inline PNG appears before cafe.txt in the related MIME structure). Corrected the assertion to identify the exact filename/CID, retain exact byte checks, assert attachment count and charset, and prove the private Bcc address is absent from SMTP wire. Current targeted result exits 0 (1 passed). Feature workspace Clippy exits 0 before that last test-only assertion adjustment. Frozen bytes are not yet stored/queued; next action remains operation schema/domain/store/API/CLI and enqueueing immutable send snapshots. API/CLI E2E binaries still represent the verified draft-preparation source before this pure frozen-MIME module addition. R05/R08 are not complete.

Latest Task 09 checkpoint: reply/reply-all/forward preparation now works through engine, authenticated PrepareDraft, and actual CLI. Schema 9 adds draft context and attachment/upload MIME parameters. Domain prepare.rs retains Reply-To arrays, visible To/Cc, self exclusion, Message-ID/References (including single In-Reply-To fallback), plain/HTML forward bodies, exact non-UTF-8 attachment bytes and inline Content-ID/disposition. Prepared drafts are created with their blobs/context in one SQLCipher transaction; a per-profile memory permit stays owned through parsing and the queued write, including cancellation. Original raw bytes are resolved with account ownership, length/hash checked, and parsed off the async reactor. Draft edits preserve reply context; source resync cannot rewrite local draft tables.

Evidence: `cargo test --locked -p nuncio-engine --test draft_domain --test draft_storage --test draft_upload --test draft_prepare --test mime --test calendar_schema` exits 0 (10 passed, zero failed/ignored). Explicit upload begin→concurrent edit→finish proves publication rechecks version. Decoded header injection is rejected. Feature workspace Clippy exits 0. New independent raw HTTP/mailparse fixture contract passes (1, 22 filtered); full mock suite is now 23 cases but has not been rerun together at this checkpoint. `cargo test --locked -p nuncio-test-support --test operation_system` exits 0 (3 passed), including auth/isolation/offline prepare, context-preserving edit, upload failures and held-stream shutdown. Initial prepare system compile used nonexistent h.system(); corrected to existing h.authenticated(). `python3 scripts/verify.py --suite google_e2e` exits 0 with fresh test binaries, 12 actual subprocess tests passed. Reply/forward E2E stops the independent mock before preparation, checks unchanged request counts and no sends, then kills/restarts the daemon and compares complete drafts. No live compatibility or remote CI claim. Source/test binaries schema 9; production artifacts still Task 08 schema 6. Fixture provenance and operating commands updated in fixtures/README.md and docs/RUNNING.md.

Next concrete action: durable operation journal and frozen MIME for send, followed by dispatch in Task 10 and Calendar writes in Task 11. Native apps excluded. Implement atomic account/request uniqueness, desired state plus intent, complete state transitions, durable attempts/receipts, cancellation before dispatch, restart reconciliation and explicitly audited uncertain/conflict resolution. Do not blindly resend. Mail-builder 0.5.0 official docs were read (stored reference) but dependency has not yet been added. Potential draft polish to include before final acceptance: attachment remove/download through API/CLI; bound maximum valid aggregate draft response (default gRPC 4 MiB may be less than worst-case metadata+body); test prepared draft survival when original message is deleted/full-resynced; stress cancelled parser/storage lifetimes in Task 14. Full normal/feature workspace gates remain pending for Task 09. All Tasks 10–16 remain. Earlier checkpoint paragraphs below are historical, superseded where they say preparation is missing.

Task 09 is in progress. Durable draft CRUD and chunked attachment upload now work through the engine, authenticated Mail API, and actual CLI. Schema 8 adds encrypted draft_uploads/draft_upload_chunks staging; source is in engine/store/{drafts,draft_uploads}.rs and draft_*_schema.sql, engine.rs, proto mail.proto, daemon {drafts,mail}.rs, CLI {drafts,draft_upload}.rs. CLI uses regular local files, hashes before streaming, and sends 256 KiB chunks with expected size/hash/version. No daemon file paths. Four uploads/profile, 64 MiB aggregate draft attachments, 30-second RPC deadline. RAII guards reserve cleanup queue space and hold worker ownership; cancellation enqueues cleanup and startup removes crash leftovers. Only complete digest/version-validated data publishes an immutable blob, attachment and one draft revision atomically. No attachment removal/download or reply/forward preparation yet; operation journal and all writes remain pending.

Latest checks: cargo test --locked -p nuncio-engine --test draft_upload exits 0 (1 passed); cargo test --offline -p nuncio-test-support --test operation_system exits 0 (2 passed); feature workspace Clippy exits 0. Commands run from rebuild/. Upload system coverage includes authentication, malformed order/chunk/offset, incomplete EOF, wrong digest, concurrent stale edit, zero-byte attachment, held-stream shutdown within five seconds and restart. Initial system compile failed because tokio-stream was missing from test-support dev-dependencies; reused the existing workspace dependency. New CLI futures-util likewise reuses the lock dependency. Latest `python3 scripts/verify.py --suite google_e2e` exits 0 with fresh test binaries and 11 passing actual subprocess cases (zero failed/ignored); logs in test-results/google_e2e/. Draft case verifies attachment size/hash, stale edit/delete, wrong account, header injection, daemon force-kill/restart and full provider projection reset preserving the entire draft. No sends recorded independently. Earlier missing save/attach reds remain historical evidence. Normal/feature full workspace gates have not yet been rerun for Task 09; production artifacts still verified Task 08/schema 6, test binaries now schema 8.

DraftContent validation uses email_address 0.2.9: addr-spec, no injected header/control characters, 1000 recipients and 1 MiB combined body. Drafts allow incomplete composition. CRUD domain/storage tests previously passed (one each). Required operation_system suite and shared OAuth helpers exist. All earlier Task 08 evidence below remains valid for that checkpoint, not full current source verification.

Next concrete action: reply/reply-all/forward creation with self exclusion, preserved threading and attachment MIME parameters, then durable operation journal. Inspection confirms domain/mail.rs already transfer-decodes attachment bytes without charset conversion and mime.rs has the non-UTF-8 cafe-byte regression. The earlier warning that this code still normalized attachment bytes was stale. Forwarding must still preserve charset/disposition parameters (currently only MIME essence is stored in attachment metadata); add independent fixture/system/subprocess assertions. Draft editing must not modify frozen enqueued MIME. Task 06 still owes operation survival; Task 08 still owes operation-transition revisions. Tasks 10–16 and CI egress denial remain. Full offline mocks for both providers are current scope; no live setup requested or live calls authorized. Do not stop at this draft milestone.

Task 08 reader runtime is verified offline. `python3 rebuild/test-results/task08/run-gate.py` exits 0; all eleven commands exit 0: fresh test-harness and production builds, fmt, normal/feature Clippy, workspace tests 85 each, separate mock 22/system 16/actual subprocess E2E 10/release isolation 1. Counts overlap; zero failed/ignored. Production binaries are current at target/production/release/{nunciod,nuncio-cli}, schema 6. Initial gate failed only on the historical Calendar schema assertion (4 versus implemented 6); its logs are retained at test-results/task08/initial-schema-assertion-failure. The corrected exact assertion and all original ownership/encryption checks pass. No remote CI or live-provider compatibility is claimed.

Implemented: owned scheduler, independent Google mail/calendar schedules, per-account lane before two-account global semaphore, coalescing/cancellation, persistent Retry-After seconds/HTTP-date and bounded exponential jitter, scoped API throttling/shared OAuth throttling, auth pause/reconnect, monotonic wake catch-up, recoverable bounded change stream, atomic run progress and detailed status, and inspectable error.sync_run in CLI --wait diagnostics. Store restart now marks interrupted scope timing/error as well as its run. A stalled ChangeReader explicitly returns ChangeHistoryExpired. Mock observation uses private hashes of issued synthetic credentials to attribute OAuth and revoked attempts without authorizing them; independent wire conformance tests establish that property. Test clock offsets are feature-only and separate from the independent mock clock; harness epochs advance across restart.

Red evidence and fixes: HTTP retry was initially about 1.3 seconds instead of at least six (system nuncio-system-4tyjRu); simulated wake initially timed out (google-e2e-MtUba9); new OAuth System test initially missed its account-scoped fault because the mock attributed only authorized Bearer requests (system nuncio-system-spL2L6). New raw HTTP attribution test failed 200 versus injected 503 before the private issued-credential ledger. The three scheduler System cases now pass, including stronger revoked-request counts. CLI --wait receipt was initially null (nuncio-google-e2e-jchavA), now passes through actual subprocess inspection/cancellation. Sync recovery unit initially retained cancelled instead of interrupted in schedule metadata, now fixed and verified.

Next concrete action: Task 09 durable drafts and operation journal. Begin with actual CLI draft save/restart/attachment/version-conflict E2E, then engine domain and SQLCipher storage plus authenticated RPC/CLI. Read Task 09 details already reviewed below. Use existing mail-parser for reply headers, and established mail-builder (official docs currently 0.5.0) for frozen MIME when send is introduced; email_address (0.2.9 docs, no required runtime dependencies) supplies strict addr-spec validation with display text disabled. Do not add dependencies until their first consumer. Plan attachment uploads as bounded encrypted staging chunks, atomically publishing the blob and draft version only after EOF/digest/expected-version checks. Freeze enqueued send bytes separately from mutable draft versions. Implement account/request uniqueness and audited reconciliation/uncertainty, not blind resend.

Task 06 still owes draft/operation survival after Task 09. Task 08 still owes operation-transition revision assertions once operations exist in Task 09; its reader milestone is complete. Task 03 CI egress enforcement remains owed with 12/15. Task 14 keeps memory, stalled-stream shutdown, and worker-lifecycle stress. Tasks 09–16 remain; do not stop at the reader milestone. User steering: full mocks for Google and Synology for now. Live acceptance is deferred, not verified; do not ask for credentials or access live accounts. Historical narrative below records earlier checkpoints and is superseded by this current checkpoint.

Task 01 is verified. The workspace has four production crates, SQLCipher 0.40.2 driver with bundled crypto, a bounded dedicated storage thread, profile/keyring bootstrap, distinct profile/API/database key scopes, authenticated System gRPC, and real CLI status/shutdown with proper JSON/exit codes. Normal and test-harness fmt/Clippy/tests passed; logs are in `rebuild/test-results/task01/`. A regression test caught and fixed future-schema opening changing journal mode before rejection. Actual daemon/CLI subprocess restart and wrong-auth behavior pass with the feature-gated synthetic keystore; no OS-keyring or live-provider access occurred in tests.

Task 02 is verified: the unshipped test-support crate implements independent stateful OAuth, Gmail, and Calendar HTTP services, raw HTTP conformance, mutable remote fixtures, scoped reusable pages, separate sync cursors, request/effect snapshots, before/after acceptance faults, and a standalone mock-google process with readiness file and stdin-only controls. Seventeen conformance tests pass. Normal and test-harness fmt/Clippy/workspace tests pass; logs are in rebuild/test-results/task02/. Tests caught and fixed actual truncated-body delivery, encoded identifiers, scope restrictions, nested field validation, generated recurrence-instance writes, and fabricated numeric history IDs. Two test fixture mistakes (MIME substring length and an invalid base32hex event ID) were corrected without changing validation rules.

Task 03 runtime harness is verified: private readiness file plus authenticated health, real subprocess force-kill/restart, captured/redacted failure artifacts, separate system RPC harness, synthetic-only test configuration, clock/limits/barriers, fail-closed verify.py runner, and separate production feature-isolation builds. Normal and feature workspace checks pass: 38 tests each, zero failures/ignored. Logs: test-results/task03 and test-results/google_e2e. A combined run caught inherited NUNCIO_TEST_ARTIFACTS in legacy children; those tests now clear their environments, preserving production rejection. CI network-level egress denial remains an explicit Task 03 obligation to finish with the Task 12/15 container/CI environment. --all currently exits 2 on missing imap_contract; this is not an overall passing gate.

Task 04 is verified offline. The independent mock gained userinfo stable subjects, canonical email scope spelling, strict optional synthetic client secret, and real refresh-token rotation before engine consumption. Engine accounts/OAuth/HTTP, schema 2 credential cleanup intents, authenticated Accounts RPC, and CLI connect-google/auth-status/list/disconnect/check now work. Reconnect checks Google's stable sub; refresh is serialized per account and rotated credentials survive actual daemon death. Callback HTTP is explicitly owned with connection/read/session bounds. A new incomplete-browser-header test caught unbounded graceful shutdown; replacing detached serving with owned Hyper connections fixed it without increasing the five-second assertion. Other red evidence: missing CLI account command, canonical scope wrongly rejected. Credential-write/deletion failures and lost rotated-refresh acknowledgement are exercised. Check RUNNING.md for precise recovery semantics.

Final Task 04 gates all exit 0: fmt, normal/feature Clippy, mock contract (18), separate system (6), actual daemon/CLI E2E (2), production isolation (1), and full normal/feature workspace tests (47 each, zero failed/ignored, overlapping). Commands/statuses are in test-results/task04 and the individual suite directories. Production artifacts were rebuilt in target/production/release; test features use target/test-harness/debug. Full --all still fails on the pending IMAP suites. No remote CI or live provider checks ran.

Task 05 is verified offline. Engine domain/mail.rs uses mail-parser 0.11.9, independently from the mock's mailparse. Schema 3 and store/{mail_schema.sql,mail,mail_staging,mail_promotion,mail_queries,blobs,sync}.rs implement account-scoped immutable chunked payloads, invisible generations, atomic promotion/cursor/revision, stable local IDs, memberships, encrypted FTS, and revision/query-bound keyset pagination. Store::execute uses the existing bounded dedicated SQLite thread. Gmail HTTP ingestion captures history before all-message listing and catches up changes before promotion; an expired incomplete cursor causes one bounded full restart. Original MIME and attachment bytes are retained. Missing, too-large, and unparsed content are explicit. Authenticated System sync methods, Mail query/download RPCs, and CLI sync/mail commands are connected. Downloads validate length/SHA-256/EOF and publish only a complete new file. HTML remains inert; JSON terminal-control escaping preserves original content.

Task 05 final logs in test-results/task05: fmt, normal/feature Clippy, full normal/feature workspace tests all exit 0; 59 passed / 0 failed / 0 ignored each (overlapping totals). Separate copied suite logs: independent mock 19, system 8, actual E2E 3, production isolation 1. Local production binaries rebuilt at target/production/release/{nunciod,nuncio-cli}; test binaries at target/test-harness/debug. The E2E stops the actual provider listener, force-kills/restarts the daemon, then proves local mail reads/search/raw/PDF hashes and no new provider requests. Missing-field, configured-limit, and explicit-fetch system checks pass. See VERIFICATION.md for behavioral red history, exact commands, and artifact paths. R01–R16 remain incomplete overall; full --all still fails closed until IMAP suites are implemented.

Task 06 reconciliation logic and crash tests are implemented. google_system now has 9 passing tests, including cap-1 and cap-10 initial scans with changes during a withheld list response, remote deletion before get, and captured history expiry. google_e2e has 4 passing tests including three actual daemon kill boundaries: gmail-before-page-commit, gmail-before-promotion, gmail-after-promotion. An initial red correctly caught absent barriers_directory in E2eHarness test configuration (failure artifacts google_e2e/runs/google-e2e-leBEny); adding the directory fixed it. Restart marks unfinished read-only runs failed, retains complete projection/cursor, and an explicit sync resumes from retained history. IDs, labels, final cursor, remote request counts and zero sends are asserted independently. All crash cases now also pass at cap 10 (six actual kill/restart scenarios); logs copied to test-results/task06. Latest feature workspace Clippy exits 0 after the first crash cases. Draft/operation preservation cannot be verified until Task 09 creates those records; its Task 06 checkbox remains open and must be revisited then.

Task 07 is in progress. Read the specification Calendar representation, Task 07 contract, mock calendar implementation/fixtures, and current official events/list/resource/calendarList documentation. Added named fixture calendar/all-day-2.json (provider ID alldate02), independent calendar-removal control with stdin operation, and wire conformance for both calendar removal/account isolation and exact two-day overlap across Chicago's November DST change. Mock contract now passes 20 tests. A new actual Calendar E2E correctly fails at the missing calendar refresh command (exit 2 vs 0; runner 1/inner 101); the four prior E2Es still pass. Artifacts: google_e2e/runs/google-e2e-BiBqVG. This scenario expects canonical sync plus expanded agenda refresh, two discovered calendars, offline daemon restart/get/agenda, preserved raw fields, exclusive all-day end, and explicit out-of-window coverage.

Added engine domain/calendar.rs and tests/calendar_domain.rs: distinct Date and DateTime with TZID/effective zone, validated date windows, offset-preserving event parsing, sparse cancellation/original-start identity, raw unknown fields, and per-calendar midnight bounds across DST. Chrono/chrono-tz were already workspace dependencies and are now direct engine dependencies; lock updated offline. Two domain tests passed after the expected missing-module compile failure. Targeted engine feature Clippy exits 0.

Schema 4 is implemented in store/calendar_schema.sql and migrations.rs. Tables: calendars (stable local/provider IDs, optional timezone, raw catalog fields, retired state, canonical revision); calendar_event_ids (stable identity shared by canonical and expanded forms); calendar_objects; calendar_occurrences; agenda_coverage; staged_calendar_catalog; staged_calendar_events. Object JSON retains typed CalendarObject plus original provider JSON; range/status/etag columns support scoped queries. Composite foreign keys enforce account/calendar ownership. calendar_schema first failed on absent calendars table, then passed. `cargo test --locked --manifest-path rebuild/Cargo.toml -p nuncio-engine --test calendar_schema --test mail_projection --test store_lifecycle`: exit 0, 8 passed/0 failed/0 ignored. Fmt exits 0. System foundation schema assertion is now 4. Calendar Store facade and HTTP application work now follow below; no RPC/CLI yet.

Calendar Store facade now exists in store/calendar.rs: staged catalog publication/retirement, stable event ID mapping, staged canonical/full/delta promotion with final cursor, separate expanded-window promotion and canonical-revision precondition, raw provider preservation, and explicit current/stale/unavailable/out-of-window/retired coverage. calendar_projection initially failed on missing methods, then passes; targeted calendar_projection/mail_projection/sync_runs command exits 0 (3 tests), and targeted engine feature Clippy exits 0. Common run methods were renamed consistently to begin_sync_run, sync_run, sync_run_progress, finish_sync_run_error, recover_sync_runs. start_mail_run remains a Gmail wrapper; start_calendar_run uses the common scoped queue. Startup now invalidates interrupted read-only runs and discards staging for both providers; Gmail tests still pass.

Task 07 runtime now works through the engine, authenticated Calendar RPCs, and CLI calendar list/get/agenda/refresh. Store calendar_queries.rs provides bounded revision/query/account-bound pagination, calendar-local date windows, local master IDs, raw canonical get, and current/stale/unavailable/out-of-window/retired coverage. Calendar proto has typed EventTime and GetEvent coverage; recurring_event_id permits canonical master retrieval. CLI remains engine-independent. Default refresh is rolling; explicit windows are 1–366 days with exclusive upper dates. Calendar HTTP shares the Google credential coordination and global read semaphore, and parses bounded JSON on a blocking worker.

New missing-timezone system test failed with invalid_provider_response before the fix. CalendarCheckpoint now promotes authoritative response timezone atomically with canonical data/cursor/revision. Omitted catalog timezone retains the last known value; a changed catalog zone clears the scoped canonical cursor and marks occurrences stale. A differing event-list zone during delta causes a full reset before promotion; inconsistent zones across pages fail. An early reset fault test used an encoded @ path while reqwest preserves @ in that URL segment; corrected the matcher and asserted the route was observed before injecting. No assertion or provider behavior was weakened.

Latest targeted results: feature workspace Clippy exit 0; calendar_domain/schema/projection command exit 0 (4 passed); google_system calendar_cases exit 0 (3 passed, 9 filtered). The three system scenarios cover all Calendar RPC authentication, two accounts/duplicate event IDs, cap-1 overlapping pages, canonical recurring master retrieval with attendees/reminders, moved/cancelled DST exceptions, metadata omissions, query-token binding, failed expired-cursor reset, discovery failure/removal, canonical-versus-agenda staleness, and remote tombstones. The earlier full actual E2E suite passed 5 tests after CLI wiring; it has since gained default rolling refresh, canonical master get after changing the agenda window, and offline request-count assertions, so rerun it before a fresh passing claim.

Task 07 is verified offline. `python3 rebuild/test-results/task07/run-gate.py` exits 0; all eleven commands exit 0: fresh test-harness/production builds, fmt, normal/feature Clippy, workspace tests 70 each, separate mock 20/system 12/actual E2E 5/release isolation 1. No failed/ignored tests; normal/feature counts overlap. Exact logs/results in test-results/task07. The latest E2E master/default-window/offline-count additions pass. Task 07 detailed checkboxes are checked. No full R01–R16 completion claim.

Task 08 is in progress. Schema 5 adds change_log.resource_id and prunes old account-only history to the 10,000-record bound. store/changes.rs centralizes transactional revision recording for accounts/mail/calendar; sync.rs and Calendar run completion now record queue/start/progress/recovery/failure/success transitions with the run ID. Store::changes_after validates contiguous committed revisions and returns ChangeHistoryExpired for expired/future cursors. changes.rs ChangeReader uses a 100-event buffer and watch wake-up hints; SQL remains authoritative. Worker emits revision hints after committed work. Engine/System WatchChanges is authenticated, closes on daemon shutdown, and returns OutOfRange for resnapshot-required. CLI system watch --after REV [--limit N] emits versioned JSONL and verifies continuity; future revision exits 5 with resnapshot_required.

TDD evidence: missing changes methods and missing WatchChanges RPC compile red; new CLI watch actual E2E red at exit 2 (artifacts google-e2e-ZbqjEM), then passed. New sync_runs revision assertion failed 0 versus 1 before transactional progress recording. Targeted engine changes/worker_lifecycle/sync_runs/calendar_projection/mail_projection command now exits 0 (5 tests). Targeted committed_change_stream System test passes (auth, future cursor, replay, active-stream shutdown). Feature Clippy passed before the last transition-recording changes; rerun it after current work.

The first change-retention test mistakenly repeated an idempotent disconnected state, so no revisions accumulated; it now alternates needs_auth/disconnected and proves the exact 10,000-record limit. That intentional assertion failure reproduced the previously observed SQLCipher teardown SIGSEGV. Store now owns the std worker JoinHandle via final Arc owner, closes request senders before joining, and constructs ownership before awaiting readiness to handle cancellation. The separate subprocess assertion-unwind test passes; it also passed before the fix, so it does not by itself prove a deterministic SIGSEGV cause. A subsequent deliberate failing sync_runs assertion exited 101 without a signal. Keep adversarial lifecycle stress in Task 14; do not overclaim the root cause eliminated.

Current full E2E passes 6 tests after adding watch. Progress revisions required updating the old crash assertion: it still compares every mail projection/coverage field, and additionally replays every intervening revision to prove it is a sync_run change for the expected run. The separate system suite similarly had an obsolete global-revision equality (25 vs 21), while mail data/cursor stayed equal. That assertion is now stronger: complete response equality except independently verified run-progress revisions. Its rerun is pending at this checkpoint. Historical failure artifacts: google-e2e-XTNDNH and system nuncio-system-fNEfFc. Exact logs in test-results/google_e2e and google_system; Task 07 immutable logs are preserved.

The revised full separate System suite now exits 0 (13 passed); actual E2E passed 6 after change-watch/crash revision checks. Added coordination.rs: shared per-account mutex acquired before the global semaphore (2). Equivalent active Mail requests coalesce; different fetch/full requests return Busy; Calendar coalesces matching windows and rejects different ones. Queued work stays cancellable before any provider request. The new actual coalescing/account-isolation test first failed at duplicate sync exit 2, then passed; E2E reached 7 passing tests. Feature workspace Clippy exits 0 after coordination.

Automatic polling is now the active red test: daemon_polling_refreshes_mail_and_calendar_without_manual_sync uses E2eHarness::start_with_polling(Seed::TwoAccounts, Some(100)), connects Google, and expects initial mail/Calendar data and later independent remote edits to appear without any sync command. It fails after its 8-second convergence deadline; 7 other E2Es pass. Runner exit 1 / inner 101; artifacts google-e2e-r5pGEa. Prior missing harness constructor compile red and coalescing red google-e2e-cnbeRK are retained. TestConfig now has background_sync (default true); focused E2eHarness::start and SystemHarness explicitly set false, while the scheduler E2E enables it. This is a test-only isolation setting; production will always schedule. Engine does not consume this setting yet because scheduler.rs does not exist.

Added clock.rs and GoogleHttp.clock shared Arc. Production reads UTC; an injected test epoch advances with monotonic elapsed time, allowing persisted retry deadlines to elapse instead of using the previous frozen timestamp. Mail/Calendar now use that clock; interrupted read recovery also uses it. OAuth access-token expiry already uses Instant separately. Feature workspace Clippy exits 0 after this clock change. The latest automatic E2E red predates that clock change; no full passing gate is claimed for current Task 08 work.

Next concrete action: implement persistent scheduler state and worker. Recommended implementation refinement: schema 6 adds sync_schedules per account/scope plus a singleton poll interval; account connection and start_run initialize rows, successful promotion/Calendar finish records last-success/next-attempt in the same transaction, failure records bounded exponential+jitter timing, and HTTP Retry-After persists a minimum retry deadline. Use shared Clock for wall timestamps and Instant for running deadlines; test epoch must advance. Honor provider guidance across restart, and check/wait outside the account/global permit so backed-off work does not occupy other accounts' capacity. Scheduler owns its stop/join lifecycle, separately considers mail/calendar, skips non-connected accounts, and coalesces via existing service start methods. Configure explicit short polling in separate System and real E2E tests; do not pause Tokio around SQLite. Detailed status must expose phase/run/progress, last success/age, next attempt/error class/coverage. Add provider Retry-After/date parsing tests, auth pause/reconnect, cancellation, and wake/catch-up. The approved Task 08 specification remains authoritative; no scope changes or new approval needed.

Schema 6 is now implemented in store/schedules.sql and migrations.rs. store/schedules.rs provides per-account/scope persisted deadlines, last-success/run/error/failure counters, global poll configuration, and a bounded exponential+jitter failure policy. Account connection/addition ensures scope rows; sync start and error completion update schedule metadata in their transaction; Gmail promotion and Calendar completion record successful timing. Targeted schedules/changes/sync_runs tests pass (3 tests, exit 0), after missing-method compile red. The schedule test proves a Gmail retry deadline survives store restart, leaves Calendar independent, and successful promotion resets failure state and uses the configured interval. Calendar completion was connected just after that pass. Scheduler loop, HTTP Retry-After parsing/recording, dispatch waiting, wake simulation, and expanded System status remain unwritten. Current database version is 6; test-harness binaries include change feed/coordination, while local production binaries still represent Task 07 schema 4 and require a later fresh build. Task 06 still owes draft/operation preservation after Task 09. Task 14 retains parser peak-memory, stalled Mail stream graceful shutdown, and storage worker lifecycle stress. No live compatibility or remote CI is claimed.

Latest user steering: full Google + Synology engine and CLI including writes; native apps later. **Full mock services for both providers for now.** Live-account setup and acceptance are deferred; do not request credentials or access live accounts during this work. Implement complete local Google HTTP/OAuth/Calendar and Synology-compatible IMAP/SMTP services, with independent protocol validation. The original live-provider completion clause remains deferred, not verified; keep its status explicit when reporting overall acceptance.
