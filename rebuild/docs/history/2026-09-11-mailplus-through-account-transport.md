# Rebuild session state

Updated September11,2026. Active unbudgeted goal: implement all16tasks/R01–R16. Execute inline; no delegation. Latest user steering: **full offline mock services for Google and Synology for now**. Live acceptance deferred/unverified and not a current blocker. No native apps, commits, pushes, merges, installs, publication, remote setting changes or live-account access. Preserve original source/data/unrelated work. Continue beyond milestones.

## Workspace and authority

- Implementation worktree `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`; branch feature/nuncio-google-first-rebuild, baseline a268fd5a004977b65fd0347ceeb1c69755529aa3. All rebuildsource/docs untracked. Originalrepo remainsdev; never edit originalappsource. Cargo cwd rebuild/.
- Approved docs ../../docs/superpowers/{specs,plans}/2026-09-10-google-first-rebuild.md, audit ../../docs/reviews/2026-09-10-nuncio-audit-and-rebuild.md, GOAL/PLAN-nuncio-rebuild. Repository AGENTS→CLAUDE + rebuild/AGENTS. Read personalrules/style and thisstate afterreset. User authorization overrides ticket/replan/commit/delegation defaults.
- Detailed chronological evidence in history/2026-09-11-through-calendar-writes.md (and earlier histories). TODO and VERIFICATION current. Skills alreadyused execution/worktree/TDD/debug/verification, architecture/docs, secondbrain-save. Do notspawn staleauditagents.
- Secondbrain `/Users/james.maes/Git.Local/KofTwentyTwo/second-brain` project/daily2026-09-11 last updated through Task11 and initial MailPlus services; current Task12 controller work is not saved there yet. Repo state authoritative. No secrets/commits.

## Current milestone and evidence

**Task11 Calendar writes/freebusy fully verified offline; Task12 Synology account integration active.** TODO01/02/04/05/06/07/08/10/11 checked. Task03 CI egress denial remains12/15; Task09 journalfoundation implemented, typedSynology/fullstatecoverage remaining.12–16 incomplete. NoR01–R16 fullycomplete; do not complete/blockgoal while offlineworkremains.

`python3 test-results/task11/run-gate.py`: **all12commands exit0**. Freshfeature+productionbuilds,fmt,normal/featureworkspaceClippy; normal142/feature142tests (overlapping); separate independentmock25,GoogleSystem21,actualGoogleE2E26,operationSystem6,releaseIsolation1. All0failed/0ignored. Exactcommands/status/logs test-results/task11/results.json and siblings. No gatecurrentlyrunning. Production binaries now schema12 and includeCalendarwrites/freebusy: target/production/release/{nunciod,nuncio-cli}; testfeature binaries target/test-harness/debug/{nunciod,nuncio-cli}. Localdevelopmentartifacts, no packaging/signing/install/live/remoteCI claim.

Independent lowerlayerreds/results are recorded VERIFICATION/history. Last E2E includes18Calendarcrashcombinations, series/singleDSTmoves, actual412exit5+inspection, zeroeffectpermissionguards, freebusyfiles/partialcoverage. Task11detailedplancheckboxes checked withevidencelink; fullgoalnotcomplete.

## Implementation map

Fourproductioncrates + separateunshippedtestsupport. CLI hasnoengine/storage dependency. Authenticated numeric-loopbackgRPCv2, OSkeystore productionsecretstore, feature-onlysyntheticsecrets/endpoints, exclusiveprofile, SQLCipherWALFULL+encryptedchunkblobs, bounded dedicatedstoreworker. Per-accountsequence beforeglobal2, ownedpolling60s+coalescing/cancellation/stop, persistedperAPIRetry-After numeric/date, pause/reconnect/wake, boundedrevisionchangefeed. Account/sourceprojectionresets preserve localdrafts/operations/attempts.

Gmail full/delta stagedprojection/cursor; MIMEparser/chunkdownloads/querycoverage. Schema7–9 draftoptimisticCRUD+64MiB/256attachments/4uploads/256KiBRPCchunks; offlinereply/replyall/forward, MIMEfreezing withmail-builder0.5.0, sender/selfexclusion/References/charset/CID/exactbodypreservation. Schema10operationjournal accounts+requestcanonicalidempotency, immutablefrozenpayload, queuedcancel, interruptedrunning→uncertain/reconcile. SMTPacceptance250 and Sentcopyjournal alreadyseparate butnotransportyet.

GoogleSendpositiveMessage-ID+exactfrozenMIMEreconciliation; missingreadneverfailureproof, noimplicitHTTPautoretry. Scopeauthentication/retrybackoff, bounded8normalrejects, lost/malformed/5xxuncertain. ManualconfirmApplied GoogleSendonly withproviderID+frozenRFCMessageID+humanobservation; abandonanyconflict/uncertain releasesFIFO; resendnewUUIDrequiresduplicate-riskCLIflag and changesouterMessage-ID/Dateonly. Historypaginationaccount/revisionbound; waitingerrorsincludeerror.operation. operationsworkerownedjoin/stop/error health; featurecrashcheckpoints.

Schema11 GmailtypedRead/Star/Archive/Trash/Labeldesiredstate; immutableprovideridentity+FIFO; AppliedMailfullobservedmembershipsatomicreceipt+projection (no sync cursoradvance); positiveGETreconcile before repeat. Allactions versionedstrictfiles andtypedAPI/CLI; independentremoteeffects testedafterdeath/reset/concurrency.

## Calendar implementation

Canonicalevents and expandedboundedagenda separate; timezone/all-day/recurrence/providerunknownJSONpreserved. Exactstablemaster/occurrenceIDs; current/stale/outofwindow/unavailable/retiredcoverage. domain/calendar_change.rs + fields.rs: explicit scopeSingle/Series+NotificationsNone/All/ExternalOnly, strictnamedboundedfields/ETag/time/permissions. Rejectfollowing, readonly/locked/nondefaulttypes, organizertransfer; guestlist/flagsorganizeronly, partialRSVPselectedaccountemail, unrelatedattendeespreserved. Google permits attendee-copycontentedits; do not invent blanketorganizer-onlysummary/time restriction.

Schema12 calendar_change_payloads + store/calendar_changes/{receipt}.rs: stableUUIDhexcreateproviderID, durablelocal+provideridentity/base/patch/timezone/expectedETag/sequence; account+requestlookup precedesprojectionlookup. DesiredpublicJSONomitsfullbase; internalpayload≤4MiB. FIFO sameproviderobject. Typed AppliedCalendar Event/Deletedreceipt checks identity/newETag/intentfields; atomicallyupdatescanonical+operationreceipt+agendaStale withoutcursoradvance. Timeequalitynormalizeszones withnanosecondprecision, matchingattendeesbyemail. DELETEempty204ack retainslastknownsnapshotminusETag+cancelled (noinventednewETag); lostackreconcilegetsfullremoteobject.

Typed RetryEvidenceUnchanged(oldETagfullobject)/Missing(stablecreateID) requiredforrepeat; genericRepeatable/bareApplied bypassesrejected. ReconcileLaterhonorsfuturedeadline; ObservedCalendarConflict storesnewcurrentevent+conflictreceiptETag whiledesiredintentremains. Positiveeventread withnotifications!=none leavesuncertain/calendar_notifications_unconfirmed, noautoreconcile/repeatedinvitation; acknowledgementsconfirmrequestedpolicyacceptance, neverrecipientdelivery. Abandon supports Calendar; provider-specificmanualconfirmation remainsTask09/14auditfollowup.

providers/google/calendar_write.rs fixedroot encodedPOST/PATCH/DELETE withsendUpdates/IfMatch, boundedresponse; 401/403/429/412/5xxclassifications. Accounts sharedprivateGoogleWritewrapper ownscredentialgate/cache/refresh/perAPIbackoff. operations/calendar_change.rs dispatch/readproof/reconcile/observed412, boundedattempts, Calendarcoordinatorlane. TypedChangeEvent proto+daemon/calendar/change.rs converts publicfields (notrawproviderstructs); strictversion1CLIfile in calendar/action.rs, optionalnullrejected, explicitclear_fields. RUNNINGexamples.

Freebusy: domain/free_busy.rs types+validation+coverageparser, CalendarSync::free_busy/Engine::query_free_busy; typed QueryFreeBusyproto, daemonstopaware30sRPC, CLIcalendar/free_busy.rs. Requiredversion1/128KiBfile/from/toRFC3339/zone/1..50distinctprovidercalendarIDs (notlocalUUIDs);≤366days, groupsunsupported. GoogleReadGet/FreeBusy reusesfixedHTTProot/auth/readbackoff, coordinator. Bounded4MiBHTTP/20000busyperiods; missing/malformedresultiserror, unknownfutureprovidererrorspreserved; explicitaggregate/per-calendarcomplete+querytimestamp+requestedbounds. PartialresultCLI0 completefalse mustbechecked; noreservation/cacheguarantee. Responsebusyintervals intersectwindow, nofreeassumption onerrors.

Independent mock implementedbeforeproduction: statefulOAuth/Gmail/Calendar+outofbandcontrols/faults/counters. NativepartialRSVP+strictincoming vsretainedunknownattendeefields; rolesowner/writer/reader. New freebusycontract checksopaque/transparent/empty/missingresults/limits+zeromutations. Recurringcontractcheckswholeseriesparentedit beforemoved/cancelledinstance; post-existing-exceptioninheritanceremainscompatibilitygap, donotclaimtested. No realproviderrequests.

## Task12 current work and next action

The independent MailPlus service composition, state controls, standalone process, and Rust harness now exist. **Production Synology account configuration/authentication now implemented; mailbox sync and writes remain next.** Native apps/live access remain outside current execution.

- Pinned official images: Dovecot 2.4.5 `sha256:c807be4fb5a97d9c3a90770569d3a6c4cbdcb36742ad41f90409cbd929166553`; Mailpit 1.31.1 `sha256:98b916bd3c8d61f7633a52d3ea2f58d00620cb01ca57ab59edde68c347a95365`. Already downloaded successfully. Docker29.7.2/Compose5.5.1/OpenSSL3.6.4/Python3.14.7. Docker/loopback commands need authorized narrow escalation; no automatic review rejection.
- `tests/imap/compose.yaml`, `dovecot.conf`, `services.py`: unique disposable composition, rootless/read-only Dovecot with only required SYS_CHROOT; two Mailpit instances for required STARTTLS and implicit TLS, separate capture databases/APIs. Numeric-loopback ephemeral ports, local per-run CA, random synthetic alpha/beta and observer passwords, private runtime/credential files. No relay/RDNS, strict MIME headers, no capture auto-deletion. `services.py start/stop` supports direct servers; reset recreates containers/data while retaining CA/passwords/project.
- Initial internal Docker bridge failed to publish ports despite HostConfig bindings. Dedicated ordinary bridge fixes host reachability; it is **not an egress firewall**. Task03/15 still requires host/container outbound denial. Dovecot shipped file-cap executables needed SYS_CHROOT; all other restrictions retained. SMTP correctly prepends Return-Path/Received; tests require a strict trace prefix plus exact submitted MIME suffix. IMAP requires entire raw byte equality.
- `proxy.py`: protocol framing and before/after acceptance faults in front of real server state. IMAPS/IMAP STARTTLS/SMTP STARTTLS/SMTPS, SASL, literals, IDLE/DONE, separate request and positive-ack counters. Disconnect/withhold/pre-dispatch rejection/truncated FETCH; max64 connections/100 faults/70MiB literals/64KiB framing/60s session. After-accept faults now remain queued on a server rejection (new COPY regression caught/fixed this). Remote Dovecot/Mailpit observations are authoritative.
- `mailbox.py` independent modified UTF-7 quoting and UID/raw/flags/size observations. `control.py` drives real IMAP/doveadm/API operations: deterministic two-account seed/reset, raw/hashed mailbox inspection, external flags/append/UIDVALIDITY, folder create/delete/rename, UID copy/scoped expunge, per-transport SMTP capture/raw/details, fault inject/wait/release and counters. Reset pauses proxy admission, drops active clients, recreates backends and restores deterministic UIDs/bytes. Folder and deletion tests preserve an unrelated Deleted UID. Fixture PDF is currently a small synthetic MIME attachment, not yet a validated renderable PDF.
- `mock-mailplus.py`: real standalone process with strict bounded JSON lines over private inherited pipes; atomic `mock-ready.json`, no control TCP listener, duplicate/unknown field rejection, fixed error categories without echoing AUTH/payload exceptions. EOF/SIGTERM/explicit shutdown clean its own composition. Signal handling now lets a blocking Docker reset finish before cleanup, avoiding a cancelled thread launching containers after stop. SIGKILL requires explicit recorded-project cleanup. Additional SIGTERM-during-reset regression is still needed.
- Rust `crates/nuncio-test-support/src/imap.rs`: `MockMailPlus` child lifecycle/readiness, bounded control RPCs, private fixture credentials, retained command/status evidence, explicit shutdown and failure cleanup. No production engine imports. `tests/imap_contract.rs` tests that interface and runs independent Python protocol contracts. `imap_system.rs` and `imap_e2e.rs` remain absent until adapter/daemon integration; verify --all must continue failing closed.

Latest completed checks:
- `python3 tests/imap/run-tests.py`: all4 commands exit0; server2 + proxy8 + control1 + actual standalone1 = **12 Python contracts**, no failures. Logs/results at `test-results/imap-services/`; retained service runs below it. Includes lost implicit-TLS SMTP acknowledgement, rejected COPY preserving after-accept fault, strict JSON/reset/EOF cleanup.
- `cargo test --locked -p nuncio-test-support --test imap_contract`: **2 passed,0 failed,0 ignored**, exit0,28.18s. One Rust test invokes the Python contracts; counts overlap. Test artifacts in `test-results/imap-contract/runs/`. Missing imap module was observed red101 before implementation.
- `cargo clippy --locked -p nuncio-test-support --all-targets -- -D warnings`: exit0; `cargo fmt --all`: exit0.
- Prior missing-control and missing-standalone tests each exit1 before implementation; extended folder controls exit1 before implementation then passed. Old failed SASL session19560 completed exit1; no longer pending.

**Latest follow-up:** strict absent-extension contract now passes. Unsupported ENABLE requests reached Dovecot and enabled hidden features (red1); profile guards/unknown-name forwarding fix that. Dovecot omits empty ENABLED for all-unknown names, so the profile emits RFC5161's required empty response only after backend acceptance. Python3.14 uid-fetch convenience wrapper rejected modifier arguments; the strict test now uses its raw command entry point. Modifiers and hidden HIGHESTMODSEQ responses are checked.

`test-results/task12-mocks/results.json`: **all9 commands exit0** (Python lint/format, Rust format, both workspace Clippy modes, required IMAP contract and Google mock/system/E2E). Rust IMAP2 includes Python15 contracts; Google25/21/26; no failures/ignored. This gate predates the latest server-Sent additions.

New mock features after that gate: optional `AutoSent` worker (`tests/imap/auto_sent.py`) observes actual Mailpit captures and authenticated-user tags, APPENDs exact captured MIME to the owner's Dovecot Sent, exposes status/wait controls, stops visibly after unknown/failing APPEND, and restarts only through explicit seed/reset. Duplicate Message-IDs remain separate captures/copies. Tests withhold SMTP acknowledgement while observing a real Sent copy, check alpha/beta and STARTTLS/TLS isolation, and pause real Dovecot after SMTP acceptance. Reset initially left worker failed (red1); restart fix passes. The standalone `--server-auto-sent` path is exercised over actual control pipes and SMTP. Rust readiness now includes server_auto_sent and `start_with_sent_policy`.

Mailpit prepends a Bcc header for envelope-only recipients; the expanded raw-capture test exposed this (red1). The test now requires that exact Bcc prefix plus strict transport headers and unchanged submitted MIME suffix, and checks Bcc/ReturnPath/authenticated tag from its independent API. Proxy snapshots additionally record successful SMTP envelope recipients and unstuffed wire SHA-256/size, bounded to1000 deliveries/100 recipients; the regression intentionally differs envelope recipients from MIME To. Unknown protocol verbs map to UNKNOWN to avoid echoing secret-like input; new canary regression red1 then fixed. Fault protocol/command mismatches now reject.

Mailpit automatic GitHub version checking was previously at its default. It is now explicitly disabled in composition; **prior gates do not establish absence of background update requests**. No tests used real Google/Synology accounts. Explicit host/container egress denial still remains Task03/15.

Local Docker guard pins all mutations/cleanup to a validated Unix socket, including saved cleanup identity. Seed SIGTERM regression passes (finish blocking reset before stopping). Python runner now isolates each test artifact tree, records timeout124 and cleanup statuses, terminates its owned process group including descendants ignoring TERM (red1 then fixed), and cleans only that tree's recorded projects. Startup diagnostics cannot skip cleanup on failure. Ruff0.16.7 downloaded for test-source checks; use `UV_TOOL_DIR=$PWD/target/tool-cache/tools` and `--cache-dir target/tool-cache` so tool metadata stays in the workspace. Initial run with only cache-dir hit sandbox permission2 at default uv tools; scoped-directory commands then passed. No normal tool installation, remote settings change, or approval rejection.

`python3 tests/imap/run-tests.py --output test-results/task12-mocks-followup/python`: all6 commands exit0; runner1/server3/proxy9/control1/autoSent1/standalone2 = **17 Python contracts**, with recorded per-project cleanup. Latest source is formatted and Ruff check passes. **Full Rust follow-up completed:** `python3 test-results/task12-mocks-followup/run-gate.py`, all6 commands exit0: both workspace Clippy modes, Python lint/format, Rust format, and required imap_contract (2 Rust tests, including all17 independent Python contracts; overlapping counts). No gate currently running. **Immediate next action:** begin production Synology adapter/account/storage/API/CLI integration, maintaining mock-first tests for each new behavior. `imap_system` and actual daemon/CLI `imap_e2e` still missing.

Continue production Synology implementation: TLS/secure account config and capabilities; explicit `(account, mailbox identity, UIDVALIDITY, UID)` placement storage; full/delta folder ingestion and quiet UID guard; flags/archive/trash/restore/move/copy/safe UID expunge; SMTP acceptance versus Sent APPEND substeps and positive-proof recovery; engine/API/CLI and separate system/actual subprocess suites. Server-auto-Sent simulation now exists; both adapter Sent policies still need system and subprocess coverage. Keep Google suites green.

After Task12: backup/restore/export/repair13; adversarial resource/security14; production packaging/contract/CI egress15; manual worksheet/R matrix16. Live checks are deferred by the user, not a current blocker. Known later follow-ups: storage worker unwind SIGSEGV root cause unproven; post-submission store failures; bounded reconciliation CPU/HTTP2 retry ownership; SMTP encoding/SMTPUTF8/line canonicalization; attachment removal/download aggregate bounds; descriptor golden/external client smoke. Passing mocks does not prove live compatibility or remote CI execution.

Implementation preparation (read-only research so far): approved spec explicitly requires a separate message record per IMAP placement; compound provider keys must use unambiguous encoding. Stable local mailbox identity + UIDVALIDITY + UID; no Message-ID dedup. Root legacy code uses async-imap/lettre/tokio-rustls; use their current official APIs but do not copy legacy string-matched SMTP errors or its incorrect “SMTP has no auth probe” claim. Official docs checked async-imap0.11.3 and lettre0.11.23; dependencies not added to rebuild yet. Rebuild lock already has rustls/tokio-rustls/ring/webpki roots, no aws-lc. Reuse explicit validated TLS and existing mail-parser/frozen MIME. Need bounded network reads, 30s deadlines, explicit STARTTLS/implicit TLS, and no credential-bearing Debug/errors. Accounts/store currently connect Google only; schedules assume gmail/calendar. Follow the existing Coordinator account lane/global2 and store credential-cleanup intent pattern.


## Task12 account integration checkpoint

Production account foundation now exists in domain/imap_account.rs, providers/imap/{mod,smtp,wire}.rs, accounts/imap.rs, store/imap_accounts.rs and schema13 imap_account_schema.sql. Required implicit TLS/STARTTLS, WebPKI plus explicit PEM roots, verified hostnames, bounded authentication framing/deadlines, IMAP SASL PLAIN and SMTP PLAIN/LOGIN. No plaintext/opportunistic option. IMAP exact username + canonical host/port/TLS hashes define saved principal identity; SMTP/address are separate settings. Passwords are Zeroizing and live in SecretStore, public config/capabilities in SQLCipher; prepared cleanup intent precedes secret writes and activation. Check/disconnect/reconnect reuse credential gates; global coordinator caps network account checks/connections. Generic IMAP NO/SMTP4xx must not revoke credentials; only explicit auth failure proof marks needs_auth. No mail sync or SMTP send transport yet.

Versioned authenticated Accounts API now has ConnectImap/GetImapConfig and typed config/capabilities/credentials; secret-bearing request Debug redacts and protobuf credentials zeroize on drop. CLI account connect-imap --config FILE --credentials-stdin [--account UUID] and account imap-config --account UUID. Version1 config rejects password fields; credential JSON only from bounded nonterminal stdin pipe. E2eHarness cli_with_stdin writes private pipes, never logs input. CLI has no engine dependency.

Dependencies added async-imap0.11.3,lettre0.11.23,tokio-rustls0.26/ring/tls12,webpki-roots1,rustls-pemfile2.2; log max_level_info/release_max_level_info compiles protocol wire traces out. Cargo fetch completed (old session81548 gone); cargo fetch --locked --offline exit0 confirms complete lock/cache. Uses lettre response parser with an independent bounded reader because its AsyncSmtpConnection read_line checks lengths after allocation. AuthWire validates complete <=64KiB lines and rejects literal announcements before async-imap sees them; 256KiB auth budget,30s combined probe. Full sync needs a separately bounded literal path.

Observed reds/results: config tests missing module101 then2passed0failed0ignored/0; first imap_system missingAPI compile101; format Zeroizing display compile101 corrected. First system run failed provider response101. Independent observer established AUTH+CAPABILITY reached Dovecot, but mock filter left duplicate spaces in capability ABNF. Added Python spacing assertion failed1; fixed removal of leading separator with hidden token; focused Python1pass0 + system1pass0. RFC3501 section9 primary reference https://datatracker.ietf.org/doc/html/rfc3501#section-9. Temporary static transport diagnostic prints removed. Second system test exposed temporary auth rejection incorrectly mapped Unauthenticated and credential revocation (101); exact wire auth response-code proof / SMTP4xx classification corrected. Latest cargo test --locked -p nuncio-test-support --test imap_system:2passed0failed0ignored/0. Covers bothTLSmodes, missingtrust/noAUTH, badIMAP and SMTPpasswords, failedSecretStoreput, temporaryfailurepreservescredentials, reopen/identitymismatch/disconnect, independentzeroSMTP effects. No full Task12 gate yet.

CLI configuration unit1passed0; feature workspaceClippy0 before finalauth fix. imap_e2e missingstdin-harness method compile101 then implemented. First actual subprocess build0/suite101 because test used nonexistent data JSON wrapper (existing contract is result); corrected assertions, retained failed runs/google-e2e-3h2oJr. Latest E2E rerun pending session50853. Production artifacts still Task11/schema12; feature artifacts rebuilt during E2E/schema13. Next: finish account negative/resource/reconnect tests, run focused account+Google regressions with logs, then implement IMAP placement schema, sync/read path and durable writes through API/CLI. All R01–R16 remain incomplete.


Account integration gate completed: `python3 test-results/task12-accounts/run-gate.py`, **all12 commands exit0**. Both Python checks, Rustfmt, both workspace Clippy modes; engine52,CLI/proto4, independentIMAP2 (wraps Python17), IMAPsystem2, actualIMAPE2E1, Googlesystem21, actualGoogleE2E26. All0failed/0ignored; counts overlap. Fresh feature binaries/schema13 built; production remains schema12. Exact commands/statuses and copied nested suite logs/results in test-results/task12-accounts. First actualIMAPE2E rerun build0/suite0 after correcting result wrapper. No gate currently running. Next concrete action: cover remaining bounded authentication/LOGIN edge cases, then IMAP placement and mailbox sync implementation. Live acceptance remains deferred/unverified.

Follow-up after account gate: SMTP LOGIN's first challenge still classified temporary rejection as permanent auth failure. New focused transport test first missing-helper compile101, then behavior1failed/1passed101 after extracting existing exchange. Corrected first challenge through the same typed require_auth classifier. `cargo test --locked -p nuncio-engine --lib providers::imap::smtp`:2passed0failed0ignored/exit0. Includes temporary rejection at each of3 challenges, mixed reply-code rejection, malformed line ending, and an unterminated response whose peer remains open; bounded parser returns before1s. This is a transport unit test, separate from independent Dovecot/Mailpit contracts.

IMAP identity/encoding domain now in domain/imap.rs and identity::ImapMailboxId. Explicit placement includes account, local mailbox UUID, nonzero UIDVALIDITY+UID; version1 JSON compound key is unambiguous and rejects cross-account decoding. MailboxName canonicalizes only INBOX, encodes strict RFC3501 modifiedUTF7, preserves other name case and rejects malformed/superfluous shifts, encoded printableASCII, invalid UTF16, controls/oversize. UID batching sorts/deduplicates bounded input without empty commands or u32 wrap; quiet new-UID range is None. Flag model preserves unknown keywords, canonicalizes known system flags, rejects command injection. `cargo test --locked -p nuncio-engine --test imap_identity`:missingmodule/type red101 then4passed0failed0ignored/exit0. Primary grammar https://datatracker.ietf.org/doc/html/rfc3501 sections5.1.3/9; independent Python base64 confirms synthetic astral-plane vector. These helpers are not yet integrated into mailbox sync/storage/API.

Next concrete implementation: stage IMAP mailbox discovery and checkpoints using a local immutable mailbox UUID. Match only an active exact decoded name (INBOX casefold only); absent/renamed folders retire and a new folder gets a fresh identity, never guessed from Message-ID. Use explicit placement metadata and account/UIDVALIDITY/UID constraints. Reuse encrypted blobs/MIME/staged mail promotion, but scope=imap and per-mailbox checkpoint promotion must be atomic. Existing mail start/promotion/coverage still hardcode gmail; scheduler dispatch currently gmail/calendar only. Quiet UID guard must preserve checkpoint/no empty FETCH, and UIDVALIDITY reset must publish a completed replacement while preserving drafts/operations. AuthWire is authentication-only; a bounded literal-aware read path is required before using async-imap FETCH/LIST. IMAP raw READ/flags/system/subprocess tests against independent servers precede integration.


Latest transport gate: test-results/task12-transport/run-gate.py, all6commands0; engine59tests, IMAPsystem2, actualIMAPE2E1, Rustfmt/bothClippy. Source then changed to add explicit command completion checks, so that gate predates this last fix.

Critical async-imap0.11.3 finding: parse.rs filter_sync ignores status on matching Response::Done; high-level CAPABILITY/LIST/SEARCH/FETCH collectors can accept partial data followed by NO/BAD. New unit missing-helper red101 then actual behavioral failure101 after extracting the existing capability helper. Production providers/imap/command.rs now uses run_command/read_response and requires the exact tag with OK before returning any collected data; rejects NO/BAD/wrong tag/BYE/EOF/continuation and bounds byte count, response count4096 and30s deadline. Parsed values are copied by a typed visitor because ResponseData is private and imap-proto Response/Capability are not Clone (compile101 at each incorrect assumption, corrected via public conversion). Do not use the unsafe high-level collectors in upcoming sync. Expanded six-scenario capability unit1pass0, including positive OK control. This still uses the established library's framing/tags/parser.

Full focused completion gate running session43781, test-results/task12-completions/run-gate.py (6checks). Poll and record final results. No mailbox sync/storage integration yet. Secondbrain project/daily updated through account gate and current transport foundations via /tmp/nuncio-imap-memory.py, scoped escalation0; no secrets/git operations.
