# Maintenance checkpoint history

Historical progress and failure records; current instructions are in ../SESSION-STATE.md.

## Immediate next action

Task 13 Maintenance engine/API/CLI implementation is in progress on schema 19. The previous goal turn was verified progress (restore/profile gate: engine 88, fmt/both Clippy all pass). This turn added bounded upload/download ownership and the authenticated Maintenance service and CLI. No commits, live providers or remote CI.

Current running handle: shell16918, `python3 test-results/task13-recovery-bounds/run-gate.py`, stdout `/tmp/nuncio-recovery-bounds-gate.log`. Seven-command gate: Rustfmt, normal/feature Clippy, full engine, proto/CLI/daemon, actual recovery_e2e and actual imap_e2e. Poll existing handle/results; do not rerun just because observation timed out. No source edits while checks run. If a check fails, retain gate logs/results before correction and rerun affected checks. Source schema19; production artifacts still stale schema12.

Newer than the completed Maintenance gate:
- `support/smtp_recovery_e2e.rs` passed actualIMAPE2E10,68.78s, both Sent policies. Snapshot says queued; original then submits with lost SMTP ACK; restored snapshot reconnect/restart does not repeat delivery/APPEND or invent receipts. Explicit duplicate-risk resend alone creates second delivery/new Message-ID/identical real PDF and expected independent Sent copies. Logs copied task13-smtp-recovery.
- Mail raw/attachment/body exports now share backup output validation via CLI `output_file.rs`. New actualCLI regression first failed success0 vs refusal1 after writing absent active store.db-journal. Fixed by authenticated System protected-path lookup and shared canonical-path refusal before download; actualGoogleE2E26 passed26.57s. Red and green preserved task13-export-protection. Existing atomic completion/hash checks retained.
- New store `backup_io.rs`: shared1TiB maximum; fstatvfs available-space preflight (8MiB margin; upload1x, backup2x, restore3x); exact bounded copy with one-byte growth probe, no reading oversized declared input; actual I/O errors propagate. Create/rekey export applies SQLCipher max_page_count and checks effective limit. Inspection hashes at most declared length+1. Preflight estimates do not reserve space against other processes. Additional migration-growth/failure/crash/parent-race checks remain.
- Bounds tests: missing helper compile101, then compile101 for pinned rusqlite types/signature assumptions; corrected using i64+checked conversion and Option<&str>. Focused2 passed. Integration backup2 failed Database(None); isolated probe proved SQLCipher page_size returns Text named cipher_page_size. Bundled libsqlite3-sys0.38.2/sqlcipher/sqlite3.c:112162–112177 explicitly returns that string. Parse validated text512..65536 power-of-two. Integration backup2/restore3/profile3/maintenance4/sparse1 then passed. Added real SQLCipher page-limited export test: raw inner FULL is wrapped by sqlcipher_export via sqlite3_result_error (source113304–113308), observed extended_code1 and exact message database or disk is full. Test now asserts that precise wrapper and checks source payload/length and target bound. No production error semantics/assertions weakened. Temporary probe source removed; diagnostic/failure logs retained task13-recovery-bounds. Final unit plus broad results are pending the running gate.

Completed preceding12-command Maintenance gate: all0 at test-results/task13-maintenance/results.json, engine93/protoCLI/daemon18/Google21+26/operation6/IMAP21+9/recovery1, zero failed/ignored, bothClippy. First canonical-path failure retained. RECOVERY/RUNNING have actual backup commands/protected stdin, original-daemon stop requirement, new target/key handling and precise remaining work. No general original-operation reconciliation API yet.

After the running gate, record counts/statuses and update RECOVERY/VERIFICATION/TODO. Continue Task13 historical migrations/repair/dry-run/account removal and safe original-operation reconciliation; then Tasks14–16 (resource/security/CI/packages/manual worksheet). Full goal remains active; local mocks only.
Implemented this turn:
- `engine/maintenance.rs` actually lives at `crates/nuncio-engine/src/maintenance.rs`; public BackupUpload/BackupInput are re-exported from engine. One maintenance slot; ordered nonempty chunks <=262144 bytes; declared size <=1 TiB; exact SHA256; private RAII files. Each blocking write/inspect/restore owns its input and permit after caller cancellation. Engine::create_backup transfers the permit into the Store worker closure and BackupArtifact. Engine restore targets only a validated new sibling name. Four maintenance engine tests pass, including a blocked keystore/cancelled caller proving admission stays occupied until work completes.
- `proto/nuncio/v2/maintenance.proto`, build.rs and proto redaction: CreateBackup server stream; InspectBackup/RestoreBackup client streams; RecoverySecret zeroizes and has redacted Debug, including nested headers. Two redaction tests pass. Info/data/complete framing, length/hash and limits are documented in proto.
- `nunciod/src/maintenance.rs`, lib/server registration: bearer-authenticated service, 512 KiB message cap, 30-second idle upload timeout, ordered upload validation, blocking download reads retaining artifact/permit, proper AlreadyExists/error handling. Cleanup/activation errors include safe nuncio-recovery-code and nuncio-restored-profile-id metadata. Real daemon in-process API test passed 1 after genuine red Unimplemented vs required Unauthenticated. All three RPCs reject anonymous callers; streamed ciphertext hash/reopen/original preservation tested.
- CLI `backup create/inspect/restore`, protected JSON stdin only, raw-key literal rejection, no argv secret flag, profile-name parser, input regular-file/symlink/sidecar checks, bounded hashing/pull-based upload, ordered download/hash/completion validation, new atomic output and protected-path checks. AppError gained optional recovery details; all existing literals updated. Recovery metadata is validated before exposing a profile ID; lost restore acknowledgement reports uncertain outcome. Backup RPC client timeout is 1 hour; ordinary calls remain 30 seconds. Full CLI tests pass 10 (7 unit + backup args + 2 existing output tests). New direct CLI rustix dependency and daemon dev sha2 resolved offline. Source schemas unchanged at 19.

Latest focused logs: /tmp/nuncio-maintenance-{red,first,second,cancellation,proto-red,proto-first,api-red,api-first,cli-red,cli-first}.log. Engine initial missing APIs compile101 then dead-code method compile101; removed unused accessor, maintenance3 then cancellation4 pass. Proto missing-type red then2 pass. API behavioral red then1 pass. CLI missing-command red then10 pass. No assertions weakened. Full normal/feature Clippy and cross-suite gates have not yet run for this turn.

Next after resolving actual E2E: add it to required suites; broaden API malformed-stream/cancellation/output-error cases, run appropriate normal/feature Clippy and engine/proto/CLI/daemon/Google/IMAP regressions; update evidence. Continue explicit safe reconciliation/resume, all historical migration/repair/export/account-removal and resource/security/production/CI work. Do not mark Task 13 or the goal complete for this transfer slice. Filesystem issues still to audit: absolute protected paths for relative daemon directories, future store.db-journal protection, creation disk limits/space estimates, copy bounds, source schema identity (triggers), activation-parent races and crash cleanup of stages/new keys. Static Engine::restore_profile bypasses per-engine admission by design as a library initialization entry point; RPC uses the owned upload permit.

Previous restore/profile details are in `docs/history/2026-09-11-restore-profile-checkpoint.md`. Production artifacts remain stale schema 12. The running E2E rebuild updates feature executables only. All local mock-only scope remains authorized; live acceptance deferred/unverified, not a current blocker.
