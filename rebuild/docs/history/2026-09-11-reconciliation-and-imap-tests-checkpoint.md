# Rebuild session state

Updated September 11, 2026. The active goal is unbudgeted and covers all 16 tasks and R01–R16. Work inline. The user requires full local Google and Synology mocks now; live acceptance is deferred and unverified, not a current blocker. Native apps are excluded. No agents, commits, push, merge, publication, normal-environment installation, remote-setting changes or live-account actions. Preserve original source/data and unrelated work.

## Workspace and next action

Worktree: `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. Cargo cwd is `rebuild/`. Original checkout remains on dev; rebuild files are uncommitted. Read root AGENTS→CLAUDE, shared personal rules/style, rebuild/AGENTS and these state/TODO/VERIFICATION files after reset. Approved spec, plan and September 10 audit live in worktree `docs/`, one level above Cargo cwd. User authorization overrides issue, replanning, delegation and commit defaults. Do not recreate or complete the goal.

Source schema is21. Feature daemon/CLI and normal production artifacts were rebuilt and verified by the completed schema21 gate. No normal environment install, remoteCI or live provider ran.

**Current command:** shell99889 is running `cargo test --locked -p nuncio-test-support --test imap_system --target-dir target/test-harness restored_flag_intents` with approvedlocal Docker/socketaccess. New test-only imap_write_system/recovery.rs covers8 flag scenarios; shared backup_system.rs was extracted from reconciliation_system.rs and registered in both Google/IMAP roots. Compile/test result pending. Source schema21 and production source are unchanged since the just-completed mutation gate.

**Latest complete gate:** shell89876 completed all13commands exit0: fmt/bothClippy, engine111, proto/CLI/daemon19, Google21+26, operations6, reconciliation3, actualrecovery3, strictIMAP22+11, releaseisolation2; zero failed/ignored. Exact logs/counts/artifact hashes in test-results/task13-reconciliation-mutations. Normal schema21 production binaries now include per-request retry fix. MigrationDDL unchanged; previousgate42cases remains valid. Earlier63594/32769 were firstgreens. No remoteCI/live acceptance, package or installation claim.

The new modules are support/reconciliation_mutations_system.rs and reconciliation_mutations_e2e.rs, registered in reconciliation_system/recovery_e2e. System cases cover12 restored Gmail action scenarios (read,star,archive,trash,restore,existing-label removal; never dispatched vs applied afterward with lostack) and16 Calendar scenarios(create/update/delete/respond; none/all notification policies; never dispatched vs applied afterward withlostack). Observe sends no writes; safe retries require a fresh explicit request; stale restored creates stayheld; independent state/write/notification counts remain correct; sameID replay afterrestart doesnotwrite. ActualCLI adds3cases: Gmailarchive/Calendarupdate-none/Calendarupdate-all; backupqueued, restore, Observe, ResumeSafe, stopafterremoteackbeforelocalreceipt, SIGKILL/restart, exactlyonewrite; notifications remainunknown when acknowledgementlost. New audit asserts global ordinals retained, plus Calendar abandonment retains uncertain outcome/history and doesnotrepeatnotifications.

Reproduced production defect: the eighth separate explicit observation exhausted lifetime retry count and returned conflict, preventing later safe resume. Red system2pass1fail at explicitobservation8 is retained at test-results/task13-reconciliation-mutations/retry-limit-red. ExecutionPolicy now derives retry ordinal from the active durable request's first_attempt_ordinal (checked arithmetic); original global ordinal remains in every SMTP/IMAP progress and receipt write. All automatic retry ceilings staybounded and store lifetime/request limit1000 remains. Afterfix, ten observations and new safe resume pass; actualCLIcrashcasespass. Full13-command regressions nowpassed. Firstcompile unknownuuid dependency was replaced with deterministic synthetic UUIDs withoutdependencyaddition. A later test-edit helper assertion failed before source writes and the absent gate script returned2; corrected helper created the gate. No mock validation/assertions weakened.

Static next-work context: SMTP start_smtp_data stores sent_floor for both Sent policies. find_server uses bounded unique Message-ID search plus versioned content comparison and rechecks candidates. SmtpIntent.sent_fingerprint is currently populated onlyforServer; ClientAppend needs comparison derived from immutable sent-copy blob (Bcc must match, server's relaxed omitted-Bcc comparison is inappropriate forclientcopy) if implementing positiveclient-Sent discovery. record_server_sent currentlyrequiresServer and Accepted. Restored Accepted/Appending clientcopies canholdlaterunrecordedAPPEND; absence never permitsrepeat. Need real independent Dovecot/Mailpit recoverycases before modifying this behavior. Preserve separate SMTPacceptance and Sentcopy evidence.

**Next concrete action:** collect/fix the full schema21 gate, then cover restored Google label/calendar and IMAP flag/move/copy/Trash state transitions with independent remote effects for Observe/ResumeSafe. Current send tests pass but do not prove all other write kinds. Add unique positive Sent discovery where receipt/placement evidence permits, confirmation handling for notification uncertainty as required, and pending recovery lifecycle/cleanup/race work. Do not mark full Task13 or the goal complete at this milestone. Frozen history now adds21 without modifying old20 entries; all current-schema expectations were advanced to21.

New implementation: `store/operation_reconciliation.rs` plus schema21 `reconciliation_schema.sql`, `Engine::reconcile_operation`, Operations.ReconcileOperation proto/daemon, CLI `operation reconcile --account ID --operation ID --request-id UUID --version N [--resume-safe] [--wait]`. Request identity is scoped and durable; duplicate input returns current original, mismatched input conflicts. Latest request is visible on Operation with mode/time/expected version/first attempt ordinal. Restored old request records remain history (`active=false`) until a new request at the restored operation version. Queued admission itself does not dispatch disconnected accounts.

ExecutionPolicy derives restoration provenance and current request mode. Observe mode prevents mutation retries. ResumeSafe permits existing idempotent flag/label/conditional-calendar continuations and completing source deletion after a proven IMAP copy; never replay unknown restored SMTP DATA/COPY or recreate a missing restored calendar create. Provider SMTP/IMAP guards act before remote writes; store begin/finish also constrain replay. Positive Google read resolves original sends; negative read stays uncertain. Restored accepted client-managed Sent with no retained placement stays uncertain (positive unique Sent discovery remains to implement/verify); server Sent uses existing positive-read path when its prior acceptance/floor evidence exists. Notification-enabled Calendar positive state still cannot prove notification effects. Do not silently narrow the full recovery goal to the current send coverage.

Evidence since schema20: focused `restored_reconciliation`2 passed0failed/ignored in0.69s (`/tmp/nuncio-restored-reconciliation-first.log`). Covers request identity/account scope, crash recovery of admitted attempt, no repeated SMTP from stalePrepared, and second restore retaining old request audit without permission. Actual recoveryE2E1 build/suite0 in test-results/recovery_e2e; negative Google observation and ResumeSafe requests stay held withzero accepted sends through SIGKILL/restart/replay, then explicit new duplicate-risk resend alone sends. Separate reconciliation_system1 build/suite0 in2.05s covers both old queued snapshots with original never-sent and original later-sent+lostack; authenticated admission/invalid mode/version/foreign account/replayed IDs, positive receipt/original identity, unchanged independent Google mailbox/calendar state andsendcounts0/1. A later limit regression failed101 because1000 previous attempts still admitted another request; admission now refuses exhausted attempt or request history before changing state. Focused3tests pass1.30s. Actual strictIMAPE2E11 passed58.13s with both Sent policies, explicit Observe/ResumeSafe after stale queued backups, identical replay afterSIGKILL, no fabricated receipts and independent DATA/APPEND/delivery counts unchanged.

Actual recoveryE2E now2passed3.53s. New positive case snapshots queued original, then original sends with lost remoteack. Restored request is durably admitted behind feature-only reconciliation_after_admission while the worker observes Sent and pauses before receipt. Real CLI has no acknowledgement; SIGKILL daemon, realCLIexit4, restart original operation reconciles with2attempts (interrupted withoutreceipt, thenpositive receipt), originalid/request preserved and independently only1send. A real bug was reproduced: abruptHTTP2 surfaced tonic::Code::Unknown and CLI claimed operation_failed/exit1. Temporary test-feature diagnostic logged only the enum (retained in lost-ack-diagnostic/status-code.log), then was removed. Unknown/Cancelled now report request_outcome_unknown/exit4, safe guidance to inspect/retryidentical request. Production exclusion test includes the new admission marker. No full schema21 claim until running gate finishes.

Retained red: missing new storage API compilation in `/tmp/nuncio-restored-reconciliation-red.log` (also corrected test create_backup signature); actual CLI missing reconcile failed2 versus expected5 and is copied to test-results/task13-reconciliation/recovery-cli-red. No assertions/mockvalidation weakened. New unit tests are crates/nuncio-engine/tests/restored_reconciliation.rs; system file reconciliation_system.rs; existing recovery_e2e and support/smtp_recovery_e2e extended. Frozen fixture tests now load schema-history-through-21.json, preserving old20definitions/hashes exactly.

## Current verified evidence

- Later `test-results/task13-operation-history/results.json`: all4commands0 (fmt/bothClippy and combined historical suites6tests,0failed/ignored). New `historical_operation_history.rs` tests all11 schemas10–20 with22 scoped send records each across Google/IMAP: running/applied/retry/conflict/uncertain/failed/cancelled, queued originals, and abandon/resend/manual-confirmation history. Exact data survives migration. Restore retains positive send/copy receipts, immutable wire/request/resolution/SMTP state and old restoration provenance; closes unfinished attempts at max(now,start), holds10 pending operations and refuses dispatch/reconciliation attempts until a new explicit path exists. Changing only account state to connected still yields no ready work. Source backup bytes remain unchanged. This is storage evidence, not independent remote observation or every write-kind restoration. First compile101 and green2tests4.28s are retained; final6-test gate passes.

- Latest `test-results/task13-migration-crash/results.json`: all eight commands exit0. Fmt, bothClippy, fullengine106, proto/CLI/daemon19, actualmigrationE2E1 (40cases), actualGoogle recovery1, releaseisolation2 pass, zero failures/ignored. `migration-cases.json` records all40 distinct before/after-commit cases, ciphertext hashes and frozen DDL hashes; exact catalog/user rows, unchanged keys/profile and independent Google snapshot, two daemon/CLI restarts each. `counts.json` and `artifacts.json` retain counts/current binary hashes. Fresh production build finished45.51s; release test flags/markers are excluded. No claim that migration tested a live provider or independent IMAP effects.

- `test-results/task13-historical/results.json`: all 8 commands exit 0: Rustfmt, both workspace Clippy configurations, full engine **105**, proto/CLI/daemon **19**, actual repair E2E **2**, actual Google recovery **1**, actual IMAP E2E **11** (57.22 seconds). Zero failed or ignored. Exact commands, copied suite logs and counts are retained there.
- Later test-only follow-up: `test-results/task13-historical/failure-follow-up/results.json`, all 4 recorded commands exit 0. Historical suite **4** passed (7.92 seconds), followed by fmt and both Clippy modes. Covers 20 schema upgrades, 20 historical backup restores, 19 injected migration failures and missing-required-table negatives. This includes one test added after the full 105-test engine run; no fabricated full-106-test claim.
- `test-results/task13-repair/results.json`: preceding 13-command gate all exit 0: engine 101, proto/CLI/daemon 19, repair system 2/actual E2E 1, Google system 21/actual E2E 26, operation system 6, IMAP system 22/actual E2E 11 and actual recovery 1, plus fmt/both Clippy. Zero failed or ignored.
- Earlier Maintenance, backup, restore and SMTP evidence remains linked in VERIFICATION and the history checkpoints. No remote CI, live provider compatibility, production package or installation claim.

## Implementation locations and guarantees

Projection repair: `store/{repair,calendar_repair}.rs`, `calendar_repair_schema.sql`, `engine/repair.rs`, `calendar.rs`, shared store calendar/mail publication helpers, Maintenance proto/daemon handler, and CLI `repair.rs`. `repair --account ID --scope mail|calendar --dry-run` performs local integrity/count reads without provider requests or revision changes. Actual mail repair stages a full scan and rebuilds scoped FTS. Calendar repair stages catalog/new IDs/canonical events/occurrences privately and publishes the entire account in one transaction. Compatible full jobs join; incompatible jobs report busy. Inspect/cancel runs through existing System commands. Drafts, attachments and durable operations/history are retained; repair never resolves or resends pending writes.

Independent tests cover failure/cancel/SIGKILL/restart and remote send/copy/notification observations, including queued Google and SMTP sends. Fixed two reproduced defects: orphan FTS rows survived full repair; lost calendar read access kept coverage current. Full publication now removes orphan search rows transactionally. Calendar permission/timezone changes invalidate coverage/cursors; retired cursors are removed while cached events retain identities. No event access yields unavailable coverage, and restored access needs a fresh read.

Historical fixtures: `tests/fixtures/migrations/schema-history-through-20.json` freezes 20 definitions and per-definition hashes, captured once from migration sources and never regenerated during verification. `tests/support/historical_store.rs` builds old keyed stores without invoking the current migration runner. `tests/historical_migrations.rs` compares existing columns across all user tables (except advancing ledger/internal FTS), checks public data/payload reads and restores each historical backup into a new encrypted store with pending sends held. `legacy-message.eml` is a real 1,424-byte multipart containing the 612-byte recovery PDF; Python and Rust decoding check exact attachment bytes.

Historical backup inspection originally failed with Database(1): schemas before 7 lack drafts, and before 10 lack operations. `store/backup.rs` now counts using explicit table introduction versions. Missing tables in schemas that require them still fail. All 19 next-migration ledger writes are tested with a real SQLite abort trigger after DDL; failure preserves original encrypted bytes, catalog and rows, and removal of the injected failure allows retry. The later actual migration suite now separately supplies all40 SIGKILL boundaries; it does not replace these rollback assertions.

Unreadable-store evidence: `tests/repair_preservation.rs` rejects relational corruption without changing database/WAL bytes or drafts. Actual `repair_e2e.rs` rejects corrupt-header/truncated profiles, preserves profile/data/test-key files, produces no provider requests, and reopens after the test restores its retained synthetic original. Compile-only fixture errors and original behavioral failures are retained in the gate directories; no assertions/mock validation were weakened.

## Remaining full scope and risks

Task 13: historical histories for other write kinds as their recovery paths are implemented; safe explicit reconciliation for every restored operation kind; abandoned staging/new-key cleanup after process death; activation parent races and storage-error cases; disconnect retention/reconnection identity and further export adversarial cases. Spec section 8 explicitly excludes destructive local account purge. Disconnect removes credentials and retains cache/drafts/history.

Restored-operation caution: an old queued/Prepared snapshot does not prove nothing was dispatched afterward. Existing SMTP Prepared reconciliation can repeat DATA, and IMAP Prepared transfer reconciliation can repeat COPY. Do not expose generic resume without restoration-provenance guards and independent remote evidence. Known accepted SMTP may still have a later unknown Sent copy. Google negative Message-ID search does not prove no send. Existing explicit duplicate-risk resend creates a new operation; it does not safely resume every original operation.

Task 14: security/parser/resource/multiple-engine suites and outstanding defects in earlier checkpoints (blocking work ownership, SMTP post-submission storage failures, worker-unwind crash investigation, IMAP literal/delimiter/case/capacity issues, attachment aggregation, legacy intents, calendar inheritance and Trash origins). Official CalendarList now includes writerWithoutPrivateAccess; current mock role control rejects it and writes permit only owner/writer, so its semantics remain unverified.

Tasks 03/15: explicit test egress denial (Docker bridge is not a firewall), external descriptor-generated client, contract/API documentation, security/dependency checks, current production packages and CI files. Task 16: final R01–R16 matrix and separately authorized live acceptance. MANUAL-ACCEPTANCE.md exists; every live row remains deferred/unapproved/unverified. No requirement is marked fully complete.

Detailed prior state is retained in `docs/history/2026-09-11-repair-and-historical-checkpoint.md`, `2026-09-11-maintenance-checkpoint.md` and the other linked history files. Their old pending/running prose is superseded by this file.
