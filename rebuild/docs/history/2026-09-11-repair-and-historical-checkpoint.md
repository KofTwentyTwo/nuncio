# Rebuild session state

Updated September 11, 2026. Active unbudgeted goal: all16 tasks and R01–R16. User requires full local Google and Synology mocks now. Live acceptance is deferred/unverified, not a current blocker. Work inline. No agents, commits, push, merge, publication, installation into normal environment, remote settings or live-account actions. Preserve original source/data/unrelated changes. Native apps excluded.

## Workspace and authority

Worktree `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, feature/nuncio-google-first-rebuild, baseline a268fd5a004977b65fd0347ceeb1c69755529aa3. Cargo cwd rebuild/. Original checkout remains dev; rebuild untracked/uncommitted. Read root AGENTS→CLAUDE, personal rules/style, rebuild/AGENTS and these state/TODO/VERIFICATION files after reset. Approved spec/plan/audit are in worktree docs/, one level above Cargo cwd. Existing user authorization overrides issue/replanning/commit/delegation defaults. Do not recreate the goal.

## Immediate next action

Running `python3 test-results/task13-historical/run-gate.py > /tmp/nuncio-task13-historical-gate.log 2>&1`, shell session75564. Eight commands cover Rustfmt, bothClippy, fullengine, proto/CLI/daemon, actual repairE2E, recoveryE2E and IMAPE2E. Format0 at this note. No source edits during gate. Prior full task13-repair13command gate all0: engine101/protoCLI19/repair2+1/Google21+26/operation6/IMAP22+11/recovery1, allzero failed/ignored. That evidence remains in its directory. Production schema12 stale; current source schema20, feature binaries need the running gate to refresh after backup inspection fix.

New tests: engine repair_preservation1 plus lifecycle6 passed `/tmp/nuncio-repair-preservation-second.log`; actual repair_e2e build0/suite0,2pass0failed/ignored,2.08s `/tmp/nuncio-repair-unreadable-e2e-first.log`. Relational integrity failures leave database/WAL exact bytes and draft intact. Actual daemon/CLI refuse corrupted-header/truncated profiles, leave profile/data/keys unchanged, make zero mock requests and successfully reopen after the test restores its retained original. First storage test compile101 was wrong get_draft name/unsigned rusqlite count type, fixed without changing production behavior.

Historical fixtures: tests/fixtures/migrations/schema-history-through-20.json contains20 frozen/checksummed definitions captured once from migration sources, never regenerated during checks. tests/support/historical_store.rs constructs keyed old stores independently of current migration runner and seeds two accounts, mail/FTS/MIME/PDF/calendar/drafts/send identities/config/legacySMTP. historical_migrations.rs first migration-only test passed1 wrapping20schemas. Added historical backup restore over all20 and missing-required-table negatives: original first failed101 Database1 because inspect_backup unconditionally queried tables absent before schema7(drafts)/10(operations). Production counts now use explicit introduction versions; newer schemas still must contain those tables. Three tests subsequently passed6.74s before final MIME-fixture improvement. Frozen legacy-message.eml is now a real1424-byte multipart with the612-byte PDF, generated/decoded by Python; current gate also requires Rust parser part2/exactPDF and consistent draft subject/body. Do not claim final fixture check until gate passes.

Next after gate: preserve all logs/counts and update docs/memory; add deterministic failed/interrupted migration cases, then safe explicit original-operation reconciliation, cleanup/activation races and disconnect retention/identity invariants. Spec section8 excludes destructive local purge. Continue Tasks13–16 and keep full goal active.

Source schema20 projection repair is implemented through Store, Engine, authenticated Maintenance RPC and CLI `repair --account ID --scope mail|calendar [--dry-run|--wait] [--from DATE --to DATE]`. Calendar stages new catalog IDs/canonical events/occurrences privately and publishes the whole account atomically. Mail full promotion rebuilds all scoped FTS rows in the transaction, including orphans. Preview runs integrity/count queries without provider requests or revision changes; counts include retained cache, not a reservation.

Focused evidence: calendarrepair3 + ordinarycalendarprojection1 pass `/tmp/nuncio-repair-calendar-access-green.log`; storage mailprojection2 pass `/tmp/nuncio-repair-search-green.log`. Latest repair_system build0/suite0,2pass0fail/ignored,0.30s (test-results/repair_system). Actual Google repairE2E1 pass,2.12s before calendar-permission fix. Actual IMAPE2E11 passed0fail/ignored,56.56s before the calendar-only follow-up. New Synology system case passed1 with bothTLSmodes (4.69s), after first full21pass1fail was only comparing global progress revision as immutable. Corrected to monotonic revision plus exact items/coverage/paging; preview still asserts exact unchanged revision. Initial compile failures retained under task13-repair. Feature binaries schema20, production schema12 stale. No current full-gate claim yet.

Permission regression reproduced current coverage after losing event-read access (red101 `/tmp/nuncio-repair-calendar-access-red.log`). Shared catalog publication now invalidates coverage/cursors on access-role or timezone change, removes retired-calendar cursors, and coverage is unavailable for none/freeBusyReader. Cached event identities/data remain preserved; restoration needs fresh read before current. API tests cover incompatible ordinary-sync admission, matching repair join, cancellation, missing-accessRole fail-closed coverage and successful refreshed access. Latest official CalendarList reference also advertises writerWithoutPrivateAccess: existing mock role control rejects it and write checks allow only owner/writer; its read/write semantics remain unverified; track under Task14 compatibility, do not silently claim all current roles.

## Latest completed work

- `test-results/task13-recovery-bounds/results.json`: all7commands0: Rustfmt, normal/feature workspaceClippy, engine97, proto/CLI/daemon18, actualGoogle recovery1, actualIMAPE2E10 (57.18s), zero failed/ignored. Exact commands/logs/nested copies/counts retained. Feature daemon/CLI current. Production artifacts remain stale schema12; no package/install/remoteCI/live-provider claim.
- Preceding `task13-maintenance`12-command gate all0: engine93, proto/CLI/daemon18, Google system21/E2E26, operation system6, IMAPsystem21/E2E9, Google recovery1, bothClippy. First canonical-path failure retained. `task13-export-protection/green` adds actualGoogleE2E26,26.57s after mail-path fix; source changes since then concern backup/resource code exercised by the latest recovery gate.
- `support/smtp_recovery_e2e.rs`: actual daemon/CLI with independent Dovecot/Mailpit, bothSentpolicies. Snapshot says queued; original then submits with lost SMTP ACK; restored reconnect/restarts do not repeat delivery/APPEND or invent receipts. Only explicit duplicate-risk resend produces second delivery/distinctMessage-ID/identical realPDF and expected independent Sentcopies. Initial standaloneIMAPE2E10 pass68.78s copied task13-smtp-recovery; latest gate rerun above57.18s.
- Maintenance implementation: `engine/maintenance.rs` means `crates/nuncio-engine/src/maintenance.rs`, plus engine/recovery.rs; proto maintenance.proto; daemon maintenance.rs; CLI backup.rs and backup/{secret,upload}.rs. Bounded owned blocking transfers retain semaphore permits through cancellation, private temp files, length/hash/framing,1TiB/256KiB limits,30s uploadidle/1h CLI RPCtimeout. Secret inputs are redacted/zeroized and protected stdin JSON. Restore initializes fresh identities/keys into an atomically activated new sibling; no arbitrary daemon-file read/write RPC.
- CLI `output_file.rs` shares canonical output validation between backup and mail raw/body/attachment exports. Authenticated System status supplies absolute protected paths, including future store.db-journal for relative profiles. Genuine new actualCLI red wrote journal and returned0; fixed refusal1 with journal absent, all26GoogleE2E pass. Existing destinations/hardlinks/danglinglinks/parentaliases refused; atomic no-replace/hash/fsync behavior retained.
- Store `backup_io.rs`: shared1TiB limit, checked fstatvfs preflight (upload1x/backup2x/restore3x plus8MiB), bounded copy with one-byte growth probe, actual I/O errors propagate. SQLCipher create/rekey export caps max_page_count and verifies effectivevalue. Inspection hashing reads at most original length+1. Sparse oversize input rejected before copy and leaves only original sentinel. Real page-limited SQLCipher export fails, preserves source payload/length and respects output bound. These estimates do not reserve disk and do not complete all migration-growth/failure/crash coverage.

## Failure evidence and implementation notes

Original Maintenance E2E found consumed barrier `.arm` cleanup (fixed to `.entered`) and direct wall clock instead of injected engine time (production clock plumbing fixed). Exact macOS parent expectations then failed /var versus canonical /private/var; tests now compare exact canonical paths, not suffixes. All original logs retained under task13-maintenance.

Backup bounds started with missing helper compile101, then pinned rusqlite signatures/types compile101; use i64 checked conversions and Option<&str>. Integration failed Database(None): isolated probe revealed SQLCipher page_size returns Text cipher_page_size (bundled libsqlite3-sys0.38.2/sqlcipher/sqlite3.c:112162–112177). Parse text and validate512..65536/poweroftwo. sqlcipher_export wraps underlying SQLITE_FULL with sqlite3_result_error (113304–113308), observed extended_code1 plus exact database-or-disk-full message. The final real export test asserts that precise wrapper as well as preserved data/outputbound. Temporary probe source removed; diagnostics and failures retained task13-recovery-bounds. No assertion weakened or production validation bypassed.

RECOVERY/RUNNING now document protected passphrase input, backup/restore commands, stopped original daemon before operating restored copy, freshkeys/target uncertainty, and limits. `docs/MANUAL-ACCEPTANCE.md` is a concrete later worksheet with approval/resource fields, exact command suffixes and independent observation criteria. All live rows are deferred/unapproved/unverified; no account setup is requested now. Native apps remain excluded.

## Remaining scope

Task13: projection repair/dry-run; all supported historical-schema/interrupted-migration fixtures; unreadable/corrupt-store preservation; account-removal semantics; safe original-operation reconciliation; broader raw-export adversarial/error coverage; abandoned stages/key cleanup after process death; activation-parent races. Task14: full security/parser/resource/multi-engine suites and known defects in historical notes. Task03/15: explicit test egress denial (Docker bridge is not a firewall), external descriptor client, contract/API/docs, fresh production packages/release checks/CI files. Task16: finalR01–R16 evidence matrix; manual worksheet exists but live checks remain deferred and require named explicit authorization later. No R01–R16 is fully complete.

Previous detailed checkpoints: `docs/history/2026-09-11-maintenance-checkpoint.md` and `2026-09-11-restore-profile-checkpoint.md`. They retain historical pending/running prose; this section supersedes it.

## Latest verified gates

`test-results/task13-restore-profile/results.json`: all 4 commands exit 0; fmt, both Clippy modes, full engine 88 passed / 0 failed / 0 ignored. Independent PDF checks both exit 0. Storage and profile lifecycle only; Maintenance API/CLI and actual recovery E2E remain outstanding.

`test-results/task13-restore-core/results.json`: all 4 commands exit 0; full engine 85 passed / 0 failed / 0 ignored. This precedes profile lifecycle.

`test-results/task13-backup-core/results.json`: all4commands0, fmt/bothClippy/fullengine82, zero failed/ignored. Backup storage only; no restore/API/CLI/release completion claim.

`test-results/task12-smtp-profiles/results.json`: all5commands0, Rustfmt, bothClippy, IMAPsystem21/actualIMAPE2E9, zero failures/ignored. Tests-only follow-up to confirmation gate below. Missing UIDPLUS client refuses before DATA; server positive with both TLS modes and actualCLI exact MIME. Queued endpoint change cannot redirect intent; restoring captured endpoint results in one delivery. First fixture used a barrier while holding the account lane (RPC timeout); moved to before_dispatch. First gate mistaken newest-first index0 assertion corrected to exact ordinal1 and strengthened receipt/state checks; retained logs. Schema18 unchanged. No process running at checkpoint.

`test-results/task12-smtp-confirmation/results.json`: all9commands0: Rustfmt, normal/featureworkspaceClippy, engine80, protoCLI8 (includes integration and doc tests), IMAPsystem19, Googleoperation-system6, Googlesystem21, actualGoogleE2E26. Separate actual IMAPE2E build0/suite0,8passed0failed0ignored; copied logs/results in imap-e2e-suite. All counts in counts.json. Feature binaries current/schema18; production still Task11/schema12. Prior gate results below remain historical.

SMTP confirmation: additive optional proto field6 smtp_acceptance_note, storage validates separate acceptance note plus exact frozen Message-ID and immutable account/mailbox/epoch/UID floors and non-Prepared phase. No fabricated SMTP/copy receipts or progress changes. Existing Google canonical evidence preserved when absent. Engine smtp_confirmation negatives/races/reopen; system and actualCLI four policy/confirm-or-abandon profiles independently verify Mailpit envelope/wire hashes and Dovecot Sent UID/epoch/raw. Explicit sync does not resolve uncertainty; restart and repeated/conflicting decisions preserve remote counts and attempt history. ActualCLI raw export equals independent Sent bytes. Initial E2E failed only mistaken conflict exit4 versus existing5; strengthened to check5 and structured conflict. First failures retained in gate directory. RUNNING updated.

`test-results/task12-smtp-resend/results.json`: all7commands0 (Rustfmt, normal/featureworkspaceClippy, fullengine79, IMAPsystem18, Googleoperation-system6, actualGoogleE2E26). Separate actual `python3 scripts/verify.py --suite imap_e2e` build0/suite0,7passed0failed0ignored,38.98s; copied into task12-smtp-resend/imap-e2e-suite before subsequent runs. Gate counts.json labels that retained result. No unnecessary second IMAPE2E rerun after only adding storage/system assertions. All new code passes bothClippy modes. Schema18 unchanged. Feature binaries current; production remains Task11/schema12 and must be rebuilt/verified before final artifacts.

Resend store fix: store/operation_resolutions.rs::insert_resend calls smtp::capture inside the original-resolution/newoperation/frozenblob transaction, capturing current SMTP/Sent configuration and fingerprint for a new Message-ID. Original unknown phase/receipts stay unchanged. smtp_resend.rs unit verifies newPrepared/floorNone, correctpolicy/endpoint/mailbox/fingerprint, malformedSent rollback, deleted draft, privateBcc/recipients, reopen and duplicate resolution. Original test red101 NotFound for replacementSMTPintent after correcting test-onlyDebug/privateAPI compile101. Focused operation_resolution+smtp_resend4passed0; fullengine79passed0 includes later rollbackassertion. System test initiallycompile101 borrowedtemporaryclients in tokio::join; namedclients fix, fullsystem18 nowpasses.

support/smtp_resend_e2e.rs tests bothSentpolicies, lostfirstSMTPack, deleted draft/binarysource, requiredduplicate-riskflag, newMessage-ID with exactfrozenbody/binary/envelope, twoacceptedSMTPmessages only after explicit newdecision, correctSentcopies, secondrestart/decisionreplay and no fabricatedoriginalreceipts. imap_write_system/smtp_resend.rs independently tests API duplicate-risk refusal, foreignaccountNotFound, concurrent identicalresolution and restart with same replacementID, separate delivery/copy/receipt counts.

`test-results/task12-smtp-server/results.json`: all12commands0: Pythonlint/format,Rustfmt,bothClippy,engine78,protoCLI4,independentIMAPcontracts2(wrapping18Python),IMAPsystem17,actualIMAPE2E6,Google21/26; allzero failed/ignored. Copied nested logs/results and counts.json retained. This gate precedes resend. Initial import-orderfailure retained. Expanded system6profiles pass: missingcopy, changedbody, duplicate, explicitUIDVALIDITY, auto-recreatedSent afterrename, genuinelymissingcustomfolder. Positive client/server send fixtures bothTLSmodes. AutoSent owner now uses Mailpit committed Username, not asynchronous/user-controlledtags; misleadingtag regression red1→green0. Safe diagnosticfile includes onlytype/sourcepositions. Source refs and prior failures in VERIFICATION and MOCK-MAILPLUS.

## Current schema18 implementation and evidence

Server-managed Sent: domain/submission/sent_fingerprint.rs hashes selected parsed identity headers and exact body; optional Bcc may be omitted but not altered/added. Duplicate identity headers rejected, trace ignored; header1MiB/512count, MIME64MiB. mail-parser0.11.9 serde enabled via offline lock update. Empty optional Subject is valid (focused red then fix). Version1 content fingerprint is comparison evidence, never proof of SMTP delivery by itself.

Store migration18 smtp_floor_schema.sql adds nullable fresh sent_floor without guessed backfill. SmtpIntent captures optional fingerprint only for server policy. Worker refreshes Sent UIDNEXT after SMTP envelope/DATA354 and persists it before DATA body. record_server_sent requires known SMTP acceptance, exact account/mailbox/UIDVALIDITY, UID>=fresh floor and matching content. Separate server_sent_observed positive_read receipt persists before atomic local Sent publication. Final application rechecks raw content. Existing schema17 client rows remain compatible; old server rows missing proof fields stay uncertain.

providers/imap/sent/server.rs searches in 4096 sequence windows, max1million messages, intersected with fresh UID floor and current UIDNEXT. Strict SEARCH cardinality/tags/UIDs/bounds; unique candidate must match MIME, and complete candidate set is rechecked after body fetch. No copy search infers unknown SMTP acceptance. Duplicate -> terminal uncertainty. Missing/mismatched -> bounded observation retry. operations/smtp.rs adds operation_after_server_sent checkpoint. PreparedWrite SMTP intent boxed to resolve Clippy large enum without suppression.

Verified focused storage command `cargo test --locked -p nuncio-engine --test smtp_server_operation --test smtp_operation --test sent_fingerprint -- --nocapture`: exit0,4passed (2fingerprint+client+server). Server test proves floor excludes old identical MIME, no proof before acceptance, no client APPEND in server policy, wrong-content rollback, persisted floor/proof on reopen and separate receipts.

Actual `python3 scripts/verify.py --suite imap_e2e`: build0/suite0,6passed0failed0ignored,37.16s (test-results/imap_e2e/). New support/smtp_server_e2e.rs covers three positive crash boundaries, duplicate remote copy and lost final DATA ACK, second restart/replayed request, independent Mailpit/Dovecot count/byte/envelope checks and no client APPEND. These precede the new SystemHarness barrier support but exercise current schema18 production adapter. Feature binaries schema18; production binaries remain Task11/schema12 and need rebuild later.

Strict server-Sent SEARCH transcript test originally hung because fixture LIST omitted required INBOX, so client correctly rejected before EXAMINE. Stopped exact owned process94529 with SIGTERM; terminated Cargo101, subsequent drop-before-join exposed peerEOF panic101. Corrected LIST fixture and added5s peer timeout. `cargo test --locked -p nuncio-engine --lib providers::imap::sent::server -- --nocapture`: exit0,1passed/12filtered,10transcript cases,0ignored,0.01s. No production parser/assertion weakened. FeatureClippy large-enum101 fixed by boxing; both subsequent full gates pass.

## Previous verified checkpoints

Client SMTP/Sent schema17: test-results/task12-smtp-client/results.json all9statuses0 (fmt,normal/featureClippy,protoCLI4,engine74,IMAPsystem15,E2E5,Google21/26). First Google run20pass1fail stale schema16 assertion(actual17), preserved first-failure files, corrected explicit assertion and resumed only affected/remaining checks. APPEND rejection follow-up test-results/task12-smtp-rejection/results.json all6statuses0 (fmt,bothClippy,engine13,IMAPsystem15,E2E5). Seven client SMTP crash boundaries, copy-only retry after definitive taggedNO; missing/conflicting ACK never repeats.

Full historical implementation/evidence retained in docs/history/2026-09-11-smtp-client-session-state.md, through-imap-transfers.md, transfer-groundwork.md, mailplus-through-account-transport.md, through-calendar-writes.md (all September11 prefix), and server-sent-in-progress.md. Supersede their stale next-action prose. VERIFICATION retains dated failures and gates; latest evidence first.

## Existing mail implementation and mocks

Schema14 IMAP reads/flags,15 strict COPYUID/nativeMOVE orCOPY+UID-scoped deletion/expunge,16 Trash restore origins survive full resync,17 durable SMTP prepared→started→accepted→appending→copied. No broad EXPUNGE. Local placement identity includes account/mailboxUUID/UIDVALIDITY/UID. Frozen send wire stripsBcc but envelope retains it, privateSent blob retainsBcc, finalCRLF fixed before enqueue. Durable markers precede DATA/APPEND and positive receipts precede projection. AcceptedSMTP never repeated because copyfailed. Complete negative DATA resets prepared+floor; complete matching negative APPEND resets only copy phase.

Independent Google stateful HTTP/OAuth provider precedes adapter. Local tests/imap Python controls real pinned Dovecot2.4.5/Mailpit1.31.1, synthetic alpha/beta/observer, localCA, ownedDocker strict faultcontrols. MockMailPlus start/start_with_sent_policy,credential,control,shutdown. Controls mailbox/raw,smtp/smtp_raw,snapshot,inject/wait_fault/release,folder/copy/expunge/uidvalidity/flags/append,wait_server_sent. Counters lowercase 'smtp DATA','imap APPEND'. Server AutoSent uses direct Dovecot and independently observes Mailpit, never client APPEND proxy. Mailpit trace/Bcc transform is mock-specific; verify wire suffix hash/bytes, do not generalize to live providers.

## Remaining audit/release risks

SMTP confirmation/resend/abandonment now verified above. Worker-unwind SIGSEGV rootcause unproven; postsubmission store failures; blocking work memory permit/cancellation ownership; HTTP2/reconcile retries; IMAP literal/delimiter/lowercase NIL profiles; retired mailboxes count toward4096limit; aggregate attachment limits; validPDF fixture; upgrade/oldqueued IMAPintent fixtures; external proto client; Calendar exception/series inheritance; Trashorigin orphan retention/repair/backup. See TODO/history.

A normal Docker bridge is not an egress firewall; explicit CI egress denial remains pending. No live provider or remote CI ran. Production artifacts stale(schema12), no package/install/signing claim. Secondbrain memory merged via /tmp/nuncio-server-sent-memory.py with scoped escalation exit0 (project/daily/index only, no secrets/git).

Latest secondbrain checkpoint merged via /tmp/nuncio-restore-memory.py, scoped write exit 0; project/daily notes only, no secrets or Git operations.
