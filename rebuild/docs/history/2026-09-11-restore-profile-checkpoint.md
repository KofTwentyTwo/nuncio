## Immediate next action

Task 13 restore storage and new-profile lifecycle are implemented; source schema is 19. The full Maintenance API/CLI and actual recovery E2E remain outstanding. Continue inline with those features after checking the current gate.

- Storage: `store/restore.rs`, `store/restore_schema.sql`, `tests/restore.rs`. Copy input into a private stage, inspect/export with SQLCipher under a new database key, migrate, clear old credential references and cleanup queue, disconnect accounts, hold pending operations. Preserve source state/version/hash in `restored_operations`; close unfinished attempts as uncertain without fabricated receipts. Atomic rustix no-replace rename preserves existing targets, including empty directories and symlinks. Post-rename directory fsync failure preserves the activated target and returns `RestoreActivationUncertain`.
- Profile lifecycle: `engine/recovery.rs`, `profile/restore.rs`, `tests/restore_profile.rs`. `Engine::restore_profile(EngineConfig, source, passphrase)` creates fresh profile/database/API identities, initializes only the private stage, and activates without starting the restored engine. Partial keystore errors roll back only newly owned references. Failed cleanup returns `RestoreCleanupIncomplete { profile_id }`. Activation uncertainty retains keys for the activated profile. Ordinary startup still rejects a database without its manifest.
- Tests: restore storage has 3 passing tests; profile restore has 3 passing tests, including partial persistence before keystore error, failed cleanup, original-profile reopen, and concurrent target creation after keys are written. Real `tests/fixtures/recovery/queued-draft.pdf` passes independent Poppler `pdfinfo`/`pdftotext` (one page, 612 bytes, PDF 1.4). Storage comparisons include exact draft/attachment/frozen MIME rows, request IDs/fingerprints, positive receipts, pending holds after reconnect/startup recovery, wrong key/corruption/truncation/future schema, schema 18→19 success and failed migration, source/target preservation, sidecars, permissions and drop cleanup.

Latest gate complete: `test-results/task13-restore-profile/results.json`, all 4 commands exit 0 (Rustfmt, normal/feature workspace Clippy, full engine: 88 passed, 0 failed, 0 ignored). `counts.json`, exact commands and logs retained. Separate independent PDF checks both exit 0 in `pdf-checks.json`. No process is running. Previous storage gate `test-results/task13-restore-core/results.json` remains 4/4 commands exit 0, engine 85. Its first sandbox engine run failed on 10 loopback binds; the narrow authorized rerun passed. Production artifacts remain schema 12; feature executables precede backup/restore. Source schema is 19.

Next action: bounded authenticated Maintenance streaming API/CLI for create/inspect/restore, then explicit safe reconciliation/resume and actual daemon/CLI recovery E2E. The restore blocking job owns its resources after an awaiting caller disappears; API admission permits must cover the worker's full lifetime, not just the awaiting future. Add one active maintenance slot and bounded chunk/file handling without buffering an entire backup. Keep passphrases out of argv/config/logs. A queued old snapshot may already have been sent afterward: never treat stale SMTP/IMAP Prepared markers as proof that repeating DATA/COPY/APPEND is safe. Profile holds alone do not complete recovery.

Remaining restore audits: source schema identity beyond user_version (triggers/semantic corruption), disk/copy bounds and cancellation, activation-parent identity races, crash cleanup of private stages/new secret references, every supported historical-schema fixture, held retry/conflict and terminal-disposition cases, explicit resume and repeat-safe provider reconciliation. Task 09 checklist/matrix, Task 14 adversarial/resource defects, Tasks 03/15 egress/production/contract/CI, and Task 16 worksheet/matrix remain authorized. Do not recreate, complete or block the goal while offline progress remains.

Diagnostic history: restore first compiled with missing API (exit 101), then unused TempDir::keep return (101). First behavioral tests failed 0/2: SQLCipher readonly main connection also made the attached export read-only. A temporary probe reproduced the exact error; only the owned input copy now opens READ_WRITE, and the source is never opened writable. Probe source removed; logs retained. Later focused storage tests pass. Profile tests initially failed compilation on the absent Engine API/error variant, then passed after implementation. No assertions weakened.
