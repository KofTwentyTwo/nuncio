# Superseded schema20 checkpoint

The eight-command migration gate finished exit 0, all commands 0 (`test-results/task13-migration-crash/results.json`). Source and fresh feature/production binaries now use schema20. Local release daemon/CLI are at `target/production/release/{nunciod,nuncio-cli}`; hashes and sizes are in that gate's artifacts.json. No package/install/remote-CI/live compatibility claim.

No process is running. Shell sessions 13121, 23164 and 95101 are terminal. The later four-command historical-operation gate also finished exit0, all commands0. Its two new tests were added after the full106-engine gate; there is no claimed full108-test run.

**Next concrete action:** implement explicit reconciliation of restored original operations through engine/API/CLI, starting with read-only observation and durable request admission. Inspect `operations.rs` process/preparation plus provider-specific `operations/{smtp,imap_transfer,calendar_change,mail_change,imap_flags}.rs` before reusing reconciliation. Existing Reconcile can issue SMTP DATA/COPY or make a write retryable; do not simply set `needs_reconciliation=1` on restored work. Add regression tests first using independent remote counters, especially an old queued backup whose original later sent/copied/notified. Preserve account-scoped request identity/version and original attempts/receipts; negative observations must keep ambiguous work held. Known positive evidence can resolve the original without replay. Current CLI supports explicit send abandonment/positive manual confirmation/new duplicate-risk resend, but that is not general restored-operation reconciliation. Continue remaining operation kinds and cleanup/activation races after this; do not restart planning or stop at the provider milestone.

Migration implementation: store `StoreOpenOptions` passes the existing feature-only TestConfig into its owned worker; migration commit helper calls finite blocking checkpoints immediately before/after actual commit. No environment/global hooks or production test flags. Public Store::open and restore worker default to no controls. Test-only hex/rusqlite dependencies were already locked; `cargo update --offline -p nuncio-test-support` locked zero new package versions. Actual test is `nuncio-test-support/tests/migration_e2e.rs`; release isolation verifies feature-positive/production-negative markers. Retained failures: intended missing checkpoint, compile-time local OpenOptions shadow, and test fixture CLI JSON/command corrections. No production assertions or mock validation weakened.
