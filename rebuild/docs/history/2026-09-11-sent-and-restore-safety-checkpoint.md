# Rebuild session state

Updated September 11, 2026. Full unbudgeted goal is active, covering Tasks01–16/R01–R16. Continue inline. User requires full local Google and Synology mocks now; live acceptance remains deferred/unverified, not a blocker. Native apps excluded. No commits/push/merge/publish/normal-environment install/remote settings/live actions. Preserve original source/data/unrelated changes.

## Workspace and next command

Worktree `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. Cargo cwd `rebuild/`. Original checkout is on dev; rebuild remains uncommitted/untracked. After reset read AGENTS→CLAUDE, personal rules/style, rebuild/AGENTS and this state/TODO/VERIFICATION. Approved spec, plan and September10audit are in worktree `docs/`, one above Cargo cwd. User authorization overrides issue/replanning/delegation/commit defaults. Do not recreate or complete the goal.

**Latest gate complete:** shell70569 exit0: `python3 test-results/task13-sent-reconciliation/run-gate.py`, all13 commands0, counts112 engine/19 protoCLI-daemon/21+26 Google/6operations/3reconciliation/3recovery/26+13IMAP/2releaseisolation, zero failed/ignored. Fresh production schema21 binaries verified; hashes in `test-results/task13-sent-reconciliation/artifacts.json`. `collect.py` exit0 verified all statuses and collected hashes/counts. All previous handles terminal.

**Latest completed path gate:** shell73675 exit0: `python3 test-results/task13-restore-paths/run-gate.py`, all7 commands0 (fmt/bothClippy,8 restore tests,3 Google recoveryE2E,13 fullIMAPE2E,2releaseisolation and freshproductionbuild). Counts/hashes collected and checked0 in that directory. No running shell/cell. Prior89732/44342/23380 terminal. Production schema21 daemon hash ab6361bb7d1ca551698c543688404fc44bea6c530e17aa92e11ac0f50192b117; CLI hash0018628d4f56a533203bc6ff906cc8b1bea6291ae07dba6c0d82329cdac9da40. These match current production source. Complete engine112 was at previousSentgate; newest focusedrestore8 includes2 newtests, not a fresh fullengine total.

Restore regression `tests/restore_path_races.rs` first failed101 because a replaced parent was activated. `store/restore/directory.rs::StagingDirectory` holds parent/stage handles, validates device/inode identity, renames relative to the retained parent with NOREPLACE, checks post-rename visibility, and disables TempDir cleanup for replaced paths. Six path scenarios plus replacement during keyput pass; normal cleanup remains tested. Replaced/moved owned stages are retained rather than deleting unrelated substitute directories. Firstcompile101 used Result equality against non-PartialEq StoreError; corrected to matches!, logretained. No schema/proto/dependency changes.

**Latest completed ownership gate:** shell55106 exit0: `python3 test-results/task13-restore-worker/run-gate.py`, all7 commands0 (fmt/bothClippy,17 lifecycle/maintenance/restore tests,3 Google recoveryE2E,13IMAPE2E,2releaseisolation and freshproduction). Counts/hashes collected0. No running shell/cell;46114/82383 terminal. Current daemon SHA2561fa76deee9552e6de1fefc6df198c56022a697b8b327c335fef3c7131470b99e, CLI0018628d4f56a533203bc6ff906cc8b1bea6291ae07dba6c0d82329cdac9da40. Latest artifacts.json is `test-results/task13-restore-worker/artifacts.json`; normal/feature binaries match current source.

`Engine._profile_lock` is now Arc<File>; restore_backup retains a clone inside its blocking closure through input cleanup. `restore_worker_ownership.rs` originally failed101 when another engine could open the source after caller cancellation/shutdown while restore keyput was blocked. Focused green1passed0.49s, gate17includes final test1passed0.40s. Source stays Locked until work finishes; both original and target then reopen with unchanged original keys. No runtime test hook/schema/proto/dependency change. Full engine112 belongs to previousSentgate; latest gates are focused counts, not a newly runfullsuite.

**Next:** implement process-death cleanup and remaining Task13 items. No full Task13 or goal completion. Crash cleanup needs durable ownership evidence for stage/newkey cleanup and must retain keys for a completed rename after a lost acknowledgement; do not scan sibling .restore-* directories and delete by prefix or trust copied manifests. Consider source encrypted-store restore jobs and startup reconciliation; no implementation decision/schema change yet. Static Engine::restore_profile is used only by engine tests; production API uses Engine::restore_backup. Read both paths before choosing a shared ownership strategy. Task14 security/resource/multipleengine suites and package/CI still missing as recorded below.

New Sent feature: `providers/imap/sent/client.rs::find_client` uses bounded unique current Message-ID search, durable floor/epoch and exact frozen Sent length/SHA256, followed by candidate recheck. Explicit manual reconciliation alone enables Accepted/Appending discovery. `AttemptOutcome::ObservedClientSent` commits validated client progress, exact-byte projection, existing sent_copy positive-read receipt and applied operation in one transaction; requires reconcile kind, active request and retained smtp_accepted. `append_receipt` validates before insertion. No new persisted discriminator/schema/dependency/proto/testcheckpoint.

Focused evidence: selected-system2passed16.07s (7scenarios:2 restoredAcceptedpolicies,5 non-restoredAppending exact/absent/duplicate/missingBcc/changedepoch); storage1passed0.48s (12proof/atomicitycases); actualCLI1passed7.74s (2restoredAcceptedpolicies, SIGKILL afterread beforecommit, exactPDF/raw/privateBcc, independentSMTP1/Sent1 and APPENDclient1/server0). Retained `task13-sent-reconciliation/focused-green` and storage/selected-*.log. Featurebinaries rebuilt0. Full13-command gate subsequently passed.

Failures retained: first two-step implementation persisted unknown client_sent_observed and made attempt reads KeyOrCorrupt (`receipt-format-failure`); replaced with atomic existing schema21 sent_copy format, keeping all independent proof/effect assertions. Prior fixtureBcc/address assumptions, intended missing-client-observation red, SQL integer compile issues retained. Latest additions had test-only undeclared uuid (replaced fixed UUID in per-case fresh profile), wrong expected epoch error (actualexisting imap_sent_epoch_changed), and wrong expected envelope order (preserve To thenBcc); respective directories retain failures. Storage fixture initially requestedmanual before finishing automaticreconcile; VersionConflict101 at line184, then modeled actual intermediateattempt beforemanual and passed. SandboxDocker101 retained in sandbox-docker-failure; narrow escalation approved. No mock validation/baselineassertion weakening.

**Next:** restore lifecycle/parent-race work. Read `store/restore.rs`, `profile/restore.rs`, `engine/recovery.rs`, testsrestore/restore_profile. Existing stage TempDir path and fresh parent open atactivation may follow a replacedparent and cleanup wrong replacement stage; write a focused independent filesystem regression. Existing normal failures clean keys; crashabandonment still lacks durable cleanup record. Need preserve activatedkeys on uncertainty and original/unrelatedfiles.

**Latest completed:** shell86997 six-command test-only gate all0: fmt/bothClippy, Google reconciliation3, IMAPsystem24, actualIMAP12; no failed/ignored. Production schema21 source/binaries remain the prior complete mutation-gate build. Previous99889/40053/25305/89876 terminal.

**Next:** restore lifecycle/path identity and process-death cleanup, then remaining full-goal work below.

## Current source and evidence

Schema21. Production source includes verified explicit client-Sent observation. Normal and feature binaries match the completed restore-path gate; latest production SHA256/size are in `test-results/task13-restore-worker/artifacts.json`. These are local binaries, not packages/installations/remoteCI/live compatibility.

- Complete `task13-reconciliation`14commands0: fmt/bothClippy, engine111, protoCLI-daemon19, Google21+26, operations6, reconciliation1, recovery2, migration1(42SIGKILLcases), IMAP22+11, releaseisolation2; zero failed/ignored. Migration cases0→1 through20→21 verify both commit boundaries, whole catalog/user rows, retained MIME/PDF/drafts/scoped queued requests, unchanged original/profile/keys and independent Google snapshot, two normal restarts each. Cases/hashes at migration-cases.json. Frozen history through20 unchanged; history21 appends only21.
- Complete subsequent `task13-reconciliation-mutations`13commands0: fmt/bothClippy, engine111, protoCLI-daemon19, Google21+26, operations6, reconciliation3, recovery3, IMAP22+11, release2; zero failed/ignored. Schema unchanged, no migration rerun. New system loops12 Gmail and16 Calendar restored mutation scenarios. ActualCLI adds3 Gmail/Calendar crash cases and Calendar abandonment preserves notification uncertainty/history. Source retry fix included in fresh production binaries.
- New test-only `task13-imap-reconciliation/focused-results.json`: targeted system2 passed66.57s,0failed/ignored,22filtered. Eight read/star on/off scenarios plus20 copy/nativeMOVE/fallbackMOVE/Trash/restore cases: queued vs recordedCOPYUID backups, original incomplete vs completed aftersnapshot, Observe/ResumeSafe/restart/replay, exact remote bytes and counts, unrelated Deleted UID retained, account isolation. Initial flag-only1passed31.81s. Full current system24 nowpassed in the six-command gate.
- New actual `imap_e2e`12 passed61.61s, build/suite0; copied at `task13-imap-reconciliation/e2e-first-green`. New test loops2 restored CLI scenarios (flag/fallbackarchive): backup/reconnect, Observe, ResumeSafe, SIGKILL after remote acknowledgement before local receipt, restart/replay; exactlyone STORE/COPY/UIDEXPUNGE as appropriate, no SMTP/APPEND/unscoped expunge, exact EML, global attempts1–4 retained. This firstgreen preceded one extra post-download remote-state assertion; final gate12passed62.76s includes that assertion.

Current test locations: `imap_write_system/recovery.rs`, `support/imap_reconciliation_e2e.rs`, roots imap_system/imap_e2e. Shared `support/backup_system.rs` extracted from reconciliation_system for authenticated streamed backup/hash/frame checks; shared `support/imap_effects.rs` independently reads real mock-server mailbox content, write counters and delivery ledger. Both helper extractions are test-only; Google reconciliation3 passed in the complete gate.

## Reconciliation implementation

`store/operation_reconciliation.rs`, schema21 reconciliation_schema.sql; Engine::reconcile_operation, Operations.ReconcileOperation proto/daemon; CLI `operation reconcile --account ID --operation ID --request-id UUID --version N [--resume-safe] [--wait]`. Durable scoped admission/replay; sameUUID changed payload conflicts. Latest request metadata includes expected version/mode/time/firstattempt/active. A second restore retains old requests inactive; newversion/newUUID needed. Original operation/write request identity is unchanged. Request and lifetime attempt admission capped1000.

Observe prevents remote mutation; ResumeSafe allows only provider-specific safe continuation. Restoration provenance forbids queued SMTP DATA, fresh COPY, and recreation of absent restored CalendarCreate. Positive Gmail Sent observation can resolve original; absence holds. IMAP retainedCOPYUID can resolve copy/nativeMOVE or safely finish source deletion with explicitResumeSafe. Calendar ETag checks remain conditional; event state doesnot prove invitation delivery. Abandonment retains uncertainty, error and history.

Fixed defect: eight separate explicit observations previously exhausted lifetime retry allowance. ExecutionPolicy::retry_ordinal now uses active request.first_attempt_ordinal with checked arithmetic; all SMTP/IMAP storage/progress/receipt writes retain global ordinals. Ten observations plus newResumeSafe retain12attempts. Red at task13-reconciliation-mutations/retry-limit-red; no assertion/mock weakening. Earlier Unknown HTTP2 lost-response bug now reports request_outcome_unknown/CLIexit4, inspect/replay identical request. Actual lost-admission-ack+SIGKILL send test proves original resolves with exactlyone independent send. Feature-only admission/migration markers excluded from production.

## Sent proof boundaries

A Sent copy never proves SMTP acceptance. RestoredPrepared and Started are held; automatic unknownAPPEND remainsheld until explicitmanual observation. Client observation requires exact frozen Sent bytes includingBcc, retainedpreDATAfloor, currentepoch and unique candidate set. Missing/ambiguous/mismatched observations never authorize DATA/APPEND. Server comparison retains its existing fingerprint semantics and separate readreceipt. Atomic clientpublication uses schema21 sent_copy; no migration necessary.

## Remaining full scope

Task13: positive clientSent/restored acceptedSMTP; historical durable payloads for other write kinds (rich send histories10–21 already covered); abandoned staging/new-keystore cleanup after processdeath; activation parent races; storage/WAL/exporterrors; disconnect retention and sameidentity reconnect. Spec excludes destructive account purge. Preserve cache/drafts/history and remove credentials on disconnect.

Task14: remaining security/parser/resource/multiple-engine suites and earlier identified defects: SMTP post-submission storage failure; blocking-task/permit ownership and worker-unwind crash investigation; IMAP literal/delimiter/case/old4096-capacity issues; aggregate attachments; legacy intents; calendar inheritance/Trash origins. CalendarList writerWithoutPrivateAccess is an official role the mock currentlyrejects; only owner/writer write semantics are verified. Keep compatibility limit explicit until implemented/tested.

Tasks03/15: explicit test egress denial (Docker bridge isnot firewall); external descriptor-generated client; API/CLI contract docs; security/dependency checks; full current local packages and CI files. Task16: final R01–R16 evidence matrix/operating/recovery/limits/risks; concrete MANUAL-ACCEPTANCE worksheet exists, all live rows deferred/unapproved/unverified. Missing live access isnot a blocker while offline work remains. No R01–R16 marked fully complete; do not finish at this milestone.

Older detailed evidence/risks retained in VERIFICATION and docs/history, including latest `2026-09-11-reconciliation-and-imap-tests-checkpoint.md`. Personal second-brain project/daily were appended after the verified Google reconciliation/mutation gates, before newIMAPtests; no git actions. NewIMAP verified outcomes were appended to the same project/daily notes after the gate using /tmp/nuncio-imap-recovery-memory.py, no git actions.

Second-brain Sent outcome append completed via /tmp/nuncio-sent-recovery-memory.py (initial heading assertion exited1 before writes; corrected to existing heading, rerun0). No git actions. Restore-path work not yet saved there.

## Crash cleanup investigation — next concrete work

Only investigation is done; no schema22/journal implementation exists. Production restore is Engine::restore_backup → a blocking closure with owned BackupInput/admission permit → engine/recovery.rs::restore → RestoreIdentity/stage_restore/activate. Static Engine::restore_profile is used only by integration tests; decide a shared ownership strategy before editing. Startup Engine::open_inner prepares profile lock, opens Store, constructs Maintenance, recovers draft uploads/operations, then Accounts credential cleanup. Engine._profile_lock was File; the current ownership fix changes it to Arc<File> retained by restore work, while shutdown still closes Store. A detached blocking restore may outlive its caller, so cleanup must not race active work after Engine shutdown/reopen.

Proposed direction to test: source encrypted-store restore-job records hold generated new profile ID, stage/target names, parent/stage file identities and phase before any new key is persisted. On startup, valid activated target identity retains keys; proven abandoned owned stage rolls back only new keys and removes owned files; changed paths or uncertain activation retain evidence/keys with a visible cleanup condition. Failed cleanup must be retryable. Do not scan sibling .restore-* prefixes or trust copied manifests to delete keys. If a new table is used, schema22/frozen history22/migration preservation tests and proper API/status evidence will be required. Revisit the static test-only restore entry point to avoid duplicate unsafe lifecycle behavior. Need actual daemon SIGKILL checkpoints before/after key puts and rename, independent original/restored profile/key/file evidence, and cancelled-caller/active-worker ownership checks. Avoid implementation until the first crash/ownership regression is in place.

Additional cleanup design constraints: BackupInput owns the upload TempDir and admission permit, but arbitrary .maintenance/.backup artifacts also survive process death. If extending automatic cleanup to those, their active owners need a retained profile lease too (not just restore's current clone). A durable encrypted source-store ledger is preferable to unsigned prefix/manifest scanning. Generated deterministic TempDir names can be reserved in a journal before creation using tempfile Builder with zero random suffix and a UUID prefix; creation remains exclusive. Save actual directory identity before any payload/newkey writes. If identity/activation cannot be established, preserve uncertain evidence rather than deleting unrelated data. Normal backups/restores must not carry cleanup authority for the original profile; sanitize any new ledger in exported/restored copies. Static Engine::restore_profile and stage_restore remain lower-level test-facing entry points; don't silently give them different recovery semantics without documenting/refactoring their ownership boundary. No final design or journal code is committed yet.

The production code compiled/tested is current. Next concrete implementation work is a failing restore crash-cleanup test at a durable boundary, followed by journal/state/startup recovery and actual subprocess coverage. All providers remain mocked; no live prerequisite is currently requested.
