# Rebuild session state

## Server-Sent schema18 work in progress (newer than all gates below)

Previous goal turn was verified progress, not a wait/blocker. Current turn implements server-managed Sent. `SentFingerprint` added under domain/submission/sent_fingerprint.rs: version1 SHA256 of selected parsed identity headers and exact raw body; optional Bcc omission allowed but unexpected/different Bcc rejected. Duplicate selected identity headers rejected, trace headers ignored, 1MiB header/512-header/64MiB MIME bounds. mail-parser0.11.9 serde feature enabled in Cargo.toml and Cargo.lock via offline Cargo. New unit tests initially compile101 missing type, then2passed0; empty Subject test failed101 and was fixed (empty optional header is valid).

Schema18 migration smtp_floor_schema.sql adds nullable sent_floor; no guessed backfill. start_smtp_data now takes a validated observed ImapMailboxState and commits current UIDNEXT before content. Server path refreshes that observation after SMTP envelope/DATA354 negotiation. Intent optionally stores versioned fingerprint (only server policy; old intents deserialize absent). Client APPEND mapping additionally respects a present fresh floor. Typed record_server_sent requires known accepted SMTP phase, exact account/mailbox/epoch, UID>=fresh floor and matching content fingerprint; durable server_sent_observed receipt precedes atomic Sent publication. Generic final Sent application independently checks raw content again. Existing schema17 client records remain compatible; old server rows lacking fingerprint/floor remain uncertain.

`providers/imap/sent/server.rs`: bounded UID SEARCH by 4096 message-sequence windows (max1million messages) intersected with captured UID floor and selected UIDNEXT, exact candidate metadata/body fetch; >1 matching Message-ID placement ambiguous, zero/content mismatch unconfirmed; recheck candidate set after body read. Worker calls it only after known SMTP acceptance, never to infer unknown delivery. New checkpoint operation_after_server_sent. First real `server_managed_sent` system rerun exit0,1passed/15filtered,3.72s; independently one server copy, SMTPacceptance1 and clientAPPEND0. This predates subsequent fresh-floor refresh/capture-only-server refinements, so rerun targeted system later. Feature workspace Clippy failed101 large enum after fingerprint growth; boxed PreparedWrite SMTP intent rather than suppressing lint.

**Immediate: poll shell22302** for `cargo test --locked -p nuncio-engine --test smtp_server_operation --test smtp_operation --test sent_fingerprint -- --nocapture`. New server storage regression exercises pre-accept prohibition, pre-DATA floor excluding older same-content UID, server-policy no APPEND, bad content rollback, proof/floor persistence through reopen and separate receipts. Then implement actual server-Sent subprocess success/crash/duplicate/lost-ACK cases and separate negative system scenarios; current new search still needs strict bounded protocol tests. No full schema18 gate yet. Existing focused client fixture remains unchanged except shared configuration helper; seven client SMTP crash cases passed at schema17.

Updated September 11, 2026. **Active unbudgeted goal: all16 tasks/R01–R16.** User requires full local Google and Synology mocks now. Live acceptance is deferred/unverified, not a current blocker. Work inline with no delegation, commits, push, merge, publication, installation into normal environment, remote changes or live-account actions. Preserve original source/data/unrelated work. Native apps excluded. Continue beyond milestones.

## Workspace and authority

Worktree `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. Cargo cwd `rebuild/`. Original checkout remains dev; rebuild files are untracked/uncommitted. After reset read root AGENTS→CLAUDE, rebuild/AGENTS, personal rules/style and this state. Approved spec/plan/audit are in worktree `docs/`, one level above Cargo cwd. User approval overrides issue/replanning/commit/delegation defaults; do not recreate the goal.

Historical implementation/evidence maps are retained in `docs/history/`, especially `2026-09-11-smtp-client-session-state.md`, `2026-09-11-through-imap-transfers.md`, `2026-09-11-transfer-groundwork.md`, `2026-09-11-mailplus-through-account-transport.md`, `2026-09-11-through-calendar-writes.md`. Their next-action prose is superseded here. Skills used: execution/worktree/TDD/debug/verification, architecture/docs, secondbrain-save. No agents spawned. Do not use stale audit agents. Secondbrain project/daily/index merged through this SMTP checkpoint with /tmp/nuncio-smtp-memory.py (scoped escalation0, no secrets/git).

## Immediate next action

**Implement server-managed Sent; its system regression is red.** `imap_write_system/smtp.rs::server_managed_sent_is_observed_without_client_append` uses the existing independent AutoSent mock. It first proves completed server copy1, SMTPacceptance1 and client APPENDrequests0, then requires applied/local Sent evidence. Focused run `cargo test --locked -p nuncio-test-support --features nuncio-engine/test-harness --test imap_system server_managed_sent -- --nocapture` exit101,14.39s: uncertain/smtp_server_sent_unconfirmed, profile `nuncio-system-8nWGXf`. Earlier attempted refactor failed before writing the file; the resulting filtered command ran0 tests and supplies NO evidence. The corrected new test actually ran1 and exposed the missing behavior. Client fixture now shares `submission(false)`; client-only rerun exit0,1pass/15filtered,0ignored,2.42s. No process running.

APPEND rejection recovery is now verified: `test-results/task12-smtp-rejection/results.json` all6checks0: format, normal/featureClippy, engine lib+smtp13tests, full IMAPsystem15 and actualIMAPE2E5 (seven SMTP crash boundaries within the SMTP test). Copied nested logs/results/counts retained. This gate predates the new server-Sent test. Matching tagged NO with no positive mapping records imap_append_rejected and resets only Appending→Accepted; lost/malformed/conflicting responses never authorize repetition. Complete negative DATA4xx/5xx and rejected-copy guards persist across reopen. Source/test schema17; no running process from that gate.

Server-Sent implementation remains in `operations/smtp.rs` as explicit uncertainty. Do not weaken the new red or remove prior client byte/remote-count checks. Primary reference for completed rejection: RFC3501 6.3.11 (https://www.rfc-editor.org/rfc/rfc3501.html).

Implement configured server-Sent policy using independent AutoSent service (already implemented/contract-tested) and positive unique observed placement, not client APPEND. Freeze a fresh pre-DATA Sent UIDNEXT observation floor with dispatch progress; the current enqueue-time floor is insufficient to exclude messages that appeared before dispatch. UIDNEXT is a lower bound, not a promised exact next UID (RFC9051 2.3.1.1, https://www.rfc-editor.org/rfc/rfc9051.html). Known SMTP acceptance stays separate from server copy observation. Missing/duplicate evidence stays uncertain. Require actual server-Sent and crash/E2E tests. Do not route server-Sent through client APPEND, assume Message-ID/MIME alone identifies a new request, or claim live compatibility.

Then finish SMTP-specific operation resolution (currently Google-only proof/resend paths), remaining Task12 capability/adversarial profiles, Task13 backup/restore/export/repair, Task14 security/resource audit, Task15 production packages/contracts/CI egress denial, Task16 acceptance worksheet/matrix. No R01–R16 is yet marked fully complete.

## Latest verified checkpoint

`test-results/task12-smtp-client/results.json`: latest results for all9 checks exit0. Rustfmt; normal/feature workspace Clippy; proto-cli4; engine74; IMAPsystem15; actualIMAPE2E5; Googlesystem21; actualGoogleE2E26. Zero failed/ignored. Counts overlap prior gates. Nested suite logs/results and `counts.json` copied into that directory. First gate failed Google system solely on stale schema16 assertion (actual17); preserved `results-at-first-failure.json`, `google-system-first-failure.log`, `google-system-suite-first-failure.log`. Updated explicit schema assertion and resumed only affected/remaining suites via `resume-gate.py`, preserving prior passing checks.

Other real failures retained in VERIFICATION: unused SMTP methods until production worker consumed them; sandbox-denied localhost unit binds, fixed by scoped escalation; Clippy item order; Bcc header angle-bracket assertion corrected to parsed exact one-recipient semantics; plain-text drafts without trailing newline rejected SMTP, fixed by freezing final CRLF before enqueue (focused red→green). No assertions weakened and no suppression added.

Current feature binaries: `target/test-harness/debug/{nunciod,nuncio-cli}`, schema17. Production binaries remain Task11/schema12 at `target/production/release/{nunciod,nuncio-cli}` and must be rebuilt/verified for final artifacts. No package/install/signing/live-provider/remote-CI claim. Existing independent mocks unchanged since last conformance gates.

## SMTP implementation and invariants

- `domain/submission.rs::freeze`: immutable MIME, account/sender/recipient snapshot; final SMTP CRLF; wire strips Bcc while optional private Sent blob retains it. Recipient arrays and binary attachments survive composition. `store/operations.rs` captures SMTP intent with send blobs; requires discovered Sent folder, no guessed folder creation.
- Schema17 `store/smtp_schema.sql`, `store/smtp{.rs,/progress.rs,/apply.rs}`: immutable SMTP endpoint/principal, Sent policy, account/local mailbox UUID/name/UIDVALIDITY/enqueue UIDNEXT. Steps Prepared→Started→Accepted→Appending→Copied. Start DATA marker commits before content. Only reconciled Prepared can RetryUnstartedSmtp. Started without acceptance stays unknown. Complete typed 4xx DATA rejection resets Prepared; 5xx fails without retry. Generic Rejected cannot reset started/accepted SMTP.
- `operation_attempts.rs`: SMTP acceptance receipt commits before APPEND. Appending without durable APPENDUID cannot repeat. `record_sent_append` validates exact account/mailbox/epoch/floor and stores mapping plus imap_append receipt. Generic sent_copy receipt no longer completes send. Typed AppliedSentCopy requires frozen MIME hash, persisted mapping, current folder epoch and positive observation; mail/FTS/attachments/placement and final receipt publish atomically. No global sync-coverage advance.
- `providers/imap/smtp{.rs,/submission.rs}`: required TLS/auth, bounded responses; Session→Prepared typestate handles envelope/RCPT/DATA354 without body; worker commits marker then transmit. Canonical CRLF, 1000-byte line limit, SMTPUTF8 including nested MIME headers, 8BITMIME for body, bounded64KiB dot-stuffing. Missing/malformed final reply uncertain. Partial RCPT rejection closes without any DATA bytes. Parsing/memory lifetime audit still pending.
- `providers/imap/sent.rs`: bounded literal APPEND, strict tag/status/APPENDUID single UID mapping, preserves valid early proof on EOF; rejects wrong/conflicting maps. Uses positive BODY.PEEK read to verify exact client Sent bytes. `accounts/imap.rs::smtp_session` retrieves private credentials and checks captured SMTP endpoint/current IMAP principal; auth failures pause account. `operations/smtp.rs` owns progress-aware execution; per-account lane/global bounded concurrency inherited from worker.
- Feature checkpoints `operation_after_smtp_start`, `operation_after_smtp_acceptance`, `operation_after_sent_append_start`, `operation_after_sent_append`, plus generic after_attempt/before_receipt. Actual `support/smtp_e2e.rs` tests six process-death boundaries, second restart/request replay, independent SMTP/APPEND counts, exact download, Bcc envelope/header distinction, stable Message-ID and binary attachment fidelity.
- Server Sent currently returns explicit uncertainty; not implemented yet. SMTP provider-specific manual resolution also remains. Existing schema<17 IMAP queued sends lacking intent are excluded from readiness rather than guessed; upgrade/recovery handling needs audit.

## Existing IMAP / mocks / later risks

Schema14 full/delta/fetch and flags; schema15 COPY/MOVE/Archive with durable COPYUID, native MOVE or COPY+UID-scoped deletion/expunge; schema16 Trash origin metadata survives full resync. No broad EXPUNGE. Exact local mailbox/account/UIDVALIDITY/UID identity; different identical-MIME placements remain different messages. See history for full maps. Barrier arm helper must clear old entered/release files before reuse; never recreate the earlier stale-marker race.

Independent `tests/imap` Python controls real pinned Dovecot2.4.5/Mailpit1.31.1, synthetic alpha/beta/observer, local CA, owned Docker and strict fault controls. Rust helper `MockMailPlus::start`, `start_with_sent_policy`, `credential`, `control`, `shutdown`. Controls include snapshot, mailbox/raw, smtp/smtp_raw, inject/wait_fault/release, folder operations, UID/flags, wait_server_sent/server_sent_status. Lowercase protocol counters: `smtp DATA`, `imap APPEND`. Mailpit adds trace/Bcc headers; exact submitted raw is the captured suffix, independently verified by wire size/hash. Do not generalize this mock transform to live providers.

Last independent MailPlus gate wraps18 Python contracts in2 Rust tests. Google mock25/system21/E2E26 verified earlier. Normal Docker bridge is not an egress firewall; explicit CI denial is pending. Other known audit items: worker-unwind SIGSEGV root cause unproven; post-submission storage failure recovery; parser/blocking cancellation and memory permits; HTTP2/reconcile retry ownership; UID literal/delimiter/lowercase NIL profiles; 4096 mailbox bound includes retired rows; aggregate attachment limits; valid PDF fixture; schema upgrade fixtures; descriptor/external-client validation; Calendar exception/series inheritance gap; Trash-origin orphan retention/repair/backup. See TODO/history, not a completion claim.
