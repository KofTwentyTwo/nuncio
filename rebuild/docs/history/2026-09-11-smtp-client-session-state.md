# Rebuild session state

## SMTP schema17 work in progress

Client SMTP is now dispatched through the engine/API worker. Schema17 captures SMTP endpoint/principal, Sent policy and local mailbox/UIDVALIDITY/UIDNEXT with frozen send intent. Durable phases prepared→started→accepted→appending→copied enforce no repeat DATA after uncertainty/acceptance and no repeat APPEND without proof. Complete negative DATA replies have a typed reset; generic rejection cannot reset started/accepted SMTP. Arbitrary sent_copy receipts can no longer mark a send applied; exact frozen Sent bytes, placement proof and positive mailbox observation publish atomically. Server Sent policy still returns visible uncertainty and requires implementation.

New locations: store/smtp{.rs,/progress.rs,/apply.rs}, smtp_schema.sql; providers/imap/{smtp/submission.rs,sent.rs}; accounts/imap.rs::smtp_session; operations/smtp.rs and worker readiness. SMTP validates envelope/CRLF/extensions and dot stuffing; strict APPEND collector preserves validated single UID/epoch proof, checks tags and bounds. Configured Sent folder must already be discovered. Production artifacts are still old/schema12; feature artifacts are being rebuilt.

Commands: focused storage compile initially101 due new unconsumed SMTP methods (WIP dead_code); those methods are now consumed with no suppressions. Unit run without escalation101 due denied localhost binds (1pass2fail); scoped localhost rerun `cargo test --locked -p nuncio-engine --lib providers::imap -- --nocapture` exit0,11pass/1filtered,0ignored. Updated `cargo test --locked -p nuncio-engine --test smtp_operation -- --nocapture` exit0,1pass, strengthening DATA marker, independent delivery/copy, foreign placement, byte mismatch rollback and reopen invariants. Focused full API `... --test imap_system smtp_submission -- --nocapture` exit0,1pass/13filtered,0ignored; independent Mailpit delivery and Dovecot Sent exact bytes/counts. Engine Clippy initially101 items_after_test_module; moved open_smtp before tests; rerun exit0.

Currently actual `python3 scripts/verify.py --suite imap_e2e` running shell22349, fresh feature build and six SMTP process-death boundaries with Bcc/binary MIME checks. Added system lost DATA/APPEND ACK and explicit pre-DATA rejection regression, not yet run. Storage negative DATA transitions and server-Sent support still need focused coverage. No full-current gate claim.

Updated September 11, 2026. **Active unbudgeted goal: all16tasks/R01–R16.** User requires full local Google and Synology mocks for now; live acceptance is deferred/unverified, not a current blocker. Work inline, no delegation. No commits, push, merge, publication, installation into normal environment, remote changes or live-account actions. Preserve original source/data/unrelated work; native apps excluded. Continue beyond milestones.

## Workspace and authority

- Worktree `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. Cargo cwd `rebuild/`. Original checkout remains dev. Rebuild remains untracked/uncommitted.
- Approved documents from rebuild cwd: `../docs/superpowers/{specs,plans}/2026-09-10-google-first-rebuild.md`, `../docs/reviews/2026-09-10-nuncio-audit-and-rebuild.md`, GOAL/PLAN-nuncio-rebuild. Read root AGENTS→CLAUDE, rebuild/AGENTS, personal rules/style and this state after reset. Existing approval overrides issue/replanning/commit/delegation defaults. Do not recreate the active goal.
- Historical implementation maps and evidence: `history/2026-09-11-through-imap-transfers.md`, `2026-09-11-transfer-groundwork.md`, `2026-09-11-mailplus-through-account-transport.md`, `2026-09-11-through-calendar-writes.md`. Historical next-action prose is superseded by this file. VERIFICATION retains command/failure chronology.
- Skills used: execution/worktree/TDD/debug/verification, architecture/docs, secondbrain-save. No agents spawned; ignore stale audit agents. Secondbrain project/daily updated through verified schema16 gate with `/tmp/nuncio-trash-memory.py`, scoped escalation0; no secrets/git.

## Immediate next action

SMTP work in progress, newer than schema16 gate: added full system regression `imap_write_system/smtp.rs` (client-Sent policy). First compile101 unavailable hex helper; replaced with standard hash formatting. Focused rerun101 reaches durable queued send and times out because IMAP send dispatch is disabled. Test independently requires SMTP DATA count/envelope/wire hash, actual Mailpit capture, Dovecot Sent bytes/count, local Sent projection and separate smtp_accepted/sent_copy receipts. Do not weaken it or claim all-current-tests-green.

New bounded SMTP component `providers/imap/smtp/submission.rs` with typestate Session→Prepared: envelope/RCPT/DATA354 first without content, caller must commit a dispatch marker, then dot-stuffed bounded64KiB DATA write and complete final2xx/4xx/5xx classification; missing/malformed ACK unknown. Canonical CRLF/1000-byte lines, payload bounds, SMTPUTF8 checks for every parsed MIME header including nested messages, 8BITMIME for body. Existing auth now returns a Session internally and probe still reports capability tuple without sending. New submission_tests initially compile101 missing types, implementation unit run pending exec16098. Production caller/journal integration not yet written; normal build may report unused submission methods until wired. Full product SMTP system remains intentionally red.

Next immediately: poll16098/fix component failures; implement schema17 immutable SMTP/Sent intent + durable phases and worker consumer. Preserve separate SMTP accepted and APPEND proof. Prepared(before DATA bytes) can retry after crash; Started without SMTP receipt stays unknown; accepted SMTP never repeats for Sent failure; SentStarted without APPEND proof never blindly repeats; known APPENDUID permits positive target observation/publication. Add negative-reply evidence to reset only proven unaccepted SMTP, and independently test those invariants. Server AutoSent and broader E2E remain required, not waived.

Primary references read September11: RFC5321 sections4.2.5/4.5.2 (2xx after terminator accepts responsibility;4xx/5xx prohibit subsequent delivery; add one dot at each line start), RFC6531 section3.2 (SMTPUTF8 required for internationalized envelope or headers at any MIME depth), RFC4315 APPENDUID/UIDNOTSTICKY. URLs https://www.rfc-editor.org/rfc/rfc5321.html , https://www.rfc-editor.org/rfc/rfc6531.html , https://www.rfc-editor.org/rfc/rfc4315.html . Missing APPENDUID is not acceptance proof of identity; no guessed repeated copies.


**SMTP production submission and independent Sent-copy bookkeeping. No running process.** Schema16 IMAP mail actions are offline-verified: read/star/archive/move/copy/trash/restore, strict account/placement identity and remote-effect recovery. Full local mocks exist for Google and MailPlus. Native Google send/calendar writes are already offline-verified; do not restart planning or stop at this milestone.

Start a separate IMAP/SMTP system regression through draft creation/send with independent Mailpit delivery envelope/raw observations and Dovecot Sent count. Establish unsupported send as red, then implement captured SMTP intent and separate durable SMTP/Sent phases. Account SMTP probe is `providers/imap/smtp.rs`: bounded framing before lettre Response parsing, required TLS + PLAIN/LOGIN. It currently drops the authenticated Wire; a reusable authenticated session will be needed. Do not replace bounded framing with lettre's unbounded read_line connection collector. `accounts/imap.rs::imap_session` is the credential/identity/auth-state pattern; SMTP needs equivalent private credential access. Existing `store/operations.rs::enqueue_send/send_payload`, frozen wire/sent blobs and operation worker can be extended. Google send preparation/dispatch is still the default and must remain intact; IMAP send readiness is intentionally disabled until implemented.

Accepted SMTP must never be resent because APPEND failed. Unknown DATA/APPEND requires positive evidence or visible uncertainty; no blind retries. Capture Sent policy/folder identity with immutable send intent. Test server-auto-Sent and client-APPEND separately, including duplicate Message-IDs, recipient rejection, transient failures, disconnect/withhold after acceptance, process death before/after receipts, unknown APPEND and exactly-once remote effects where provable. Independent mocks already support SMTP DATA and IMAP APPEND fault controls. Use actual daemon/CLI subprocesses in addition to separate system tests. Verify SMTP line canonicalization/dot stuffing/SMTPUTF8/8BITMIME against primary protocol documentation and independent server acceptance before claiming support.

After SMTP: Task13 backup/restore/export/repair, Task14 adversarial/security/resource audits, Task15 production packages/contracts/CI egress denial, Task16 acceptance worksheet and full matrix. Live accounts require later named authorization; no account setup is currently requested. Goal remains incomplete.

## Latest verification

Nine-command Trash/restore gate complete: `test-results/task12-trash/results.json` all9commands0. Rustfmt, normal and feature workspaceClippy, proto-cli4, engine69, IMAPsystem13, actualIMAPE2E4, Googlesystem21, actualGoogleE2E26. Zero failed/ignored. Fresh feature builds and copied nested suite logs/results retained. Native and hidden-MOVE fallback Trash/restore both pass, including before-publication SIGKILL, no-op and full-resync origin retention. Source/test schema16; production artifacts still Task11/schema12. No process running; no live compatibility or remote CI claim. SMTP is next.

Prior gates (counts overlap, never sum as independent coverage):

- `task12-folders/results.json`:9commands0, engine69/proto-cli4/IMAPsystem11/E2E3/Google21+26, schema15 explicit Move/Copy.
- `task12-archive/results.json`:8commands0, engine69/IMAPsystem10/E2E2/Google21+26, schema15 native/fallback/no-op/unknown-copy crash recovery.
- `task12-move-mock/results.json`:5commands0; independent Rust2 wraps18 Python contracts and owned cleanup0; Pythonlint/format/Rustfmt/featureClippy. Mock source unchanged since this gate.
- `task12-flags/results.json`:8commands0, engine67/IMAPsystem8/E2E1/Google21+26, schema14.
- `task12-fetch/results.json`:7commands0; `task12-reads-recheck/results.json`:11commands0. Task11 full12commands0, normal142/feature142 overlap; independent Google25/system21/actualE2E26/operation6/release1. Production last rebuilt at Task11/schema12.

Current test executables: `target/test-harness/debug/{nunciod,nuncio-cli}` schema16. Production development executables `target/production/release/{nunciod,nuncio-cli}` are Task11/schema12 and predate IMAP commands; no final packaging/install/signing. Never imply local gates prove remote CI or live compatibility.

## Transfer implementation map and invariants

- Domain `domain/mail_change.rs` and `store/imap_changes.rs`: immutable read/star flag states or typed transfer payload. `imap_transfers.rs` captures source account/local mailbox/UIDVALIDITY/UID and active destination collection UUID/name/epoch. Destination account checks and strict canonical IDs. Proto `mail.proto` additive oneof fields9/10 + MailFolderChange; CLI version1 move/copy action with destination_collection_id; Gmail rejects folder actions. Existing Trash(bool) covers restore.
- Schema15 `imap_transfer_schema.sql`, `imap_transfers/progress.rs`: Prepared is no row; Started is durably recorded before COPY/MOVE; validated COPYUID saved with imap_copy receipt; phases Copied/DeletingSource/ExpungingSource/SourceRemoved. Generic Repeatable and post-start Rejected are forbidden. Only Reconcile+Prepared may RetryUnstartedImapTransfer. Known copy proof resumes remaining substeps, never copy again. Missing proof stays imap_copy_identity_unknown across restarts. Duplicate MIME cannot identify a request's copy.
- `providers/imap/transfers.rs`: strict tagged completion and exact single-UID COPYUID mapping, including early untagged proof before lost final ACK. Valid partial mapping retained for unavailable/EOF; malformed/wrong-tag/conflicting mapping rejected. Native MOVE, otherwise COPY+UIDPLUS-scoped source deletion. No broad EXPUNGE. Fallback without UIDPLUS rejects before COPY. Positive target observation before separate source deletion; source epoch checked; exact target metadata/MIME fetched within bounds.
- `operations/imap_transfer.rs`: progress-aware execution, finite retries/deadlines and positive reads. `operations.rs` owns account/scope lane and composition permit. Feature-only checkpoint after durable copy receipt `operation_after_imap_copy`; generic before-dispatch/after-attempt/before-receipt remain. Same-folder Move is positive-read no-op with no new placement.
- `imap_transfers/apply.rs`, `mail_projection.rs`: validate exact persisted copy proof and terminal required phase; atomically publish destination encrypted raw/decoded/headers/attachments/FTS/membership, remove exact source on Move, append final receipt/state. No global sync-coverage advance. Existing attachment part IDs preserved on no-op/upsert.
- Schema16 `imap_trash_schema.sql`, `imap_transfers/origins.rs`: encrypted origin keyed by account+exact trashed provider placement, independent of reconstructible local message ID. Captured optional restore_origin defaults absent for schema15 JSON. Any transfer into configured Trash records original placement/name; copy within Trash inherits known origin. Metadata commits atomically with publication/receipt; completed Move removes source origin. Restore checks original active local folder identity/name/epoch and refuses unknown origin in Trash, retirement or epoch change. External Trash needs an explicit destination via Move. Already-Trash and already-outside-Trash restore no-ops preserve identity.
- Storage tests `engine/tests/imap_projection/transfers.rs` cover intent/proof/reopen/unsafe retry and atomic publication; strict COPYUID parser unit covers9 replies. System tests `imap_write_system/{mod,transfers,trash}.rs` cover flags/transfer native+fallback/no-op/account isolation/lost acknowledgements/unsafe extension profile/origin failure and copy inheritance. Actual tests `support/imap_{read,flags,transfer,folder,trash}_e2e.rs` independently inspect remote counts/raw/flags, SIGKILL/restart and local publication. Clear old entered/release markers before rearming a checkpoint (`imap_read_e2e::arm`); stale marker caused two real test failures and is fixed without weakening remote assertions.

## Existing read/mock foundation

SQLCipher WAL/FULL with dedicated bounded worker and encrypted blobs. IMAP full/delta/fetch use strict LIST/EXAMINE/UID SEARCH/FETCH BODY.PEEK, exact decoded folder identity (INBOX-only casefold), staged atomic promotion and explicit absent-UID handling. Metadata updates reuse immutable cached MIME; fetch one existing placement never advances global coverage. UID discovery pages message sequence positions4096 with captured UID intersection, ≤1million/245pages even sparse UID4billion; recheck epoch/NEXT/modseq/inventory before publication. Folder rename/retirement/UID reset replaces placements, preserving durable drafts and operations.

Critical parser rules: async-imap0.11.3 high-level collectors discard NO/BAD; always use strict command completion. imap-proto quoted strings retain escaping and ResponseData owner contains trailing bytes; LIST decoder uses parser-consumed prefix and preserves literal backslashes. ImapWire bounds framing64KiB/literal64MiB/total70MiB with zero literal auth budget,30s deadline. TLS chain/hostname validation remains on with optional CA. Wire logging compiled out.

Independent `tests/imap` stdlib Python controls real Dovecot2.4.5 and Mailpit1.31.1 pinned image digests documented in MOCK-MAILPLUS.md. Owned rootless services, local CA, synthetic alpha/beta/observer credentials, implicit/STARTTLS, numeric-loopback/private pipes, strict faults, capability profiles and EOF/TERM cleanup. Normal Docker bridge is not an egress firewall; Tasks03/15 explicit denial still pending. Mock exact capability strings and delayed truncated-FETCH fault behavior were independently fixed and verified. Server AutoSent observes real Mailpit captures/auth-user tags and APPENDs into Dovecot; unknown APPEND stops without blind retry. Mailpit adds known Bcc/trace headers; test exact additions plus original suffix instead of pretending raw capture equals submitted bytes. Google mock is independent stateful HTTP/OAuth with its own schemas/effect counters; separate Google system/subprocess suites stay green.

Use `UV_TOOL_DIR=$PWD/target/tool-cache/tools uv tool run --offline --cache-dir target/tool-cache --from ruff==0.16.7 ruff ...` for Python gates. Docker/socket/loopback checks use narrowly scoped escalation already authorized; no approval-review rejection. Avoid unquoted zsh globs for optional paths and avoid concurrent Cargo commands sharing a target directory.

## Remaining defects and scope

No R01–R16 is marked fully complete. Tasks01/02/04–08/10/11 verified offline; Task03 CI egress pending, Task09 full typed operation states and SMTP/resolution coverage remain; Task12 SMTP active;13–16 pending. Do not treat the mock or mail actions alone as completion.

Later audits: store-worker unwind SIGSEGV root cause unproven; post-submission store failures; bounded reconciliation CPU/HTTP2 retry ownership; MIME blocking cancellation/global memory permits; literal/quoted hierarchy delimiter and lowercase NIL;4096 mailbox lifetime includes retired catalog entries; aggregate attachment/upload bounds; valid renderable PDF fixtures (current synthetic markers prove bytes only); schema upgrade/downgrade fixtures; descriptor golden plus external-client semantic compatibility; Google existing exception/series inheritance gap noted in earlier state. Trash-origin orphan retention/repair consistency and backup inclusion need Task13/14 coverage. Follow existing test failure evidence; never erase originals or weaken assertions.
