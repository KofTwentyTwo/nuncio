# Rebuild session state

Updated September 11, 2026. The full unbudgeted Tasks01–16/R01–R16 goal has resumed and is active. Scope is unchanged. Continue inline; do not recreate the goal or start planning over. Full local Google and Synology mocks are required now. Live acceptance is deferred/unapproved/unverified, not a blocker while offline work remains. Native apps are excluded. No commits, push, merge, publication, normal-environment installation, remote settings, or live acceptance actions. Preserve original source/data/unrelated work. The executive report was separately emailed to james@kof22.com through the connected Gmail tool on explicit request; that was not a rebuild-provider acceptance test. The user asked about regular commits/pushes; recommendation given, authorization has not yet changed.

## Resumed work — startup and relative-path fixes under verification

The following failed-regression checkpoint is historical. Startup recovery now retains Arc<File> source-profile ownership inside its blocking cleanup closure; direct fixture recovery obtains an actual file lock. Focused journal suite passed4tests (shell82528/0, `task13-startup-cleanup/green-lock.log`). Strengthened relative-path test first failed at premature InvalidPath (shell82643/101, relative-strong-red.log); scoped canonical owner paths with identity validation now permit journal admission and startup cleanup. Profile lifecycle5tests passed (shell75532/0, relative-green.log), proving encrypted inspection, an actual pending cleanup row, row retirement and original keys/manifest preservation. Added successful one-component relative restore/independent-key test and owner-symlink preservation scenario; these await the new relevant gate. Rustfmt0. Source changed; previous production artifacts are not current until rebuilt. Next gate: `python3 test-results/task13-startup-cleanup/run-gate.py`, then `summarize.py` after checking its expected counts. Full Task13–16 scope remains.

## Immediate next action — startup cancellation, then remaining maintenance cleanup

**Latest state is a confirmed failing startup-cancellation regression, not a green current suite.** All known build/test handles are terminal. `test-results/task13-startup-cleanup/red-corrected-fixture.log` (shell98698, exit101) records 0 passed/1 failed: a second Engine reopened the source while the cancelled first startup's cleanup worker remained active. No production fix has been applied. The initial red (shell90964/101) also contained a fixture mutex-lifetime error; that fixture was corrected before confirming the product failure. The separate relative-path test (shell94618/0, 1 passed) is too weak to prove recovery: any restore error satisfies its assertion, possibly before journal admission.

**Last passing checkpoint:** seven-command maintenance-ownership gate shell25031 completed, all exit0. Full engine122, recoveryE2E4, IMAPE2E13, release-isolation2, fmt/bothClippy; zero failed/ignored, production rebuilt. Exact commands/logs/counts/source/artifact hashes: `test-results/task13-maintenance-ownership/`; runner `run-gate.py`, summary `summarize.py`. New test `maintenance_ownership.rs` first reproduced all3 cases (shell22551/101) and then passed with existing maintenance/restore tests (shell33689/0,9tests). No schema/proto/dependency change. Only new tests were added after this checkpoint; the existing production binaries therefore contain the newly demonstrated lifecycle defect.

MaintenanceLease in `maintenance.rs` contains the existing transfer permit and Arc<File> source-profile lock. It travels through Store backup creation/output and upload/append/finish/input/inspection. Engine constructs the shared lock before Maintenance; backup/output and upload/input retain it through cleanup. Another engine stays Locked after source Engine shutdown until the last retained object drops; original keys/profile identity remain unchanged.

**Next: fix the reproduced startup cancellation failure.** `engine.rs::open_inner` creates the shared profile lock only after awaiting `engine/recovery/journal.rs::recover`; the blocking cleanup closure retains no lock. Create the shared lock immediately after profile preparation and retain a clone inside cleanup through completion. Preserve the existing deterministic test `cancelled_startup_retains_profile_ownership_until_cleanup_worker_finishes` in journal_tests.rs; it must prove Locked during cleanup and a normal restart with unchanged original keys afterward. Then strengthen `profile_lifecycle.rs::single_component_relative_profile_recovers_failed_restore_without_changing_original_keys` to prove a pending journal row or successful restore. Tempfile produces absolute upload paths while the source may remain relative, so journal admission can fail before the intended recovery path. Preserve path-identity checks and avoid changing CWD globally in concurrent tests.

Then extend durable cleanup to earlier upload/export artifact lifetimes and safe ordinary-failure retirement. Source restore rows currently begin once restore staging starts; an upload/backup abandoned earlier is not recovered automatically. Maintain the new profile leases and admission across cancellation; use recorded identities and handle-relative cleanup, never filename-prefix deletion. Preserve empty/unrecognized directories when no durable ownership record exists. Static caller-owned restore helper and other remaining Tasks13–16 work below remain in scope. No external blocker prevents further offline work when the paused goal resumes.

## Workspace and immediate next action

Worktree: `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. Cargo cwd is its `rebuild/`. Original checkout remains on dev. Rebuild is uncommitted/untracked. After reset read AGENTS→CLAUDE, personal rules/style, rebuild/AGENTS, this file, TODO and newest VERIFICATION entries. Approved spec/plan and September10 audit are in worktree `docs/`, one above Cargo cwd. User authorization overrides issue/replanning/delegation/commit defaults.

**Schema22 restore-cleanup journal checkpoint is fully verified.** The original actual daemon/CLI crash test (four boundaries: stage, database key, API key, activation) passed after journaling/startup recovery, feature build0/test0, shell64141. An earlier sandbox socket denial exited101 before test startup; retained as journal-sandbox logs. Original abandoned-stage red and CLI lost-response red remain under `test-results/task13-restore-crash/`. Normal release artifacts are stale.

New code: store/restore_jobs{.rs,_schema.sql}; engine/recovery/journal.rs and journal_tests.rs. stage_restore_with records directory identities before exporting payload; activation intent is durable before keys, permitting detached work to finish after source Store shutdown. Startup checks source/parent/stage/target identities, retains activated target keys, deletes only owned files/new keys and clears rows. Backups and restores sanitize cleanup authority. Frozen history22 appends only migration22; historical/current schema expectations updated. Feature-only restore markers added to release-isolation source tests, not yet release-verified.

Safety regression shell86514 failed on keys changed for unexpected-stage-entry (2passed/1failed); fixed with bounded handle-relative preflight of both directories before keys. Focused shell6722 passed all3 safety tests (seven path/content scenarios, failed-key retry, activated retention, backup authority exclusion). Cleanup helper now lives in store/cleanup_directory.rs and is reused by explicit BackupInput cleanup. Added restore_stage_owned before-export marker and fifth actual crash case, source-directory fsync and explicit finish-error handling (closed store retains durable row for startup).

First12-command gate shell37710 stopped at engine101 after fmt/bothClippy0: restore.rs's constructed schema18 fixture retained the newly added22 table. Corrected the fixture by dropping it when downgrading; preserved failure logs as gate-fixture-failure-*. New restore_upload_cleanup.rs then reproduced success despite TempDir cleanup failure (shell38209/101). Production restore now explicitly removes the original owned upload through retained handles and requires successful cleanup before retiring the row; cleanup/fsync failure after activation reports activation uncertainty, retaining new keys and record. Stage/upload parent fsync now precedes side effects. Focused shell87827 passes5 tests:3restore +1cleanupfailure/retry +1detachedworker ownership. All earlier shell handles are terminal.

**The prior journal commands are terminal. Full12-command gate shell28944 completed with all exit0**: fmt/bothClippy, engine119, protoCLI/daemon20, migrationE2E1 (44 SIGKILL cases), recoveryE2E4 (five restore crash points), Google21/26, IMAP26/13 and production isolation2 with a fresh normal release build. Zero failed/ignored. Commands/logs/results/counts/source-hashes/artifact-hashes: `test-results/task13-restore-crash/`; runner `run-gate.py`, verifier `summarize.py`, per-migration evidence index `migration-case-index.json`. No full Task13/R01–R16 completion. No live, remoteCI, package, installation or git action claim.

## Current artifacts and verification

Source and verified production schema22. Normal production binaries: `target/production/release/nunciod` (16,319,344 bytes, SHA256 `f0bcf60ac3db98dd0e3d4361e7a2b01420b6d2eabc10b5d5aad15d5aae13f8dc`) and `nuncio-cli` (3,506,544 bytes, SHA256 `23f75c75837bc64b25a40cdac3d5a3e0abcff436bbf8d75d161664ac3659d4b1`). Feature binaries: `target/test-harness/debug/{nunciod,nuncio-cli}`. Both configurations are current. These are local binaries, not packages/installations, remote CI, or live compatibility evidence.

- Latest `task13-maintenance-ownership`: all7commands0, fullengine122, recoveryE2E4, IMAPE2E13 and release-isolation2. Three new lifecycle regressions. Schema22/proto/dependencies unchanged.
- Preceding `task13-restore-crash`: full12-command gate all0; exact results above. Frozen history22 appends only22, preserving1–21. New unit3/cleanupfailure1 and five actual restore-crash scenarios pass; all44 migration commit boundaries and two restarts per case pass.
- Preceding `test-results/task13-restore-worker/results.json`: all7 commands0 — fmt, both Clippy configurations,17 lifecycle/maintenance/restore tests,3 Google recovery E2E,13 IMAP E2E,2 release-isolation tests plus fresh production build. Zero failed/ignored. Exact commands/logs, counts.json and artifacts.json retained. The17 is a focused total, not a new full-engine run.
- Preceding `task13-restore-paths`: all7 commands0 — fmt/both Clippy,8 restore tests,3 Google recovery E2E,13 IMAP E2E,2 release isolation and fresh production. Path substitution regression first failed, then6 filesystem scenarios and key-put replacement passed. Source/artifacts from this gate are superseded by the worker gate.
- `task13-sent-reconciliation`: full13 commands0 — fmt/both Clippy, full engine112, proto/CLI/daemon19, Google system21/E2E26, operations6, reconciliation3, recovery E2E3, IMAP system26/E2E13, release isolation2; zero failed/ignored. This is the latest complete engine suite, before the subsequent path/worker tests. Focused storage12 cases/system7 scenarios/actualCLI2 crash scenarios are in focused-green and individual logs. Both later changes kept schema21/DDL/dependencies/proto unchanged.
- Earlier schema21 `task13-reconciliation` full14 commands0 includes actual daemon SIGKILL before/after every migration commit (42 cases, source schemas0–20). Two normal restarts per case, whole catalog/user rows, original encrypted bytes, EML/PDF/drafts/queued requests/keys and independent Google state remain correct. Frozen histories through20 unchanged; history21 adds only21.
- Earlier `task13-reconciliation-mutations` full13 commands0 includes28 restored Gmail/calendar system scenarios and3 actual CLI crash cases. `task13-imap-reconciliation` six commands0 includes28 restored IMAP system scenarios and2 CLI crash cases. Detailed commands/exit statuses and earlier failures remain in VERIFICATION and docs/history.

## Latest implemented changes

Client Sent observation: `providers/imap/sent/client.rs`, `operations/smtp.rs`, `store/operation_attempts.rs`, `store/smtp/apply.rs`. Explicit reconciliation requires retained SMTP acceptance, a durable pre-DATA UID floor, current mailbox epoch, one candidate, and exact frozen Sent MIME length/SHA256 including private Bcc. Candidate search is bounded and rechecked after reading. `ObservedClientSent` commits progress, projection, existing sent_copy positive-read receipt and completion atomically. Original smtp_accepted acknowledgement stays separate. Automatic unknown APPEND remains held until an explicit request. Missing/ambiguous/mismatched copies never authorize DATA/APPEND. Server fingerprint semantics and separate read receipt remain.

Tests: engine `smtp_client_observation.rs` (12 proof/rollback cases); system `imap_write_system/recovery_sent.rs` (both restored Accepted policies plus ordinary lost-APPEND exact/absent/duplicate/missing-Bcc/changed-epoch); subprocess `support/smtp_sent_reconciliation_e2e.rs` (both policies, restored old Accepted backup, SIGKILL after read/before local completion, restart/replay, exact PDF/raw and private Bcc, independently SMTP1/Sent1/clientAPPEND1 or serverAPPEND0). No false APPEND acknowledgement. First implementation used an unknown receipt discriminator; replaced it with existing schema21 format. append_receipt now validates before insertion. Failure logs preserve this and test fixture/compile/sandbox corrections; no mock validation or baseline assertions were weakened.

Restore path identity: `store/restore/directory.rs::StagingDirectory` retains parent/stage handles, validates device/inode identity, renames relative to the retained parent with NOREPLACE, checks post-rename visibility, and disables TempDir cleanup for replaced paths. Replaced/moved stages are retained rather than deleting unrelated directories. Tests `restore_path_races.rs` (6 activation/drop × parent/stage/symlink replacement cases) and `restore_profile.rs` (replacement during key creation; rolls back only new keys). Original red was unintended activation of substitute stage. Normal existing-target and partial-key cleanup still pass.

Detached restore ownership: Engine._profile_lock is Arc<File>; restore_backup retains a clone inside its blocking closure through completion/input cleanup. `restore_worker_ownership.rs` originally failed because cancelling the caller and shutting down Engine let another Engine open the source while restore keyput remained blocked. Fixed test verifies Locked until work finishes, then both profiles open with distinct identity/unchanged original keys. Deterministic test-only SecretStore barrier has RAII release; no added runtime hook. Red/green and full relevant gate retained in task13-restore-worker.

## Existing reconciliation contract

Schema21 `operation_reconciliation_requests`; `store/operation_reconciliation.rs`, Engine::reconcile_operation, Operations.ReconcileOperation, CLI `operation reconcile --account ID --operation ID --request-id UUID --version N [--resume-safe] [--wait]`. Durable scoped admission/replay; changing payload under the same UUID conflicts. A second restore keeps old requests inactive. Original operation/write request identity and global attempt ordinals remain. Requests and lifetime attempts are capped1000.

Observe never mutates the provider. ResumeSafe permits provider-specific proven continuation: desired-state Gmail/IMAP flags, conditional Calendar writes, retained COPYUID source removal. Restored Prepared SMTP, fresh IMAP copy, and missing restored CalendarCreate remain held. Positive Gmail Sent can resolve a send; absence cannot prove no delivery. Calendar state never proves invitation delivery; uncertain notifications survive abandonment/history. Per-request retry ordinal uses the request's first global ordinal, so repeated observations do not exhaust later manual requests. Unknown lost admission reply reports request_outcome_unknown/CLI4 with safe identical replay guidance. Existing actual CLI crash tests verify one independent effect.

## Cleanup boundaries to preserve

The streamed restore journal and backup/upload profile leases are implemented and verified as described above. Do not reimplement them. Read `engine/recovery.rs`, `engine/recovery/journal.rs`, `store/{restore_jobs,cleanup_directory}.rs`, `store/restore{.rs,/directory.rs}`, `profile/restore.rs` and `maintenance.rs` when extending cleanup. `Engine::restore_backup` is the production API path; static `Engine::restore_profile` is a caller-owned lower-level helper currently used by integration tests.

New cleanup must keep active blocking work owned after caller cancellation/Engine shutdown, retain activated target keys and uncertain path evidence, avoid store-worker self-deadlocks, retire rows only after durable cleanup, and exclude cleanup authority from backups/restores. The immediate startup cancellation/relative-path regressions above come first. Then cover earlier upload/backup creation and ordinary failed-job retirement. Preserve unrecognized empty directories, existing sources and unrelated files/keys. No prefix sweeps or destructive account purge.

## Remaining full scope

Task13: remaining startup/earlier-artifact process-death cleanup; wider storage/WAL/export error checks; audit historical durable payloads for other writes and disconnect retention/same-identity reconnect. Rich send histories10–21 and every schema commit are verified. No destructive account purge is in scope.

Task14: create/run security_system, resource_system and multi_engine_system suites. Audit actual unresolved defects rather than repeat work: post-submission storage errors, blocking permit lifetime, operation-worker failure, strict IMAP literals/delimiters/case and old capacity limits, aggregate attachments, legacy intents, calendar inheritance and Trash origins. Calendar writerWithoutPrivateAccess is an official role not yet covered; only owner/writer writes are verified. Requirements include3 independent engines,10,000 metadata messages,16MiB attachments,64MiB limits, concurrency2, bounded queues, real RSS/latency evidence and canary/encryption checks.

Tasks03/15: explicit test egress denial (Docker bridge alone is not a firewall); descriptor-generated external client; contract/CLI docs; dependency/security/license checks; package.py, local extracted package verification and CI definitions. Task16: final R01–R16 evidence matrix and operating/recovery/compatibility/risk report. MANUAL-ACCEPTANCE.md exists; live actions remain unapproved/deferred/unverified. Never equate mocks with provider compatibility or local checks with remote CI. No R01–R16/full-goal completion claim yet.

## Durable notes

VERIFICATION retains dated evidence and failures. Full prior checkpoint archived at `docs/history/2026-09-11-sent-and-restore-safety-checkpoint.md`; older reconciliation/IMAP checkpoint also retained. Second-brain project/daily notes were appended for verified Sent, path, and worker gates via /tmp/nuncio-{sent-recovery,restore-path,restore-worker}-memory.py, all successful after one harmless heading assertion correction. No git actions. All earlier shell handles are terminal.

Latest second-brain notes: verified schema22 journal and maintenance leases appended to projects/nuncio.md and daily/2026-09-11.md using /private/tmp/nuncio-{restore-journal,maintenance-lease}-memory.py; both succeeded without git operations. All commands are terminal.
