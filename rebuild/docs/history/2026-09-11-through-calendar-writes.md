# Rebuild session state

Updated September11,2026. Active **unbudgeted** goal: implement all16 tasks and R01–R16. Execute inline; no delegation. User requires **full offline mock services for both Google and Synology for now**. Live acceptance deferred, not verified or a current blocker. No native apps, commits, pushes, merges, installs, publication, remote changes or live-account access. Preserve original source/data/unrelated work. Continue beyond milestones.

## Workspace and authority

- Worktree `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch feature/nuncio-google-first-rebuild, baseline a268fd5a004977b65fd0347ceeb1c69755529aa3. All rebuild source untracked, no commits. Always run implementation tools here, Cargo in rebuild/.
- Approved docs: ../../docs/superpowers/{specs,plans}/2026-09-10-google-first-rebuild.md; audit ../../docs/reviews/2026-09-10-nuncio-audit-and-rebuild.md. Repository AGENTS→CLAUDE and rebuild/AGENTS apply. Read personal rules/style and state after reset. User authorization overrides issue/commit/delegation/replanning defaults.
- History: history/2026-09-10-through-send-enqueue.md, history/2026-09-10-through-audited-resolutions.md, and history/2026-09-11-through-google-mail.md. VERIFICATION.md contains commands/evidence; TODO.md tracks scope.
- Skills already applied: superpowers execution/worktree/TDD/debug/verification, architecture/documentation, secondbrain-save. Do not spawn agents. Stale audit agents are unrelated. Second-brain project/index/daily2026-09-11 updated without commits/secrets through the pre-version Google mail checkpoint. Exact repository state supersedes memory.

## Current checkpoint

**Task10 Google mail reads/writes is verified offline through actual daemon/CLI. Task11 Calendar writes/freebusy is active.** Task09 foundation is implemented but Calendar typed intents/full state coverage remain tied to11. Tasks01/02/04/05/06/07/08/10 checked in TODO. Task03 CI egress denial still owed12/15. Tasks12–16 remain. No R01–R16 fully complete.

Source uses **SQLCipher schema12**. Latest verify.py test-harness binaries use12; production artifacts remain schema11 and must be rebuilt after Task11. Production paths target/production/release/{nunciod,nuncio-cli}; feature paths target/test-harness/debug/{nunciod,nuncio-cli}. They are local development binaries; no packaging/install/signing/live or remote-CI claim. No test process currently needs polling.

## Latest verification

- `python3 test-results/task10/run-gate.py`: **12 commands all exit0**. Both fresh builds, fmt, normal/feature workspace Clippy; normal118 tests/feature118 tests (overlapping), separate mock23/system16/E2E21/operation6/release1. Zero ignored. Exact commands/status/logs in test-results/task10/results.json.
- Subsequent action-file contract correction: CLI files require integer schema_version1 and reject extra fields. Two behavioral reds: missing version accepted; internally tagged serde unit Archive ignored unknown fields. Archive changed to empty struct variant with same JSON shape; strict validation now passes.
- `python3 test-results/task10-action-version/run-gate.py`: **11 commands all exit0**. Refreshed both artifact variants, fmt, both Clippy modes, focused CLI unit1 normal/feature each, engine mail_changes1, separate E2E21/operation6/release1. Full118 counts precede this added unit; no full119 run claimed. Results/logs in that directory.
- Separate verify.py suite logs still exist; runner fails closed for missing required IMAP suites. All provider traffic in automated tests uses synthetic numeric-loopback services. Sandbox escalation used solely for authorized local listeners/builds. No review rejection.

## Implemented foundations

Four production crates plus independent unshipped test-support; CLI has no engine/storage dependency. SQLCipher/WAL-FULL, chunked encrypted blobs, bounded dedicated store worker/owned shutdown, OS-keystore production secrets, authenticated numeric-loopback gRPC v2, synthetic feature-only secrets/endpoints, exclusive profile ownership. Independent mock uses separate wire/state, mailparse rather than engine mail-parser, stateful OAuth/mail/Calendar, out-of-band controls, accepted-send/notification records and before/after failures. Standalone mock-google stdin controls documented. No Synology service started; Docker29.7.2 available earlier.

Gmail full/delta staging atomically promotes projection/cursor, sparse IDs >2^53, deletions/expiry/overlap. Calendar canonical objects preserve unknown JSON/timezones/recurrence/all-day and expanded bounded agenda separately. Owned polling/coalescing/cancellation, per-account lane before global2, persisted Retry-After numeric/date, pause/reconnect/wake, bounded replayable change log. Source disappearance/full-reset preserves prepared drafts and queued sends; operation enqueue/start/finish/resolution events verified through system and actual CLI WatchChanges.

Schemas7–9: optimistic drafts, 64MiB/256 attachments, four bounded uploads, 256KiB RPC chunks/30sec deadline, cleanup after cancellation/death. Reply/reply-all/forward use offline raw originals, recipient arrays/self exclusion, thread/References, preserved MIME/CID/charset/non-UTF8 bytes. mail-builder0.5.0 freezes Date/Message-ID and body at first enqueue. SMTP wire omits Bcc, optional Sent copy retains it. Resend reidentify changes outer Message-ID/Date only, never body/nested messages.

## Journal and send

Schema10 store/{operations,operation_attempts,operation_queries,operation_resolutions}.rs: atomic desired state/intents/frozen blobs, canonical account+request uniqueness. Same ID survives draft edit/delete. Bounded revision-bound lists/history; max1000 attempts/op,16 receipts/attempt. Queued-only cancellation; interrupted dispatch becomes uncertain and reconcile. Nonnegative elapsed retry deadlines clamp to commit-now. Reconciliation absence never means failed send.

record_smtp_acceptance persists final SMTP250 separately (queue ID optional). Sent-copy completion requires prior acceptance; later copy failure cannot relabel SMTP rejected. Reopen unit verified; actual SMTP/IMAP transport still12.

Resolution: optimistic expected version, no active attempt, uncertain/conflict only; one immutable audit, identical retries no new mutation. Abandon retains uncertainty/conflict and stops work. ConfirmApplied currently **Google send only**, requires provider ID + matching frozen RFC Message-ID + timestamp/note; no fake provider attempt/receipt. Resend requires new UUID and explicit duplicate-risk acceptance, atomically creates replacement frozen payload and leaves original uncertain/resend_requested. Calendar/Synology confirmation evidence models still owed. CLI operation show/list/attempts/cancel/wait/resolve; resolve file + version, separate --accept-duplicate-risk flag. Wait30sec succeeds only applied, errors carry last error.operation.

operations.rs owns worker/JoinSet/stop/error, 250ms scan, ≤100 queued account jobs; coordinator/global2 and composition permit for sends. Drains before Store closes. Status operation_worker_error is proto tag9 (sync remains8). Feature checkpoints before_dispatch/after_attempt/before_receipt/job_finished have real process tests.

providers/google/send.rs shared WriteResult<Value> and HTTP write_gmail; Accounts::gmail_write handles encoded path, fixed HTTPS prod or synthetic loopback, no proxy/redirect, **retry::never**. Journal owns all attempts. Sender checked against account for send. Known401 refresh once then new separately journaled dispatch. Auth failure pauses account. Persist provider backoff; bounded jitter retries, max8 ordinary rejects. 5xx/408/lost/malformed/truncated send uncertain. Post-submission bookkeeping failure must remain uncertain.

Positive Sent reconciliation searches rfc822msgid+SENT (100pages/1000uniqueIDs), exactly one match with parsed identity/address/subject/MIME headers and exact body. Provider-added outer headers allowed; MIME/Bcc normalization may leave conservative uncertainty. Accepted is submission/Sent evidence, not final recipient delivery. No blind resend. Actual tests prove compose and reply/reply-all/forward attachment bytes/headers/thread, four concurrent duplicate requests→one send, both crash boundaries, unknown/abandon/manual-confirm/new-resend no third send, numeric/date long backoff after restart/wake and independent other-account send.

## Gmail mutations

New domain/mail_change.rs MailAction Read/Star/Archive/Trash/Label. schema11 mail_change_payloads stores immutable provider message identity/payload plus AUTOINCREMENT sequence indexed by account/provider. store/mail_changes.rs atomically commits intent + desiredJSON + canonical fingerprint. Same ID lookup precedes projection resolution, so deletion cannot erase request identity. Later nonterminal changes for a provider message cannot overtake delayed/uncertain/conflict earlier ones, including equal timestamps. begin_attempt enforces order too; applied/failed/cancelled/abandoned releases it.

AppliedMail validates provider ID and desired labels, replaces complete observed memberships in current local message resolved by provider identity, keeps unknown labels, updates history/providerJSON and commits mail+operation revisions together. It does not advance sync cursor. Bare Applied receipt cannot bypass this. Pending desired state is visible on operation; normal mail reads remain observed.

operations/mail_change.rs uses modify or dedicated trash/untrash paths. Recovery reads minimal message: matching state applies without another POST; otherwise bounded repeatable desired state, missing remote message conflict. Unrelated external labels survive. Existing-label action only active user label of that account; system labels use explicit actions. Mail.ChangeMessage/GetCapabilities; CLI mail change --account --message --request-id --file --wait and mail capabilities. Action files ≤8KiB, require schema_version1, strict fields. RUNNING.md contains examples. All9 state directions, remote/local labels, no extra duplicate writes, account/auth guards, pre400/404/401/429/503 and postdisconnect/malformed/truncated/503, exact crash checkpoints/external edits verified in independent system and actual E2E.

## Next concrete action — Task11

1. **Strengthen independent Calendar mock permission/RSVP conformance before adding production adapter.** Current calendar_state.rs reports owner for all calendars, write path lacks organizer/attendee restrictions. Add controllable accessRole, reader denial, invited-user own-response-only behavior preserving other attendees and notifications. Standalone controls live src/bin/mock-google.rs. Existing mock calendar writes already support stable supplied IDs, If-Match/412, patch merge, generated occurrence edits, notifications, freeBusy. Keep mock independently grounded.
2. Implement versioned Calendar action files and typed engine/store intent: stable create ID, local/provider event identity, expected ETag, explicit single occurrence/whole series (reject following), notification policy, named field patch/RSVP permission validation. Atomic event projection/receipt/agenda-stale semantics. Separate notification outcome evidence: positive event read is not proof of notification delivery; unknown remains visible. Never repeat notifications on lost ack.
3. Add production Calendar HTTP write/freebusy (fixed endpoints/If-Match), operation executor, API and CLI. Actual conflict exit5 and inspectable operation; independent notifications/effects before and after crashes. Read approved Task11/spec for exact scope (already reviewed this turn).
4. Full Synology mocks + pinned strict Dovecot/SMTP local servers12; backup/restore/export/repair13; adversarial/security/resource14; artifacts/contract/CI egress15; deferred live worksheet/matrix16. Continue, do not stop at Google completion.

Primary sources just checked: developers.google.com/workspace/calendar/api/v3/reference/events/{insert,patch}, calendar/api/guides/version-resources. Gmail modify/trash/untrash linked RUNNING. Provider docs do not authorize any live access.

## Remaining risks/bounds

Calendar/Synology manual evidence models; SMTP container encoding/SMTPUTF8/line limits and Sent-copy executor; draft attachment removal/download and max aggregate response; reconcile parsing/base64 async CPU bounds; HTTP2 retry ownership/adversarial post-submission storage failures; storage-worker lifecycle stress (old assertion-unwind SIGSEGV root cause unproven); descriptor golden and external-client smoke; all later scope. Pending action files reject versions/unknown fields, but broader malformed/duplicate JSON parsing belongs adversarial14. No live compatibility or remote CI claim.

## Task11 independent mock update (latest)

Calendar permission conformance now implemented **before** production writes. New test support/calendar_permissions_contract.rs (included by google_mock_contract.rs) first failed with200 instead of403 for non-organizer shared guest flags, then passed. Added Calendar.access_role defaultowner, GoogleControl::set_calendar_role and standalone SetCalendarRole stdin control; owner/writer/reader supported, primary cannot relinquish owner, unsupported role rejected. Lists report actual role. Reader writes rejected beforeeffects; non-organizer guest control flags forbiddenForNonOrganizer. Existing If-Match remains intact. Changes are only in independent test-support; no production Calendar write code yet.

**Important research correction:** Google explicitly permits attendees to edit shared content in their own copy; these may be overwritten by organizer changes. Do not implement a mock blanket organizer-only restriction for summary/description/time. The test proves allowed copy description + own RSVP preserves other attendees/unknown private properties. Engine RSVP must change only selected user's supported response fields; validate its narrower product actions separately. Official sources linked docs/MOCK-GOOGLE.md: calendar/api/concepts/sharing, api/guides/errors, api/concepts/inviting-attendees-to-events.

`python3 scripts/verify.py --suite google_mock_contract`: build0/suite0, **24 passed**, zero ignored. Feature workspace Clippy0 after these changes. No gates running. Latest full system/E2E gate remains Task10; permission scenarios have not been exercised through a production write adapter (not yet built). Next concrete step: Calendar domain/request validation + durable typed intent, then API/CLI/executor/freebusy with independent notification evidence. Maintain mock-first ordering for any new provider behavior.

## Task11 Calendar intent checkpoint — September11

Independent mock now implements native partial RSVP (`attendeesOmitted:true` + selected attendee). Extended permission contract rejects empty/missing/other-person/invalid-status/displayName/unknown-request changes and checks unchanged remote version/notifications. Added unknown existing attendee provider field exposed a behavioral red (400 vs200 on an unrelated patch); corrected validation split: request fields remain strict, untouched attendee provider fields survive merge. Separate verify.py google_mock_contract build0/suite0, **24passed**, log under test-results/google_mock_contract. Production adapter not yet implemented.

Engine domain/calendar_change.rs + calendar_change/fields.rs added after unresolved-import red: required explicit scope/notifications; bounded named fields and ETag, writer/owner checks, organizer-only guest/attendee-list changes, selected-email unique partial RSVP, recurrence master/instance distinction and date/time validation. Official Events reference confirms attendeesOmitted native response update; pre-implementation test was refined from full attendee replacement to the documented partial primitive, while independent tests assert other attendees/provider fields remain intact.

Schema12 calendar_change_payloads stores immutable provider IDs, local identity, patch/base/timezone/precondition and ordered sequence. store/calendar_changes.rs + receipt.rs atomically enqueue canonical request fingerprint/desired state and revision. Stable UUID-hex Google create IDs; typed source-checked AppliedCalendar requires observed event or specific deletion evidence. Receipt applies canonical projection, increments calendar revision and stales agenda in same transaction without advancing sync cursor. Lost-ack positive read with requested notifications leaves uncertain/calendar_notifications_unconfirmed, needs_reconciliation=false; API acceptance means requested policy, never recipient delivery. Bare Applied bypass rejected. Desired state omits full base; durable internal payload retains it. Calendar begin-attempt FIFO guard added. **Calendar is intentionally not admitted by ready_operations until the executor exists**; no API/CLI Calendar writes yet.

Current focused tests: `cargo test --locked -p nuncio-engine --test calendar_intents --test calendar_changes --test calendar_projection --test mail_changes` **exit0:2+2+1+1passed**. Covers source disappearance, duplicate/cross-account, changed request, stale/mismatched receipt rollback, no-notification positive-read success vs notification uncertainty, reopen, create stable ID, deletion, cursor/agenda semantics, order. First store compile red absent interfaces; implementation visibility error corrected. Feature workspace Clippy0 before second intent test; rerun after remaining edits. Existing calendar_schema assertion exposed expected schema11 vs actual12 (exit101); expected migration version and google_system schema assertion updated12, schema test rerun pending. Full workspace gate not rerun since Task10.

Next concrete action: complete Calendar HTTP write and executor, typed proof before safe retries (unchanged ETag or stable create ID), 412 conflict with observed version, delayed reconciliation reads, API/proto/CLI action files and freebusy. Add separate system + actual subprocess tests; no Task11 completion until all required paths and independent remote notification counts/crashes pass. Known followups: receipt comparison must normalize timezone-qualified offset-free dateTime; domain organizer.self=false should not be overridden by an email fallback. Current doc compatibility: owner/writer/reader mock roles only; newly documented writerWithoutPrivateAccess not yet supported. No live provider/CI/packaging claims.


## Latest retry/equality gate

`python3 test-results/task11-intents/run-gate.py` **all4 commands exit0**:fmt,normal+feature workspace Clippy,all engine tests. Exact commands/status/logs in that directory. Engine contains Calendar actions3 + intents6 including recurrence/permission, source disappearance/deletion, notification outcomes, submillisecond time normalization, stable create absence and typed retry evidence. Old schema assertion updated12 and verified separately0. A command initially named nonexistent test target operations (exit101); corrected to operation_recovery/storage/resolution/pages, all passed.

Fixed test-first behavioral failures: organizer.self=false cannot be overridden by matching email; offset-free timezone-qualified dateTime compares to equivalent provider offset response; equality keeps submillisecond precision. Schema12 desired JSON excludes base (internal payload retains full original). New CalendarRetryEvidence Unchanged(full event) or Missing(providerID), payload.permits_retry validates expected ETag/current resource or stable create absence. RepeatableCalendar permitted only during reconcile, persists a calendar_retry positive_read receipt before retry_wait. Generic Repeatable still rejects Calendar. ReconcileLater preserves uncertain+needs_reconciliation and future read deadline; both begin_attempt and ready_operations enforce it. No Calendar dispatcher yet, so ready_operations still excludes calendar_change by kind. No production binary refresh after schema12; test-harness latest verify.py build predates these retry changes.

Next: implement typed Calendar proto action requests/application conversion (public fields use existing EventTime, explicit scope/notification enums and named optional fields/clear semantics; never expose a provider HTTP struct as the public contract), production HTTP and account refresh wrapper, operation worker Calendar lane+safe reconciliation, observed412 conflict. Then CLI action-file schema_version1, freebusy, separate system and actual subprocess notification/crash tests. Continue12–16 afterward.


## Task11 typed API and dispatcher checkpoint (latest)

Calendar.proto now has ChangeEvent with explicit scope and notification enums, typed Create/Update/Delete/Respond, public CalendarEventFields using existing EventTime and optional named fields, explicit clear_fields for nullable fields. No raw provider HTTP structs accepted as public API input. Server calendar/change.rs translates public fields to engine-owned action JSON; Engine.change_event enqueues. Calendar RPC maximum request2MiB (domain1MiB), stop-aware30sec enqueue. CLI Calendar writes still absent; no whole-task completion.

New providers/google/calendar_write.rs issues encoded fixed-root POST/PATCH/DELETE with explicit sendUpdates and If-Match. Bounded1MiB responses; 401/429/backoff, 403 permission vs scope, 412 conflict, duplicate stable ID409→reconcile, malformed/truncated/5xx→uncertain. Accounts Gmail and Calendar now share private google_write credential gate/refresh/backoff with per-API scope, preserving separately journalled auth retry and post-submission storage uncertainty. Ready operations admits Calendar with provider-event FIFO; worker prepares payload and acquires calendar lane. operations/calendar_change.rs validates receipts, reads before retry, uses typed unchanged-ETag/stable-create-absence proof, delayed reconcile reads and bounded attempts. Positive event with notifications remains uncertain, no extra dispatch. Store ObservedCalendarConflict updates canonical observed state plus a calendar_conflict receipt/ETag atomically while preserving desired state.

New support/calendar_write_system.rs included by google_system: first missing-interface red (plus test method/cleanup naming mistakes corrected), then behavior red operation stayedqueued exit101/profile nuncio-system-wmmFAW because dispatcher absent. Dispatcher implemented: targeted create1pass. Expanded two tests now pass (exit0): create with authentication/dedup/remote notification, remoteETag412 conflict preserving intent and observed version, and eight isolated create/update/delete/RSVP lost-disconnect cases with none/all policy. Exact remote version and notification counts, source fields/other attendees, full local/remote equality and positive-read history checked. Added independent HTTP count=1 assertion for each lost response, broader gate pending. Test helper uses a fixed UUID per fresh isolated account, no extra test dependency. Feature Clippy0 before latest expanded tests.

**Running gate:** `python3 test-results/task11-api/run-gate.py`, exec session52589. Eight checks: fresh feature binaries,fmt,normal+feature Clippy,engine suite, independent mock/system/operation suites. Poll and inspect results.json; do not claim passed until done. It does not rebuild production (still schema11) or run actual Calendar subprocess E2E. Latest completed lower-layer gate task11-intents4checks0,engine49passed; mock standalone24passed. Fullworkspace latestTask10. Next concrete action after gate: CLI typed version1 JSON action parser/mapping and actual daemon/CLI Calendar writes, recurrence/all-day/permissions scenarios, create/update/delete crash boundaries and independent notification counters, freebusy query coverage/errors. Then all Task11 gates before Synology12 and later tasks. Live remains deferred, full offline mocks first, no current external blocker.


**Task11 API gate completed:** task11-api/results.json has all8 commands exit0: fresh feature build,fmt,both Clippy modes,engine49,independent mock24,Google system18,operation system6. Zero ignored; the suites overlap and counts must not be summed as unique tests. Latest full workspace/E2E remainTask10. Independent lost-ack HTTP count=1 assertions passed in expanded system cases. No gate currently running. Feature daemon/CLI artifacts reflect current schema12/API; production stillschema11. Continue with Calendar CLI action parser and actual E2E; freebusy, recurrence/permissions/time-edit paths and crash coverage remain.


## Latest Calendar CLI checkpoint

CLI calendar change now accepts --account/--calendar/--request-id/--file/--wait. calendar/action.rs uses direct strict serde decoding (no intermediate Value map), required integer version1/scope/notifications, typed public fields/time/RSVP, explicit clear_fields. Present optional values must be non-null; a regression first accepted summary:null then was corrected with a present-value deserializer. Missing read() compile red and integration helper signature/test Debug mismatches corrected. Focused CLI unit1pass after changes. RUNNING.md includes action examples, fields, scope, permissions, uncertainty and empty DELETE acknowledgement semantics.

Actual support/calendar_write_e2e.rs initial test now passes through real binaries: create with all notifications and dedup, all-day→timed edit with description removal/externalOnly, selected RSVP/no notifications, delete/all; recipient lists and independent remote events/counters checked. `python3 scripts/verify.py --suite google_e2e` build0/suite0,22passed at this checkpoint. Then added 18 crash cases (3 actions×3 boundaries×2policies) in another test. First whole suite22passed1failed (inner101/runner1): delete after pre-HTTP death eventually gets empty204 ack, so local tombstone has no newETag; test incorrectly expected byte-identical provider resource. Official Events.delete verified empty success body. Corrected this branch to assert exact last-known snapshot minusETag+cancelled status, no inventedETag, independent remote cancelled/newETag, exact one mutation/version increment and notification count. Other receipt-bearing paths keep full local/remote JSON equality. No production code/asserted remote-effect requirement weakened. Failed artifacts: test-results/google_e2e/runs/google-e2e-ERX6s6.

**Currently running:** verify.py --suite google_e2e exec session38788; poll and record before claiming crash suite passes. Fresh feature binaries include Calendar CLI; production remains schema11/unrefreshed. After this gate, pending Task11: actual CLI conflict exit5 with observed412 version, recurrence single/series edits, permission/time/action-file guard E2E, more Calendar retry/error cases, freebusy engine/API/CLI with coverage/errors, full normal+feature/production/CI-equivalent gates. Then full Synology mocks/independent servers12 and13–16. Do not stop at Calendar scaffold or mark any Rcomplete.


**Calendar crash rerun completed:** `python3 scripts/verify.py --suite google_e2e` fresh build0/suite0, **23passed/0failed/0ignored**. Includes all18 Calendar crash combinations and prior22tests. Exact one mutation, one remote version increment, selected notification count, stable create identity and expected operation state proven after SIGKILL/restart. Empty204 deletion acknowledgement branch asserts honest no-ETag tombstone; lost-ack deletion reconciles full remote body. No processes/gates pending. CLI parser+E2E changes still need fresh lint gates after last edits. Next concrete work: independent recurring-exception/whole-series inheritance mock conformance before new recurrence E2E (inspect CalendarState Event/put and calendar_recurrence::expanded), actual CLI412 conflict and permission/action guards, freebusy engine/API/CLI, finalTask11 gates. Full task/R requirements remain incomplete.


## Task11 free/busy and CLI acceptance checkpoint (latest)

The recurrence contract and actual CLI series/moved-instance test pass: stable occurrence identity/originalStartTime, Oct25/Nov1 DST offsets, whole-series update, one moved occurrence, wrong-scope guards, independent versions/event counts/notifications. Separate Google E2E before freebusy:24passed/build0/suite0. Latest actual E2E after freebusy/conflict/permission tests: **26passed0failed0ignored**, build0/suite0 in test-results/google_e2e. CLI412 exits5 with inspectable desired intent/current observedETag, identical retry produces no second PATCH, reader and nonorganizer guest/list changes reject with zero effects.

Freebusy domain/free_busy.rs, CalendarSync::free_busy, Engine::query_free_busy, typed proto QueryFreeBusy, daemon handler and calendar free-busy CLI implemented. Strict version1 128KiB file, explicit offset-qualified bounds<=366days/IANA zone/1..50distinct provider calendar IDs; groups unsupported. Bounded4MiB HTTP and20000period result; online checked_at + aggregate/per-calendar complete/errors; missing/malformed coverage rejected; unknown error reasons retained. Optional provider groups must be empty. GoogleRead enum refactors existing HTTP GET/authenticated read path to also support POST freeBusy under the same fixed root/credential gate/backoff; freebusy uses account coordinator and30sec stop-aware RPC.

Test-first reds: missing domain/RPC compile errors; E2E initial invocation lacked binary env (harness setup failure), corrected invocation then failedCLI2 for missing subcommand; output serializer whitelist compile error corrected. Domain1passes; independent new freebusy contract1 + existing recurring contract1 pass (mock total25). System freebusy auth/partialcoverage1passed. Expanded calendar_write_system **5passed16filtered**, exit0: eight write-fault cases (before401/403/429/503/disconnect, after503/malformed/truncated); six freebusyfaults + persisted429 deadline across engine restart; independent one-event/version/effects assertions. Harness stop_daemon typo fixed to shutdown. Feature workspace Clippy0 after these tests. Production still old until following gate.

**Running fullTask11 gate:** python3 test-results/task11/run-gate.py (session ID in current tool output).12commands: both artifact builds,fmt,both Clippy modes,fullnormal/feature workspace tests,separate mock/system/E2E/operation/release. Poll/check results.json before declaringTask11checked. RUNNING/MOCK-GOOGLE updated. Docker engine29.7.2 confirmed under scoped escalation; no Dovecot/Mailpit images present, no new service started. Official Docker docs opened for Task12. Next: resolve fullgate; then pinned independent localDovecot+Mailpit services with synthetic users/localCA and strictprotocol/fault harness BEFORE Synologyadapter. FullTask12–16 remain, live acceptance deferred/not currentblocker.
