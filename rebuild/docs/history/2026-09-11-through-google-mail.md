# Rebuild session state

Updated September 11, 2026. Active **unbudgeted** goal: implement all 16 approved tasks and R01–R16. Execute inline; no delegation. User requests **full offline mocks for Google and Synology for now**. Live acceptance is deferred, not verified or a current blocker. No native apps, commits, pushes, merges, installs, publication or live-account access. Preserve original source/data and unrelated work.

## Workspace and authority

- Worktree: `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. All rebuild source is untracked; no commits.
- Implementation is the separate `rebuild/` Rust 2021/1.97.1 workspace. Run Cargo there. Four production crates plus independent unshipped test-support. CLI has no engine/storage dependency.
- Approved spec/plan: `../../docs/superpowers/{specs,plans}/2026-09-10-google-first-rebuild.md`; audit `../../docs/reviews/2026-09-10-nuncio-audit-and-rebuild.md`. Read repository AGENTS→CLAUDE, rebuild/AGENTS, personal rules/style and state docs after reset. User approval overrides issue/commit/delegation/checkpoint defaults; do not replan.
- Detailed history in `history/2026-09-10-through-send-enqueue.md` and `history/2026-09-10-through-audited-resolutions.md`. VERIFICATION.md records exact commands/evidence; TODO.md tracks scope. No R01–R16 is fully complete.

## Current checkpoint

Task 09 foundation is implemented; Task 10 **Google send works through the real daemon/CLI against the independent mock**. Gmail send/mutations now work offline; Calendar writes, Synology, maintenance, remaining resilience, full gates and final artifacts are still required. Continue beyond this milestone.

Source schema **11**. Test binaries `target/test-harness/debug/{nunciod,nuncio-cli}` are built through basic send/lost-ack recovery/audited resolution plus the elapsed-deadline fix and explicit HTTP retry ownership (latest operation_system build). The current test binaries include the 401 refresh fix and all four write-worker checkpoints. Production artifacts `target/production/release/{nunciod,nuncio-cli}` still represent Task 08/schema 6; do not use those on a schema-10 profile. No Task 09/10 final production build or full normal/feature workspace gate yet.

### Preserved foundations

Tasks 01/02/04/05/07 verified offline. Task 08 reader gate in `test-results/task08/` passed 11 commands, normal/feature workspace 85 tests each (overlapping), independent mock22/system16/E2E10/release1. SQLCipher/WAL-FULL, encrypted chunked blobs, bounded dedicated store worker/owned shutdown, OS-keystore production secrets, authenticated numeric-loopback gRPC v2, isolated feature-only synthetic secrets/endpoints, and separate actual subprocess harness exist.

Google reader sync stages pages/generations and promotes projection/cursor atomically; delta/full-reset reconciliation handles sparse history IDs >2^53 and deletions. Canonical Calendar objects retain unknown JSON, timezones/recurrence/all-day semantics; separately expanded rolling/windowed agenda. Owned polling/coalescing/cancellation, per-account sequencing before global 2-account admission, persisted Retry-After seconds/date, pause/reconnect, wake catch-up and bounded replayable change stream exist.

Independent Google service has no normal dependency on engine/proto, uses mailparse rather than the engine's mail-parser, stateful OAuth/mail/Calendar, independent accepted-send/notification records, standalone stdin control, and before/after faults. Latest full mock contract is now **23 passing** (not 22). No Synology services have started; Docker 29.7.2 was available earlier.

### Draft and operation storage

- Schemas 7–9: optimistic local drafts, bounded uploads with reserved RAII staging cleanup and startup crash cleanup, immutable account-owned blob references. Attachments ≤64 MiB aggregate /256 parts, RPC256KiB chunks, four uploads, 30-second deadline. Reply/Reply-All/Forward use offline originals; correct recipient arrays/self exclusion, references/thread context, preserved MIME parameters/CID and exact non-UTF-8 attachment bytes. Header injection rejected.
- `domain/submission.rs`: mail-builder0.5.0, explicit sender, frozen Message-ID/Date, correct MIME containers, explicit base64 leaf attachments. SMTP Bcc-free wire and optional Bcc-retaining Sent copy; Gmail submission includes Bcc. `reidentify` changes only outer Message-ID/Date using parser offsets, preserving exact body/nested-message bytes. Container attachment transport validation remains for SMTP/Task14.
- Schema10 `store/{operation_schema.sql,operations,operation_attempts,operation_queries,operation_resolutions}.rs`: atomic operation/desired state/frozen blobs, account+request uniqueness, canonical request fingerprint, immutable original after draft edits/deletion. Bounded lists/history bind account/query/page-size/revision; receipts have observation timestamps. Queued cancellation only; dispatch/reconcile attempts separately journaled; one unfinished attempt/account.
- Eight states enforced. Interrupted attempts become uncertain and reconcile; unknown reads never prove non-delivery. Applied Google receipts require provider ID; manual_confirmation cannot use automatic attempt finish. Safe rejected attempts may retry; elapsed provider deadlines are clamped to commit time, negative deadlines rejected. Repeatable retry allowed only for mail_change. Typed mail/calendar mutation payloads and their transition acceptance remain for Tasks10/11.
- `record_smtp_acceptance` durably stores final SMTP acceptance before copy. Queue ID can be absent (SMTP does not require it). Sent-copy completion requires prior acceptance; copy failure cannot relabel accepted SMTP as rejected. These journal substeps are unit-tested across reopen, not yet connected to IMAP/SMTP transports.
- Resolution requires expected version, no active attempt and uncertain/conflict state. One immutable terminal audit records decision/evidence/version/replacement. Identical retries produce no extra mutation. Abandon retains uncertainty/conflict with abandoned disposition. Current ConfirmApplied is Google-send-only and requires provider ID, matching frozen RFC Message-ID, observation timestamp and note; applied/manual_confirmed preserves actual attempts unchanged. Other provider evidence models still owed. Resend requires fresh UUID + duplicate-risk acceptance, atomically creates a separately queued frozen replacement and leaves original uncertain/resend_requested.

### Google send worker and CLI

- `src/operations.rs` owns worker task, JoinSet, stop channel and error status. It scans connected Google send candidates every250ms, one queued job/account (≤100 accounts), uses shared sync Coordinator/global2 and shared single composition permit. Shutdown drains jobs before Store close. Health exposes operation_worker_error (proto System tag9; existing sync is tag8). No background write task is detached. Test read-polling disable does not disable explicit queued writes.
- `store::ready_operations` uses row_number per account to avoid one account's backlog hiding another. It admits queued/due retry_wait/uncertain+needs_reconciliation, excludes resolved/paused/non-Google. Current dispatch supports send only; mutations must extend it.
- `providers/google/send.rs` posts frozen base64url MIME to messages/send through fixed production endpoints or synthetic loopback test config. `Accounts::gmail_send` uses credential gate, refreshes expired access, checks frozen sender still matches selected account, persists provider guidance, pauses invalid auth. Latest fix refreshes after a definitive 401 and returns a rejected attempt for a later separate dispatch, instead of forcing immediate reconnect. Targeted test now passes with one code exchange plus one refresh and two journaled attempts. Full suite reruns are pending/latest tool results.
- reqwest's implicit retry policy is disabled with retry::never; journal owns attempts. Known preacceptance/connect/400/401/403/429 failures are distinct from ambiguous sends. Retry backoff has jitter, at most8 ordinary rejected attempts; positive Retry-After is persisted. 5xx/408/lost/malformed/truncated response are uncertain, never blind resend. Failure persisting post-submission guidance stays uncertain rather than becoming a safe failure.
- Reconciliation searches `rfc822msgid:` with SENT and includeSpamTrash, bounds100pages/1000uniqueIDs, verifies matching account-scoped remote ID/SENT label, parsed identity/address/subject/MIME headers and exact body bytes, requires one match. It permits provider-added outer headers; normalizing MIME/body/Bcc may leave conservative uncertainty. Absence/ambiguity/error stops automatic reconciliation with no_positive_send_evidence. No final-recipient-delivery guarantee is claimed.
- Feature checkpoints operation_before_dispatch, operation_after_attempt, operation_before_receipt, and operation_job_finished now have actual subprocess coverage. Crashes before HTTP leave zero sends and uncertain status; crashes after acceptance reconcile the single remote send. Editing after enqueue preserves original transmitted bytes. Cancellation while held before dispatch wins with zero remote sends. Existing E2E kills after mock independently accepts and withholds acknowledgement.
- CLI `mail send ... [--wait]`; `operation show/list/attempts/cancel/wait/resolve`. Wait30seconds, success only applied; failures include error.operation. Resolve takes --account/--operation/--version/--file, resend additionally --accept-duplicate-risk with warning. RUNNING.md includes commands/JSON examples and provider acceptance versus delivery distinction.

## Latest verification and current failures

All automated credentials/accounts are synthetic and tests use numeric-loopback services; scoped sandbox escalation was used for listeners. No remote CI/live compatibility claim.

- `python3 scripts/verify.py --suite google_mock_contract`: build0/suite0, **23 passed**. Logs/results in test-results/google_mock_contract/.
- `python3 scripts/verify.py --suite google_e2e`: build0/suite0, **15 passed** at Google-send/resolution checkpoint. Actual CLI send with To/Cc/Bcc/binary attachment, frozen metadata/receipt, repeat after death, independently one send. New crash loop covers positive read, unknown+abandon, unknown+manual confirmation, explicit resend after original draft deletion; exactly one original acceptance, exactly one additional acceptance only after resend, same decision after another restart creates no third send. Latest elapsed-deadline/HTTP-retry/sender/401 refinements were afterward; rerun before claiming current binaries pass all15.
- Full `--suite operation_system` now **5 passed**, exit0, after elapsed-deadline fix and HTTP retry/sender changes, before newest 401 case. Fault case independently checks before400/429, afterdisconnect/malformed/truncated/500; every acceptance count accompanies journal assertions.
- `cargo test --locked -p nuncio-test-support --test operation_system google_send_system`: initially passed1/4filtered, then full suite caught timing race. After 401 test added, it failed at retry_wait/authorization_required (profile nuncio-system-gZacIL); fix now passes targeted1/4filtered. The first refined assertion counted grouped request rows; summing RequestCount.count fixes the evidence query and retains exact expected2. Full system/E2E/Clippy reruns are pending.
- Engine operation_storage/pages/recovery/resolution/smtp_operation/frozen_message: **8 passed**, exit0 before elapsed-deadline addition. Updated pages/recovery/smtp: **3 passed**, exit0 after fix. Normal workspace Clippy0 before latest fixes; feature workspace Clippy0 after explicit HTTP retry/sender work, before 401 change. Full gates remain pending.
- Relevant behavioral reds fixed: missing mail send --wait (CLI2), missing operation wait (CLI2); receipt with no provider ID; Sent copy without SMTP acceptance; expired Retry-After caused InvalidInput and left running. Deterministic pages test confirms expired deadline means immediate eligibility. Full system failure profile nuncio-system-6sybQ1 preserved. Header test originally expected bare addr-spec but RFC-valid angle brackets were present; switched to independent mailparse address decoding with exact recipient/count checks. No assertions/timeouts were weakened.
- Compiler fixes: new proto field initially collided with existing tag8 (now9), mutable stop borrow (separate receiver clone), new AppError.operation literals, static test request UUID instead of adding dependency, cloned client channel/token for wait. These are not behavioral red evidence.

## Latest continuation evidence

- Current `python3 scripts/verify.py --suite google_e2e`: build0/suite0, **17 passed**, zero ignored. Includes prepared forward and queued operation surviving source-message deletion during full sync; pre-dispatch cancel; both worker crash boundaries; post-enqueue edit immutability. Actual CLI WatchChanges verifies audited decision revision/order and both linked resend records. Logs/results in test-results/google_e2e/.
- Current feature workspace Clippy: exit0. Current targeted operation_system google_send_system: **1 passed /4 filtered**, including exact enqueue/start/finish change-stream revisions and 401 refresh evidence. Full operation_system **5 passed** before the added WatchChanges assertions; final full gates remain pending.
- Tasks06 and08 final cross-feature assertions now pass. Task09 optimistic draft/frozen-byte requirement passes. Task10 remains active.

## Next concrete action

1. Implement typed Gmail mutations (read/star/archive/trash/untrash/existing-label add/remove) through store/worker/API/CLI with separate system and actual subprocess tests. Preserve provider identity and requested changes across projection resets; serialize pending changes for each message, including delayed earlier operations. Observed labels remain observed until acknowledged; pending desired state lives atomically with the operation.
2. Finish remaining send acceptance: actual reply/reply-all/forward delivery/thread assertions, connected concurrent duplicate requests, durable long Retry-After across crash/wake. Complete Task09/10 full gates after mutations; no early completion.
3. Calendar writes/freebusy11, full Synology mocks and independent strict servers12, maintenance13, adversarial/bounds14, artifacts/CI15, deferred live worksheet/matrix16.

## Outstanding bounds/compatibility work

Current positive reconciliation deliberately requires byte-identical MIME body and selected header equality; live Gmail may normalize it and remain uncertain. Applied is API acceptance/Sent evidence, not recipient delivery. Manual evidence for Calendar/Synology remains unimplemented. SMTP container encoding/SMTPUTF8/line limits, draft attachment removal/download, max aggregate draft API response, parser/reconciliation CPU and memory bounds, storage-worker lifecycle stress (old assertion-unwind SIGSEGV mitigation not proven root cause), API descriptor golden, CI egress denial, and fresh production packaging remain. Source uses SQLCipher10 while old production binary is6. All live acceptance is deferred; no credentials are requested now.

## Gmail mutation continuation checkpoint

All Gmail actions are now implemented through engine/worker/store/API/CLI. New domain/mail_change.rs MailAction; store/mail_changes.rs EnqueueMailChange/MailChangePayload/MailChangeReceipt; schema11 mail_change_payloads with AUTOINCREMENT sequence and account/provider index; Atomic desiredJSON + request + payload. Same ID is deduplicated before resolving current projection so deletion cannot erase identity. Later nonterminal changes for the same provider message cannot overtake an earlier delayed/uncertain/conflict operation; abandoned/applied/failed/cancelled releases it. begin_attempt guards ordering too.

AppliedMail requires correct provider ID and satisfied desired labels, installs complete remote labels into current local projection by provider identity, retains unknown collection identities, updates history/providerJSON, records mail then operation revision in one transaction. It does not advance sync scope cursor. Bare Applied receipts cannot bypass these checks. Normal local reads remain observed; queued desired state is on the operation.

operations.rs now branches PreparedWrite into Send (bounded frozen memory) or Change. operations/mail_change.rs dispatches modify or specific trash/untrash. Recovery reads minimal message; positive desired state applies without another write, otherwise bounded safe repeat, missing remote message conflict. Google shared response enum is now WriteResult<Value>, HTTP write_gmail uses encoded path segments; Accounts::gmail_write includes optional send identity check, common auth/backoff behavior. Existing send regressions pass.

Mail proto ChangeMessage has typed oneof Read/Star/Archive/Trash/Label and GetCapabilities. CLI mail change takes --account/--message/--request-id/--file/--wait. Capability fields indicate label model and no folder move/copy. CLI JSON input currently lacks the explicit version required by the spec: **add required schema_version:1 before closing Task10** (this was spotted while reviewing spec). Enforce file version and update examples/tests; retain strict unknown-field validation. API itself is versioned v2; field numbers additive.

Newest checks: engine mail_changes/pages/recovery/smtp4 pass; separate operation_system6 pass; separate google_e2e21 pass (all zero ignored). New actual send test verifies reply/reply-all/forward headers/thread/arrays/attachments and four concurrent duplicates each resulting in one acceptance. New backoff test loops numeric/date one-hour guidance, death/restart, other-account send, synthetic wake, exactly one acceptance/account. Feature Clippy passed before latest reply/backoff tests.

**Running:** `python3 test-results/task10/run-gate.py`, shell session18529. This copies Task08 full gate plus separate operation_system (12 commands total), builds fresh production and feature artifacts. Inspect results.json and logs. Do not edit source during this gate. After it finishes, add explicit action-file schema_version and focused regression then rerun appropriate final checks. Update TODO/plan Task10 only after full relevant passes. Task09 retains Calendar typed-intent obligation with11.

Next: validate action version; finish gate; implement Calendar writes/freebusy per Task11 already reread. Stable create ID, If-Match, explicit scope/notification, named-field patch, RSVP permissions, independently counted notification effects, no blind replay after loss. Synology Task12 has not begun; full mocks and strict independent servers still required. No live account access requested.

## Final Task10 gate and immediate continuation

The full Task10 gate finished: all12 commands exit0, normal/feature workspace118 tests each (overlapping), independent mock23/system16/E2E21/operations6/release1. Artifacts rebuilt to schema11. Logs test-results/task10/. After that, required file schema_version validation was added. Unit red accepted an unversioned archive; next red accepted unknown fields on the serde internally tagged unit Archive variant. Changed Archive to an empty struct variant in CLI/domain (same JSON shape) and now strict validation passes. CLI strips and requires integer schema_version1 before deserializing the action. Updated all action-file E2E examples and added invalid version/extra-field rejection with zero operations.

Current follow-up gate: `python3 test-results/task10-action-version/run-gate.py`, shell session89657. It rebuilds production/test binaries, fmt, both Clippy modes, focused action-file tests normal/feature, engine mail_changes, E2E21, system operations6 and release isolation. Inspect results before starting Calendar code. This focused follow-up avoids needlessly rerunning unrelated workspace tests after CLI-only validation and equivalent Archive representation.

Task11 preparation only (no Calendar-write code changes yet): reread task/spec and official Events.insert/patch/version-resources docs. Mock currently exposes accessRole owner for every fixture and lacks organizer/attendee mutation enforcement; strengthen independent mock roles/RSVP conformance **before** the production Calendar adapter. Add read-only denial and own-response-only validation preserving other attendees, then provider-client tests. Stable client chosen event IDs, If-Match update/delete, explicit occurrence/series scope, versioned action file, notification policy/effect uncertainty and authoritative projection receipt are required. Notifications must have separately visible requested/unknown evidence; a matching event does not prove notification delivery. No live compatibility claims.

Second-brain project/index/daily2026-09-11 updated with the approved active scope and pre-version full gate; no commits or secrets. The exact repository state supersedes that checkpoint. No real accounts or Synology services accessed. Continue into11/12 after the current follow-up gate.
