# Historical IMAP transfer implementation checkpoints

# Rebuild session state

Updated September 11, 2026. **Active unbudgeted goal: all 16 tasks and R01–R16.** Work inline; no delegation. Latest user steering requires full local mocks for Google and Synology now. Live acceptance is deferred/unverified, not a current blocker. Continue offline implementation beyond milestones. No commits, pushes, merges, installs, publication, remote changes or live-account actions. Native apps excluded; preserve original source/data/unrelated work.

## Workspace and authority

- Worktree: `/Users/james.maes/Git.Local/KofTwentyTwo/nuncio/.worktrees/nuncio-google-first-rebuild`, branch `feature/nuncio-google-first-rebuild`, baseline `a268fd5a004977b65fd0347ceeb1c69755529aa3`. Cargo cwd `rebuild/`. Original checkout remains `dev`. Rebuild source/docs remain untracked; no commits.
- Approved spec/plan: `../docs/superpowers/{specs,plans}/2026-09-10-google-first-rebuild.md`; audit `../docs/reviews/2026-09-10-nuncio-audit-and-rebuild.md`; GOAL/PLAN-nuncio-rebuild. Read root AGENTS→CLAUDE, rebuild/AGENTS, personal rules/style and local state after reset. Existing approval overrides issue/replan/commit/delegation defaults.
- Detailed prior implementation maps and chronological evidence: `history/2026-09-11-through-calendar-writes.md` and `history/2026-09-11-mailplus-through-account-transport.md`. TODO/VERIFICATION retain exact check history. Skills already applied: execution/worktree/TDD/debug/verification, architecture/documentation/simplicity/secondbrain-save. Ignore stale audit agents.
- Secondbrain `/Users/james.maes/Git.Local/KofTwentyTwo/second-brain`: existing Nuncio project and daily2026-09-11 updated through schema15 Archive gate and explicit Move/Copy focused tests using `/tmp/nuncio-transfer-memory.py`, scoped escalation exit0. No secrets or git operations. Repo state is authoritative.

## Immediate next action

**Newest: schema16 Trash/restore is implemented and focused tests pass.** `imap_trash_schema.sql` migration adds encrypted `imap_trash_origins` keyed by account+exact provider placement, independent of reconstructible message IDs. `imap_transfers/origins.rs` validates/loads/records original placement+folder name; captured optional restore_origin defaults absent for legacy schema15 intent JSON. Transfers into configured Trash capture original folder identity/epoch; copies within Trash inherit known origin. Publication records origin atomically with destination/local receipt; a completed Move removes source origin. No-op preserves it. Restore refuses unknown origin in Trash or retired/name/epoch-changed original folder, and uses an explicit Move when a user needs to choose a different destination. Already outside Trash and no recorded origin is a no-op. No native purge.

CLI Trash(true/false) already existed; IMAP dispatcher/capability is now wired. New actual test `imap_trash_e2e.rs` initial build0/suite101 (3passed1failed) unsupported action exit2. Implementation fresh build0/E2E4passed0 for native original-Archive restoration across before-publication SIGKILL, repeated Trash no-op, restart/full resync and exact raw bytes/MOVE2. It now also loops hidden-MOVE fallback (first execution in pending gate). FeatureClippy initially101 missing restore_origin None in protocol fixture, corrected; featureClippy0. Separate `imap_write_system/trash.rs` restore_requires1passed0 across3profiles covers unknown/retired/epoch origins with no extra wire writes. copies_into1passed0 proves copies into/within Trash inherit origin and restore after reopen/fullsync while identical placements stay distinct (COPY2/MOVE1). Schema-version assertions now16. Full9-command trash gate running exec18194; inspect result before further source changes. Source/test schema16; production remains Task11/schema12.

Next after trash gate: SMTP transport + durable independent Sent-copy bookkeeping. Accepted SMTP must never be resent because APPEND failed. Unknown DATA/COPY/APPEND require visible uncertainty or independently justified reconciliation, with remote SMTP/mailbox counters in separate system and actual CLI crash tests. Then Tasks13–16/CI egress denial; no full scope or live compatibility claim.


**Current work newer than Archive gate: explicit Move/Copy is wired and focused tests pass.** Domain actions, additive API oneof fields9/10 and MailFolderChange, CLI version1 action files and daemon mappings. Capture resolves an active account-scoped destination collection UUID and freezes folder identity/epoch. Existing schema15 transfer journal/executor/projection handles it. Gmail rejects folder actions. New actual E2E3passed0 includes exact quoted/Unicode/backslash names, same-folder copy distinct identities, move plus no-op, request replay, raw-byte equality and remote COPY2/MOVE1. New system folder_actions1passed0 covers foreign/invalid/missing destination and missing remote folder conflict without wire mutations. Feature workspaceClippy0. Nine-command folder gate complete: `test-results/task12-folders/results.json` all9commands0; Rustfmt/bothClippy, proto-cli4, engine69, IMAPsystem11, actualIMAPE2E3, Googlesystem21, actualGoogleE2E26. No failed/ignored. Fresh feature builds and copied nested evidence retained. Schema15 source/test; production remains Task11/schema12. No running process. Next trash/restore with original placement metadata, then SMTP. Next implement trash/restore origin metadata then SMTP; no replanning or renewed authorization required.


Schema15 Archive is wired end-to-end. Typed immutable source/destination placement and epochs, durable started/copied/deleting/expunging/source-removed progress, COPYUID proof/receipt, native MOVE or safe COPY+UID EXPUNGE fallback, atomic destination MIME/FTS/attachment projection and source removal with final receipt. No broad EXPUNGE. Unknown copy identity is never blindly retried; only a proven unstarted transfer can return to dispatch after recovery. Already-archived Move is a positive-read no-op, preserving message/attachment IDs. Capabilities now expose archive plus read/star; explicit move/copy/trash/restore/SMTP remain pending.

Locations: `store/imap_transfers{.rs,/progress.rs,/apply.rs}`, `store/imap_transfer_schema.sql`, `store/mail_projection.rs`, `store/operation_attempts.rs`; `providers/imap/transfers{.rs,/tests.rs}`, `operations/imap_transfer.rs`, `operations.rs`; system `tests/imap_write_system/mod.rs`, subprocess `tests/support/imap_transfer_e2e.rs`. Transfer parser retains validated early COPYUID on lost final ACK, rejects malformed/contradictory/incorrectly tagged mapping; nine-case unit passed. Source/destination equality no-op skips network mutation. Attachment upserts preserve existing part IDs.

Latest checks: engine storage/schema6+1 passed0, engineClippy0, feature workspaceClippy0 (before final no-op/test additions), focused archive system1 passed0 across native/fallback/no-op cases, strict COPYUID unit1 passed0 across9 protocol cases. Actual archive E2E initially failed101 twice: stale operation_before_receipt.entered from earlier flags made wait return early. TestControls explicitly requires old entered/release cleanup. Added shared test arm helper clearing those markers; fresh harness build0/E2E1passed0 now proves two archive SIGKILL boundaries (before local publication and before dispatch) with independent exact MOVE counts and equal-MIME distinct placements preserved. Original failing log retained in `test-results/task12-archive/stale-barrier-failure.log`.

Newest E2E has five native/fallback crash substeps: after durable COPY proof, after accepted STORE, EXPUNGE, COPY, MOVE with withheld ACK. Remote target bytes/count and unrelated Deleted UID are independently asserted. Known proof resumes deletion without recopy; unknown proof stays visible uncertainty over two restarts. First new run build0/suite101 (1passed1failed), missing operation_after_imap_copy checkpoint. Added feature-gated checkpoint after record_imap_copy with stop receiver threaded from work_stop, corrected new test CLI subcommand to operation show. Fresh rerun build0/E2E2passed0, including prior read/flag/archive crash cases. Missing-boundary failure retained at task12-archive/missing-checkpoint-failure.log.

Separate system regression `imap_write_system/transfers.rs` added: native early COPYUID + disconnect applies via positive read; fallback lost COPY acknowledgement remains uncertain with one copy; absent MOVE+UIDPLUS rejects before copying. Initial compile101 optional-name assertion type, then test101 missing explicit harness shutdown before restart; both corrected using actual API. Focused1passed0 across3profiles. No production workaround was needed. Eight-command archive gate complete: `test-results/task12-archive/results.json`, all8commands exit0. Rustfmt/both workspaceClippy, engine69, IMAPsystem10, actualIMAPE2E2, Googlesystem21, actualGoogleE2E26; zero failed/ignored. Fresh feature builds and copied nested logs/results retained; test counts overlap. Source/test schema15; production artifacts still Task11/schema12. No live compatibility or remote CI claim. No running process. Next explicit move/copy API/CLI using captured destination collection identity, then trash/restore origin metadata and SMTP. Test artifacts schema15; production artifacts still Task11/schema12.

Historical detail: `history/2026-09-11-transfer-groundwork.md` preserves superseded intermediate next actions and schema14 gate details. No stage or mocks alone complete Task12. Next broader work is explicit move/copy API/CLI and trash/restore with prior-placement metadata, SMTP delivery plus separate Sent-copy bookkeeping; then Tasks13–16 and CI egress denial. Live compatibility remains deferred and unverified.

## Progress and evidence

Tasks01/02/04/05/06/07/08/10/11 checked offline. Task03 CI egress denial remains; Task09 journal exists but typed Synology actions/full state coverage remain. Task12 active; Tasks13–16 pending. **No R01–R16 fully complete.**

- Task11 full gate: `test-results/task11/results.json`, all12commands0. Normal142/feature142 tests (overlapping), separate Googlemock25/system21/actualE2E26/operation6/release1, fresh builds/fmt/bothClippy. Calendar writes/freebusy including18 crash scenarios verified offline.
- Task12 independent mock gates: `task12-mocks` all9commands0; `task12-mocks-followup` all6commands0. Python17 contracts; Rust IMAP2 wraps those. Earlier Google25/21/26 regressions passed. Zero failures/ignored. Later capability-spacing fix verified again in account gate.
- Task12 account gate: `test-results/task12-accounts/results.json`, **all12commands0**. Both Python checks, Rustfmt/both workspaceClippy, engine52, CLI/proto4, IMAPcontract2 (wraps Python17), IMAPsystem2, actualIMAPE2E1, Googlesystem21/actualGoogleE2E26. Copied nested suite logs/results alongside gate. Counts overlap, no failures/ignored.
- Task12 transport follow-up: `test-results/task12-transport/results.json`, **all6commands0**: Rustfmt/bothClippy, engine59, IMAPsystem2, actualIMAPE2E1. This predates the latest explicit completion check. Latest focused completion unit1passed/exit0 covers6 scenarios; its full gate is pending above.
- Current schema14; feature artifacts `target/test-harness/debug/{nunciod,nuncio-cli}` rebuilt during gates. Production artifacts `target/production/release/{nunciod,nuncio-cli}` remain Task11/schema12; no install/packaging/signing claim. Passing mocks proves neither live compatibility nor remote CI execution.

## Existing engine and Google implementation

Four production crates plus independent unshipped test-support. CLI has no engine/storage dependency. Numeric-loopback authenticated gRPCv2; production OS keystore; feature-only synthetic secrets/endpoints/clock/checkpoints; exclusive profile; SQLCipher WAL FULL and encrypted chunked blobs; bounded store worker. Account lane before global2, owned/cancellable polling and operation workers, persisted per-API retry deadlines, change feed.

Google account identity pin/refresh/reconnect/disconnect; staged Gmail full/delta and Calendar canonical/expanded projections; bounded MIME/blob reads and local search/coverage. Draft CRUD/uploads/reply/reply-all/forward and immutable frozen MIME. Schema10 durable journal/attempts/receipts/resolution, schema11 typed Gmail mutations, schema12 Calendar intents. Unknown send outcomes reconcile via positive exact-MIME/Message-ID evidence; no blind resend. Google manual send confirmation requires providerID+MessageID+human observation; retry as a new operation requires explicit duplicate-risk flag. SMTP acceptance and Sent-copy journal substeps exist but have no dispatch transport yet.

Calendar stable create IDs, conditional ETags, explicit Single/Series scopes and notification policy, partial selected-attendee RSVP, permissions/FIFO; receipt and canonical projection update atomically, agenda becomes stale. Lost notification acknowledgements remain uncertain even when event content matches; no repeated invitations. Freebusy has explicit per-calendar/aggregate coverage, bounded timezones/window/count/results and no reservation claim. Post-existing-exception series inheritance remains a mock compatibility gap. Read detailed map/history before modifying these areas.

## Independent MailPlus mock services

Read `docs/MOCK-MAILPLUS.md` for complete controls and operating commands. Implementation `tests/imap/`, no engine/proto imports. Rust harness `crates/nuncio-test-support/src/imap.rs` runs the actual standalone Python process with private inherited control pipes and retained evidence.

- Pinned official images already downloaded: Dovecot2.4.5 `sha256:c807be4fb5a97d9c3a90770569d3a6c4cbdcb36742ad41f90409cbd929166553`; Mailpit1.31.1 `sha256:98b916bd3c8d61f7633a52d3ea2f58d00620cb01ca57ab59edde68c347a95365`. Docker29.7.2/Compose5.5.1/OpenSSL3.6.4/Python3.14.7.
- Rootless/read-only Dovecot with only shipped-file-cap-required SYS_CHROOT; two independent Mailpit captures for STARTTLS and implicit TLS. Numeric-loopback ephemeral ports, local synthetic CA, private random alpha/beta/observer credentials. Auth/TLS required, no relay/RDNS/capture deletion; Mailpit version check disabled.
- Docker local Unix socket is validated and pinned for all mutations/cleanup. Unique owned projects; reset recreates disposable backend data while retaining credentials/CA/stable proxy ports. Ordinary isolated bridge is **not an egress firewall**; host/container denial remains Task03/15. No real accounts used. Early pre-disable version checks are not evidence of zero background update traffic.
- Proxy forwards framing/SASL/literals/IDLE to real server state; before/after-accept disconnect/withhold/reject/truncated FETCH faults. After-accept faults consume only actual positive acknowledgements. Optional capabilities hidden/enforced; hidden CONDSTORE implies hidden QRESYNC. Unknown ENABLE forwarded as unknown; RFC5161 empty ENABLED added only after backend OK. Capability filtering now removes token+separator together; duplicate-space regression caught by strict parser and independently fixed.
- Controls seed/reset deterministic two-account mailboxes/UIDVALIDITY/UID/raw bytes, mutate flags/UID epochs/folders/copy/scoped expunge, inspect SMTP count/raw/metadata, inject/wait/release faults. Independent SMTP envelope sender/recipients/wire hash/size observations. Mailpit adds Bcc/trace headers; tests assert exact additions plus unchanged submitted MIME suffix. IMAP raw bytes must match entirely.
- Optional AutoSent polls real captures/authenticated-user tags and APPENDs to owner's real Dovecot Sent. Unknown/failing APPEND stops visibly, never blind retry; explicit reset restarts. Duplicate Message-IDs remain separate captures/copies. This simulates a configurable policy, not actual MailPlus behavior.
- Full standalone bounded strict JSON controls over private pipes, atomic readiness, EOF/SIGTERM cleanup including reset races. Python runner bounds time, kills owned descendants (including TERM-ignoring children), records per-project cleanup. Rust harness explicit shutdown/Drop fallback; credentials never echoed. All17 Python contracts passed.
- Ruff0.16.7: `UV_TOOL_DIR=$PWD/target/tool-cache/tools uv tool run --offline --cache-dir target/tool-cache --from ruff==0.16.7 ruff ...`; workspace-local tool/cache. Initial default-tools sandbox failure was resolved with the scoped directory. Docker/loopback need authorized narrow escalation; no automatic approval-review rejection.

## Current production IMAP/SMTP account and transport foundation

- Domain `domain/imap_account.rs`: strict public config, required implicitTLS/STARTTLS, exact username+canonical IMAP host/port/TLS principal pin, explicit Sent policy/folders, optional PEM CA. Zeroizing separate credentials and redacted Debug. Store `imap_accounts.rs`, schema13 `imap_account_schema.sql`: public config/capabilities in SQLCipher, credentials only SecretStore with prepared cleanup intent before put/activation; old credential cleanup failures surfaced separately.
- `accounts/imap.rs`, Engine and daemon Accounts handlers: authenticate both servers before save, check/reconnect/disconnect; credential gates and coordinator concurrency. Temporary generic IMAP NO/SMTP4xx preserve credentials. Only explicit IMAP authentication-failure code or SMTP auth-failure reply invalidates credentials. Missing TLS trust cannot send AUTH. Capability snapshot is advertised support, not proof that all operations are implemented.
- Proto `accounts.proto` ConnectImap/GetImapConfig with typed config/credentials/capabilities, secret Debug redaction and ZeroizeOnDrop. CLI `accounts/imap.rs`: version1 public config rejects passwords/unknown fields; `account connect-imap --config FILE --credentials-stdin [--account UUID]` takes bounded nonterminal credential pipe, never argv/config passwords. `account imap-config`, check/list/disconnect. RUNNING documents getpass pipe and current limitations. Configured folder existence is not checked yet.
- `providers/imap/{mod,smtp,wire,command}.rs`: rustls/WebPKI plus explicit CA, full hostname verification,30s probe/command deadlines. IMAP SASL PLAIN; SMTP PLAIN/LOGIN with each challenge classified. SMTP uses lettre's response parser behind bounded framing (lettre connection itself checks read_line size only after allocation). Mixed multiline reply codes, malformed endings and unterminated oversized lines reject.
- `ImapWire` validates complete <=64KiB framing lines and announced literal length before async-imap can allocate; bounded overall bytes and up to64MiB literal content (zero-byte limit during auth), preserves binary bytes. `set_limits` refuses an unfinished frame. Literal helper had a leftover authentication-only rejection (behavior red101), fixed; binary/oversized tests pass. Future sync must select appropriate per-command budgets and handle bounded large UID search responses.
- **Critical library caveat:** async-imap0.11.3 `parse.rs::filter_sync` discards final status in high-level CAPABILITY/LIST/SEARCH/FETCH collectors. Do not trust them. New `command::complete` uses library tags/parser but independently requires exact matching tagged OK before returning typed collected data; NO/BAD/wrong tag/BYE/EOF/continuations fail, maximum4096 responses and30s deadline. Read capabilities now uses this path. Regression failed after extracting old helper, then passes6 cases including positive control. Private ResponseData and non-Clone capability assumptions caused compile101; corrected with typed visitor/public Capability::from. Generic command errors are sanitized; future write executors must still classify unknown dispatch outcomes as uncertain.
- Dependencies added async-imap0.11.3,lettre0.11.23,tokio-rustls0.26/ring/tls12,webpki-roots1,rustls-pemfile2.2; log max_level_info/release_max_level_info compiles wire traces out in every build. Cargo fetch --locked --offline0 confirmed cache/lock. No normal environment install.
- `domain/imap.rs` + `identity::ImapMailboxId`: explicit account/mailboxUUID/nonzeroUIDVALIDITY/UID placement and versioned unambiguous JSON key; strict modifiedUTF7 and INBOX-only casefold; safe sorted/deduped UID batches/no empty ranges/u32 wrap; preserved unknown keyword flags and injection guards. Domain4passed. These helpers now back sync and schema14 storage.

## Next implementation and remaining work

1. Finish current completion gate and log final evidence, then implement staged mailbox discovery and explicit placement storage. Local mailbox UUID is immutable; match only active exact decoded names (INBOX casefold only). Disappearance/rename retires the old identity; never guess identity from Message-ID. Provider UIDVALIDITY resets require staged replacement, preserving durable drafts/operations.
2. IMAP full/delta, folder discovery, explicit checkpoints/placements, atomic promotion, quiet/empty-inventory and actual CLI reads are implemented; finish the read gate and explicit fetch/bounds review before durable mutations.
3. Typed read/star/archive/trash/restore/move/copy, retained prior placement, missing-folder conflicts and safe UID-scoped expunge. Never issue broad EXPUNGE that can delete unrelated mail; report unsupported without safe fallback. SMTP acceptance and Sent APPEND are separate journal steps; lost send/COPY/APPEND acknowledgements require positive proof or visible uncertainty, not blind replay. Both server/client Sent policies need system/subprocess crash tests and independent effects.
4. Then backup/restore/export/repair13; adversarial/security/resources14; local production packages/contracts/CI egress15; manual worksheet/R-matrix16. Live acceptance remains deferred and requires later named authorization.

Known later audits: storage-worker unwind SIGSEGV root cause unproven; post-submission store failures; bounded reconciliation CPU/HTTP2 retries; SMTP encoding/SMTPUTF8/line canonicalization; attachment remove/download aggregate bounds; schema fixtures, descriptor golden/external-client smoke. Test PDF fixtures are synthetic markers, not yet validated renderable PDFs. Keep all evidence claims scoped to actual offline checks.


Eight-command archive gate complete: `test-results/task12-archive/results.json`, all8commands exit0. Rustfmt/both workspaceClippy, engine69, IMAPsystem10, actualIMAPE2E2, Googlesystem21, actualGoogleE2E26; zero failed/ignored. Fresh feature builds and copied nested logs/results retained; test counts overlap. Source/test schema15; production artifacts still Task11/schema12. No live compatibility or remote CI claim. No running process. Next explicit move/copy API/CLI using captured destination collection identity, then trash/restore origin metadata and SMTP.


Explicit folder move/copy now implemented after actual CLI red101 (2existingpassed/1newfailed, unsupported action exit2). Added MailAction Move/Copy and additive proto oneof fields9/10 with MailFolderChange; daemon/CLI mapping, account-scoped captured destination UUID/epoch and IMAP move_copy capability. Gmail explicitly rejects folder actions. Schema remains15; existing Archive/flag intents unchanged. Capture shares the proven transfer executor/projection. New actual E2E `imap_folder_e2e.rs` proves copy to quoted/Unicode/backslash folder, copy in same folder with distinct placements, explicit move back and same-folder no-op, exact request replay/raw bytes and remote COPY2/MOVE1. Fresh `python3 scripts/verify.py --suite imap_e2e` build0/suite0,3passed. Separate system `folder_actions`1passed0 proves foreign/empty/missing destination rejection, missing remote folder conflict and zero COPY/MOVE/STORE/EXPUNGE. Feature workspaceClippy0. Nine-command full folder gate running exec86339; poll before changing source. No full-current-gate claim until result. Next trash/restore with original folder metadata, then SMTP and Tasks13–16.


Nine-command folder gate complete: `test-results/task12-folders/results.json` all9commands0; Rustfmt/bothClippy, proto-cli4, engine69, IMAPsystem11, actualIMAPE2E3, Googlesystem21, actualGoogleE2E26. No failed/ignored. Fresh feature builds and copied nested evidence retained. Schema15 source/test; production remains Task11/schema12. No running process. Next trash/restore with original placement metadata, then SMTP.


Nine-command Trash/restore gate complete: `test-results/task12-trash/results.json` all9commands0. Rustfmt, normal and feature workspaceClippy, proto-cli4, engine69, IMAPsystem13, actualIMAPE2E4, Googlesystem21, actualGoogleE2E26. Zero failed/ignored. Fresh feature builds and copied nested suite logs/results retained. Native and hidden-MOVE fallback Trash/restore both pass, including before-publication SIGKILL, no-op and full-resync origin retention. Source/test schema16; production artifacts still Task11/schema12. No process running; no live compatibility or remote CI claim. SMTP is next.
