# Historical transfer groundwork and superseded next actions

## Immediate next action

**Current active transfer work (newer than gates below): schema15 storage groundwork implemented.** `store/imap_transfers{.rs,/progress.rs}` adds strict flags-or-transfer payload union (existing flag JSON unchanged), immutable Archive source/destination folder IDs/names/epochs, copy/move modes, typed COPYUID proof and durable dispatch/copied/deleting/expunging/source-removed phases. `imap_transfer_schema.sql` and migrations version15. `record_imap_copy` validates source/destination/epoch, persists proof plus an imap_copy receipt before deletion; restart preserves it. Start cannot repeat once a transfer was started. Generic Repeatable now rejects transfers; post-copy Rejected was exposed by new behavioral red101 and now rejects any transfer already marked started. Two schema_version assertions updated to15.

New `tests/imap_projection/transfers.rs` initially compile-red101 on missing types/APIs, then focused1passed0; engineClippy0; combined calendar_schema1+imap_projection6 passed0 before additional Rejected guard. Additional test failed101 because post-copy rejection was allowed; guard implemented, focused combined rerun6+1passed0. No running process from that check. No full regression gate for schema15 yet.

**Temporary incomplete wiring:** Archive now enqueues a typed transfer, but `operations.rs` still loads only ImapFlagPayload for IMAP mail_change. Do not run/rely on archive system as implemented: dispatch must switch to ImapChangePayload and call the new transfer executor. No production transfer network function exists yet. Atomic transfer projection application now exists: `imap_transfers/apply.rs`, `mail_projection.rs`, boxed AppliedImapTransfer outcome. It requires the persisted exact COPYUID proof and terminal source-removal phase for moves, validates destination folder/epoch/UIDNEXT, installs encrypted MIME/headers/attachments/search/placement, removes exact original placement for moves, and commits with the operation receipt without advancing global coverage. Expanded storage regression initially compile101 missing result/outcome; implementation first compile101 visibility/missed re-export, fixed; focused1passed0. EngineClippy latest poll recorded next. Test does not yet independently stream copied local raw bytes or negative/rollback receipt variants; add these during integration. Next concrete action: provider COPY/MOVE response collection preserving early COPYUID on missing final ACK, then wire executor through ImapChangePayload. Known copy progress must resume source deletion/scoped expunge and destination fetch, never the copy. Only prepared progress may safely return to dispatch after a crash; generic Repeatable and post-start Rejected are blocked at storage. Handle native MOVE success and COPY+UIDPLUS fallback, with no broad EXPUNGE. A COPYUID may arrive before terminal EOF; retain validated partial proof only without falsely treating the whole command as acknowledged. Preserve progress across target-fetch/storage failures, never recopy. Full move/copy/trash/restore APIs, prior-placement restore metadata, fallback safety and crash/E2E cases remain. Feature executables still latest schema14 build, production Task11/schema12.


**Task12 read gate complete: `test-results/task12-reads-recheck/results.json`, all11commands exit0.** No running process. Engine64, independentIMAP2 (wraps Python17), IMAPsystem5, actualIMAPE2E1 (three SIGKILL boundaries), Googlesystem21/actualE2E26; no failed/ignored. Both Python checks, Rustfmt/both workspaceClippy and fresh feature builds included. Nested logs/results copied beside gate. Initial failed gate preserved at `task12-reads`.

Explicit IMAP single-message fetch is implemented after system red101/InvalidArgument. Targeted authenticated EXAMINE/metadata/BODY.PEEK, epoch guard, fetch-specific promotion preserves other messages/mailboxes and global scope checkpoint. `imap_read_system/fetch.rs` focused1passed0; actual CLI `mail fetch` added to E2E fresh build0/suite0; storage rejection regression4passed0. Source: `providers/imap/sync.rs` shared ingest+fetch, MailSync dispatch, `store/imap_mailboxes/promotion.rs::validate_fetch` and shared promotion.

Sparse UID inventory correction implemented after protocol test101 (old UID-space query differs from expected sequence-range query). `read::examine` now returns `(ImapMailboxState, EXISTS count)`; `read::inventory` pages sequence positions in4096-entry ranges intersected with captured UIDNEXT. RFC3501 section6.4.8 explicitly defines this SEARCH intersection. Handles UID4billion in one query; count<=1million bounds at245pages, total parsed UID set exact count, zero/out-of-range/duplicate UIDs fail, unsolicited expunges/changed count abort collection. Final EXAMINE count/epoch/UIDNEXT/modseq and full inventory equality still required. Focused unit1passed; expanded to6 positive/negative reply cases for the next gate. Full IMAPsystem6passed0 after sequence paging. No schema increment (still14). Expanded sparse UID unit6 cases passed in engine gate.

**Seven-command fetch/paging gate complete:** `test-results/task12-fetch/results.json` all7commands0. Engine66, IMAPsystem6, actualIMAPE2E1, Googlesystem21; no failed/ignored. Fmt/both workspaceClippy and fresh feature builds included. Nested logs/results copied. No running process. Read11-command prior gate all0 remains historical evidence.

**Task12 read/star writes verified offline: eight-command flags gate all0.** New `store/imap_changes.rs` typed immutable placement/name/Seen-or-Flagged desired state in existing mail_change_payloads (schema14 unchanged). Account-scoped request fingerprints/FIFO reused; `AppliedImapFlags` validates placement/desired receipt and atomically updates projection/flags/receipt/state. Operation readiness/worker now dispatch IMAP mail_change only, leaving SMTP send disabled. Scope is imap. Provider `imap/flags.rs` does strict LIST/SELECT (READ-WRITE required), UIDVALIDITY check, explicit single UID metadata, +/-FLAGS.SILENT and observed flags. Unrelated flags preserved. Missing/epoch changed conflicts; lost write acknowledgements reconcile via EXAMINE; failed reconciliation stays uncertain with bounded deadline. Only positive desired-state absence allows idempotent flag retry. No-op positive desired state applies without STORE. Capabilities expose read/star only.

New system test2passed0: all four read/star desired states after accepted STORE disconnect, each exactlyone remote request/accept and positive-read receipt; failed EXAMINE reconciliation stays uncertain/local flags unchanged, later positive read with no repeated STORE; account isolation; already satisfied state no-op; stale UIDVALIDITY conflicts without touching reused UID. Initial test compile101 missing uuid dependency fixed deterministic request UUIDs; expected red101 missing capability; first implementation run101 only because attempt API orders descending, corrected assertions to explicit ordinal (no weakened counts).

New storage test1passed0: request replay/conflicting reuse, FIFO, cross-account rejection, wrong flags/account/epoch receipt rollback, valid atomic receipt and reopen. First compile101 called incorrect Store method names, corrected to existing get_mail/get_operation. Full `imap_projection` now5 tests. Actual `python3 scripts/verify.py --suite imap_e2e` freshbuild0/test0: expanded one suite test includes three new flag SIGKILL boundaries operation_after_attempt, operation_before_receipt, remote after-STORE withhold; independent count exactlyone per operation, local flags unpublished before receipt, concurrent Answered preserved after restart. Prior three read SIGKILL cases remain. Feature artifacts rebuilt schema14. Workspace normal Clippy exit0.

**Flags gate complete:** `test-results/task12-flags/results.json`, all8commands0: fmt/bothClippy, engine67, IMAPsystem8, actualIMAPE2E1 (six total read/write SIGKILL boundaries), Googlesystem21/actualE2E26. Nested logs/results copied alongside gate. No failed/ignored. No running process. Next concrete action: implement explicit IMAP folder move/copy/trash/restore with durable substeps. Lost COPY/MOVE/APPEND acknowledgements need positive proof or visible uncertainty; never use flag Repeatable logic for transfers. Production artifacts remain Task11/schema12. No running process before gate start. Full task12/goal incomplete.

**Independent MOVE fault groundwork:** added `tests/imap/test_proxy.py::IndependentFaultProxy.test_native_move_lost_ack_preserves_copyuid_and_unrelated_deleted_messages`. Focused test1passed/0: rejected missing-target MOVE does not consume after-accept fault; later native MOVE loses final acknowledgement, retains forwarded COPYUID, copies exact bytes once and preserves unrelated Deleted UID2. Request2/accept1 and no broad/scoped expunge from client. Initial invocation1 wrong Python cwd, second1 because imaplib capabilities are strings, corrected assertion type; actual behavior passes without proxy changes. Five-command follow-up `test-results/task12-move-mock/results.json` all5commands0: Pythonlint/format/Rustfmt, featureClippy, independentIMAPcontract2 (wraps all18 Python tests, with all owned cleanup exit0). Nested Rust/Python logs/results copied beside gate. No running process. Do not rerun the known-red archive system test as a passing-suite claim.

**Newest test is intentionally red; gate above predates it.** Added `imap_write_system::archive_uses_native_move_or_scoped_copy_expunge_and_preserves_unrelated_deleted_message`. Command `cargo test --locked -p nuncio-test-support --features nuncio-engine/test-harness --test imap_system archive_uses_native -- --nocapture` exits101/InvalidArgument at enqueue because IMAP Archive is unimplemented. No process remains. Test expects native UID MOVE when advertised and UID COPY + UID-scoped EXPUNGE when MOVE is hidden; UID2 already Deleted must remain. Independently compares destination raw bytes, counts commands, prohibits broad EXPUNGE, requires new local Archive placement and request replay. Implement rather than removing/ignoring this test.

Transfer implementation decisions to carry forward (no code yet): preserve mailbox/UID epoch intent and per-placement FIFO across flags/transfers; extend typed IMAP payload dispatch without interpreting transfer intent as flags. Persist transfer dispatch phase and COPYUID proof before any separate source deletion/expunge. Only known copy proof allows resuming remaining substeps; an unknown COPY/MOVE is never a flag Repeatable retry. Positive MIME equality alone cannot prove which client created a copy. Native MOVE can emit untagged OK COPYUID before EXPUNGE and final tagged completion; collect/validate that mapping explicitly. NO may accompany partial MOVE effects, so it is not automatically no-effect proof. A missing COPYUID must remain explicit in state/compatibility and never become invented identity. Read-only sync staging currently only supports full/delta/fetch; do not fake a fetch or change global coverage to publish a transfer. Add a shared validated message-publication helper or typed transfer application that atomically publishes observed destination content/source removal with the operation receipt, while preserving restartable known-copy progress if target fetching fails. Review capture/retention and trash restore origin during that implementation. Move/Copy API fields and CLI action files, archive/trash/restore dispatch, safe no-extension behavior, crash/ambiguous-ack system/E2E coverage all remain required.

Primary transfer references verified directly: RFC4315 sections2.1/3 (UID EXPUNGE affects only selected Deleted UIDs; COPYUID maps source/destination UID sets/epoch; mapping may be omitted), https://www.rfc-editor.org/rfc/rfc4315.html ; RFC6851 sections3.3/4.3 (MOVE intermediate/partial outcomes and untagged COPYUID before EXPUNGE), https://www.rfc-editor.org/rfc/rfc6851.html . Use existing imap-proto0.16.7 ResponseCode::CopyUid(u32,Vec<UidSetMember>,Vec<UidSetMember>), with single-source/single-destination nonzero/range-cardinality validation. Current generic command collector drops collected values on error; transfer evidence needs deliberate preservation without accepting a missing/mismatched terminal tag as success.



Folder issue: imap-proto quoted strings retain escape bytes, but literals must preserve their backslashes. Added original-buffer callback to strict command collector and `providers/imap/read/names.rs` to decode LIST string representation. First system rerun still101 because ResponseData's owner includes trailing response/spare bytes; extended unit reproduced101 and now uses parser-consumed prefix before decoding. Unit tests cover equivalent quoted/literal names and literal double backslashes; system1 covers Unicode/quote/backslash folder, rename and account isolation. These fixes retain strict grammar and exact identity assertions. Initial failed gate and copied nested logs preserved.

Schema14 IMAP projection/storage and network ingestion are implemented. `store/imap_mailbox_schema.sql`, `store/imap_mailboxes{.rs,/promotion.rs,/cached.rs}`, `providers/imap/{read,sync}.rs`, IMAP-only authenticated session in accounts/provider and MailSync/scheduler dispatch. Full and delta enumerate folders/UIDs, strict tagged completion, metadata batches256, BODY.PEEK only for missing MIME (full refetches), SQLCipher cached MIME staging for delta; explicit absent UID deletions. Reserved local mailbox UUIDs match only active decoded exact names, INBOX-only casefold; UID epoch reset/newfolder never guesses message identity. Complete catalog/checkpoint/messages promotion is atomic. Stable snapshot hash for quiet delta; nonselectable/retired/changed-epoch placements removed; foreign staged collection/membership rejects with rollback. Recovery cleans staged mailbox catalog.

Focused storage3passed, engineClippy0; engine suite63passed after sandbox-only loopback EPERM was rerun with authorized narrow escalation. Read system initial InvalidArgument101, then nonexhaustive enum compile101; extension attributes already include backslash in imap-proto, doubled prefix caused invalid_provider_response101, fixed according to downloaded parser. Diagnostic helper had unused/mutability compile errors101, corrected. IDLE hiding is not a supported mock profile, so test uses documented four optional extensions; both profiles pass all raw/flags/deletion/UID reset assertions. System4passed before new fifth folder/isolation test. `python3 scripts/verify.py --suite imap_e2e` freshbuild0/test0, one actual test expanded with exact raw/PDF download, saved draft and three SIGKILL boundaries before stage/before publication/after publication across UIDVALIDITY resets. Independent mailbox count/epoch and SMTP0 checked. Feature binaries now schema14; production remains Task11/schema12.

Independent truncation fault regression proved metadata-only FETCH incorrectly consumed a body-literal fault (Python1error1 after an initial wrong cwd/class invocation1). `tests/imap/proxy.py` now reinserts truncation fault when no literal occurred; independent regression1passed0. Do not weaken grammar or assert truncated-literal coverage without this fix. All detailed commands/statuses in VERIFICATION; gate will retain logs/results.

Next after gate: close read gaps (sparse UID inventory bounds, large content and API presentation review), then typed IMAP read/star/archive/trash/restore/move/copy with safe scoped expunge and missing-folder conflicts. SMTP durable send/APPEND/reconciliation after that, then Tasks13–16. Do not end at read-only milestone.
